model: "gpt-5.4-mini"
# Translator: reasoning_effort=none + temp 0.3 (LOCK §2.2(v)).
# No retrieval / no memory inject / no rolling / no critic — pure S0 baseline.
temperature: 0.3
seed: 20260612
reasoning_effort: "none"
verbosity: "low"
# Window output in Vietnamese is longer than English source; cap at 4096.
max_output_tokens: 4096
daily_token_cap: 2400000
pricing:
  input: 0.25
  cached_input: 0.025
  output: 2.00
