# THESIS WORK LEDGER — sổ truy vết công việc

> **Cold-start cho agent mới (đọc theo thứ tự, KHÔNG cần đọc gì khác):**
> 1. `../../THESIS_ARCHITECTURE_LOCK.md` — mọi quyết định kiến trúc + changelog.
> 2. File này — trạng thái mọi việc, commit tương ứng.
> 3. Mở đúng `TASK_*.md` đang liên quan.

## Quy ước (chốt 2026-06-11, LOCK §8.2)

- **1 việc = 1 file** `TASK_<Px>_<nn>_<slug>.md` theo `TASK_TEMPLATE.md`. Spec + imple
  + review sống chung 1 file. Vòng đời: `READY → IMPLEMENTING → REVIEW → DONE / REWORK`.
- **Phân vai:** Claude viết §1–§4 (spec + acceptance) → user đưa CodeX imple, CodeX điền
  §5 (implementation notes + test output) → Claude review điền §6 (verdict) → cập nhật
  bảng dưới.
- **Acceptance criteria (§4) phải là lệnh chạy được** (pytest/script), không phải mô tả.
- **Commit:** `P0-01: tóm tắt` (1 task = 1 commit chính); tài liệu → `docs:`; đồ clone
  → `vendor:`. Tag mốc phase: `P0-done` … `P5-pilot`. Nhánh dài hạn: `thesis/main`.
- **Quyết định kiến trúc thay đổi** → ghi LOCK §10 changelog, KHÔNG ghi ở đây.
- **Gotcha kỹ thuật** → §5 của task; ảnh hưởng dài hạn → thêm cột Ghi chú + (nếu là
  quyết định) LOCK changelog.
- Tasks AI-LAB cũ (clone theo) nằm ở `_ailab_legacy/` — chỉ tham khảo, không làm theo.

## Bảng trạng thái

| Task | Tóm tắt | Status | Commit | Review | Ghi chú |
|---|---|---|---|---|---|
| P0-01 | Vệ sinh clone + skeleton pipeline/ + migration schema v3 | DONE | `P0-01:` (xem git log) | PASS (Claude, 2026-06-12) | 4/4 migration + 88/88 smoke; DDL khớp spec |
| P0-02 | LLM client: pin model + seed + reasoning_effort + replay cache + quota | DONE | `P0-02:` (xem git log) | PASS (Claude, 2026-06-12) | 11/11 tests; chặn alias latest từ config; **P0 hoàn thành** |
| P1-01 | Nguồn TI sạch (strip annotations) + loader document.json → blocks | DONE | `P1-01:` (xem git log) | PASS (Claude, 2026-06-12) | 23/23 tests; source 40ch/1476 blocks tracked + PROVENANCE sha256; chốt chặn Directional Lock (raise nếu còn annotations) hoạt động; trục block_id thesis↔oracle pinned |
| EV-01 | Module TAR/ECS/FVR + đo lần đầu trên oracle output | DONE | `EV-01:` (xem git log) | PASS (Claude, 2026-06-12) | 23/23 tests; **số đầu tiên: TAR 0.8866 / FVR 0.0 / ECS 0.9195**; 3 caveat đọc số ghi ở §6 task (Jim=artifact narrator, glossary termhood, occurrence provider) |
| P2-01 | World Builder pre-pass: trích T1–T4 từ text trắng, pilot ch02–ch03 TI | DONE | `P2-01:` (xem git log) | PASS (Claude, 2026-06-12) | 29/29 tests; **GO/NO-GO #1: GO** (json_fail 0.0); probe API → LOCK (v) (minimal không tồn tại, temp chỉ với effort=none; sửa llm_default); 6 findings phải vá ở P2-02 (§6 task — nặng nhất: prompt hardcode "Jim Hawkins" = tri thức ngoài văn bản) |
| P2-02 | Fix prompt + re-run pilot + Span Resolver + persist T1–T4 + FREEZE | DONE | `P2-02:` (xem git log) | PASS (Claude, 2026-06-12) | 37/37 tests; **bằng chứng tự học: ch02 narrator vô danh → ch03 học tên "Jim" từ text**; FREEZE probe DB thật OK (memory chặn, runtime ghi được); timeline xuyên chương OK; 2 zero-occurrence = apostrophe ’ vs ' (follow-up nhỏ task kế); **P2 pilot HOÀN THÀNH** → tag `P2-done` |
| P3-01 | S0 end-to-end: windower + dịch ch02+ch03 + đo S0-vs-oracle cùng thước | DONE | `P3-01:` (xem git log) | REWORK→PASS (Claude, 2026-06-13) | **Milestone "dịch được" GVHD ĐẠT**: 81/81 blocks, 31 windows, $0,021; **S0 TAR 0.4151 vs oracle 0.6226 cùng 53 pairs** (số đã tái tính độc lập); ECS 0.76≈0.77; mẫu đắt giá "ngực người chết" vs "rương người chết"; REWORK do agent ngoài bịa số/mẫu + giả danh reviewer — CodeX làm sạch; quy tắc mới: headline số phải được reviewer tái tính |
| P4-01 | Embedding client + dựng 3 Chroma collections từ memory frozen | DONE | `P4-01:` (xem git log) | PASS (Claude, 2026-06-13) | 68/68 tests (tự chạy); 81 passages / 8 motifs / 0 TM; rebuild $0 (89 cache hits, tự xác minh); memory_meta pin model+dim; smoke query "old captain at the inn" → top-5 đúng vùng, deterministic; cost thật $0.0008 |
| EV-02 | Trục ngữ nghĩa/văn phong: judge Gemini (pairwise A/B + GEMBA) + MATTR + hiệu chuẩn | DONE | `EV-02:` (pending) | PASS (Claude, 2026-06-13) | 11/11 + 86/86 tests (tự chạy lại); **pairwise S0 10 / tie 59 / S1 12 / n=81 — tái tính độc lập từ evaluation_runs, KHỚP**; `calibrated=false` (chưa human → chưa trích hội đồng); **F1 block rác: b001 "I"=số chương La Mã, S1 dịch "Tôi" SAI mà judge cho thắng → block-pairwise có nhiễu; headline dùng holistic + lọc token tối thiểu**; **F2 GEMBA S0>S1 là đánh-đổi thật (S1 mua TAR bằng chi phí tự-nhiên nhỏ), diagnostic-only**; F3 route qua proxy ShopAIKey (cross-provider giữ ở cấp model, cost≠giá Google official); F4 313 call/pilot → LOCK (bb) bắt buộc sample; guard+key-no-log audited; COMET→EV-03 |
| P4-02 | Context Builder (anchor → hard constraints) + chạy S1 + số S0/S1/oracle | DONE | `P4-02:` (xem git log) | PASS (Claude, 2026-06-13) | 75/75 tests (tự chạy); **S1 TAR 1.0 / S0 0.4151 / oracle 0.6226 cùng 53 pairs** (tái tính độc lập); ECS 0.7556→0.8111; b005 "ngực người chết"→"Rương Người Chết" (slide demo); **phát hiện: TAR BÃO HÒA ở S1 → LOCK (z)**, phân hóa S1→S3 phải đo ECS+judge, không TAR; $0,022 |
| P1-02 | Nạp nguồn D2L (EN gốc→blocks) + glossary gold EVAL-ONLY + báo cáo coverage chọn chương | DONE | `P1-02:` (pending) | PASS (Claude, 2026-06-13) | CodeX implemented; 11/11 targeted + 97/97 full tests; real ingest: 23 source chapters / 22 loaded (`chapter_references` no `_origin.md`) / 165 sections / 8803 blocks / 4609 prose / 458 gold terms; `glossary_entries=0`, `eval_glossary_gold=458` guard OK; report `data/reports/d2l_ingest_coverage.json`; top density chapters led by attention/linear/GAN/conv-modern; agent flag appears in introduction + preliminaries |
| P2-D2L | World Builder D2L chế độ thuật ngữ kỹ thuật + chẩn đoán C (Builder-vs-gold) + FREEZE | DONE | (P2-D2L:) pending | PASS (Claude, 2026-06-14) | CodeX completed registry-bloat rework with prompt `d2l_terminology_v7`: D2L extraction omits registry context, consolidation handles consistency, UTC quota guard + preflight + per-call prompt cap added. Baseline copied to `data/_baseline/d2l_marker_v6_registry_bloat_20260614/`. Tests: targeted `17/17` for LLM+D2L, full `107/107` pass. Rerun used `OPENAI-KEY-2` (new separate account/key; key not logged). DEV gate: recall **0.500**, agreement **0.800**. Full 4-ch benchmark completed + frozen: `glossary_entries=1608`, `memory_frozen=1`, `eval_glossary_gold=458`; usage **762,586** benchmark tokens, **856,016** total DEV+benchmark tokens / 255 calls on 2026-06-13 UTC. Prompt/call bounded: intro avg 1252.7, prelim 1431.6, linear 1383.8, MLP 1378.8, max 2146 < cap 6000. Final C: recall **0.742331**, agreement **0.743802**, `agent -> tác nhân` captured; 31 conflicts + 1487 extra terms need Claude review before PASS. **PASS (Claude tái tính): recall 0.123→0.742 (121/163), agent→tác nhân captured+khớp gold; token-bloat HẾT (prompt/call avg 1365, max 2146 vs cũ 10.8k; benchmark 762k); frozen=1, gold 458, Directional Lock giữ; 107 tests. 31 conflicts TOÀN biến thể hợp lệ (Builder đôi khi hơn gold; gold có typo) → bằng chứng (dd). Follow-up P3-D2L không chặn: siết termhood (~20-30% của 1487 extra là non-term) + chuẩn hóa source-term + bơm canonical-only cho Translator.** |
| P3-D2L | Dịch S0/S1 4 chương D2L + chấm headline B(TAR-vs-gold)+D(consistency) + A/C chẩn đoán + judge sample | DONE | pending | PASS (Claude, 2026-06-14) | CodeX implemented staged P3-D2L, no commit/push. Added `technical_d2l_v1` document_profile, D2L base prompt `s0_d2l_v1`/`s1_d2l_v1`, heading+prose filter, passthrough code/math/image/label, profile-aware injection `occ>=2 + translate-only + canonical-only`, eval-only `data/eval/d2l_gold_variants.csv`, D2L B/D/A scorer, `--preflight-only`, `--configs`, `prompt_token_cap=6000`, and offline tests. Stage 1 preflight full 4ch: 169 windows / 1302 translated-scope blocks; passthrough 553; max prompt S1 2258 < cap 6000; upper total all configs 1,797,743 < 2,400,000. Stage 2 pilot preliminaries passed mechanical gate, JSON fail 0, usage 160,005 tokens; B flat S0 0.6913 -> S1 0.7896, D 0.5964 -> 0.6996, A S1 0.9273. Stage 3 full reused prelim cache, final rows 1302 S0 + 1302 S1, JSON fail 0; D2L translate cache total 655,254 tokens / 338 calls on 2026-06-14 UTC; B flat S0 0.7519 -> S1 0.8172, B recurring 0.7520 -> 0.8195, D 0.5930 -> 0.7007, A S1 0.9343. Gate pass: no passthrough translated, scope=translation_runs, preserve excluded, eligible terms occ>=2, memory_pack leak 0. Tests: 112/112 pass. Claude should inspect A misses/worst terms before PASS. **PASS (Claude tái kiểm từ scorer + 5/5 guard tự chạy): scope=scope true, gold không bị bơm (context_builder chỉ đọc glossary_entries), injection occ≥2+role+canonical test xanh, token 655k<2.4M. Headline occ-weighted B 0.764→0.832 (KHÔNG bão hòa), D 0.593→0.701 (undetected 27→1), A 0.934 chẩn đoán (miss chủ yếu AI giữ tiếng Anh + lệch canonical example/set) KHÔNG chặn. Follow-up không chặn: snapshot D2L runs, rà canonical vài term P4+.** |
| HYG-01 | Vệ sinh prompt/context/cost TI: bump translator v2 (em-dash + version) + `LiteraryBuilderContextPack` (filtered continuity, Option C) + render prompt thật + preflight | DONE | pending | PASS (Claude, 2026-06-15) | Tái kiểm độc lập: D2L không đụng (36 guard pass), em-dash khôi phục + version bump, pack lọc THẬT (ch03 included 25/excluded 16/token 314≤600), 6 mục bắt buộc đủ, preflight max 1711<6000 & 296k<2.4M, Claude tự chạy 13+36 test pass. Artifact prompt đã tách: `literary_builder_prompt_sample.txt`, `literary_translator_s1_prompt_sample.txt`, index `literary_prompt_samples.txt`; full frozen registry riêng ở `literary_registry_snapshot.json` (22 glossary / 10 entities / 10 relations). **GATE còn đóng:** re-baseline S0/S1 dưới v2 là task RIÊNG, mở sau khi user duyệt prompt render đã trình. |
| HYG-02 | Literary Builder: bỏ cap recall→density audit + relation label + 4 process-guard (version v3 / render-chronology test / full-set preflight / cache-friendliness), offline | DONE | pending | PASS (Claude, 2026-06-15) | Tái kiểm độc lập: D2L không đụng (30 regression pass); cap bỏ + v3 (test assert); relation label `[lodger/inn-boy]` + notes-có-cờ; chronology guard RAISE nếu thiếu artifact (đóng lỗ HYG-01); cache prefix byte-identical + render deterministic; density audit đủ field (ch02 5.23/ch03 2.42 OK, jump-2x→REVIEW_REQUIRED); preflight ch02/ch03 status OK, prompt<6000. Claude tự chạy 8+3+30 pass. **GATE còn đóng:** re-baseline S0/S1 dưới v3 = task kế, kèm bảng cost-quality. |
| APP-A01 | App khóa luận = research cockpit: read-only **DatasetReadModel** adapter (SQLite pipeline → viewer shape) + trỏ viewer + **quarantine** AILAB gold-authoring + provenance tách cấu trúc | DONE | pending | PASS (Claude, 2026-06-15) | Tái kiểm độc lập: chỉ app/ đụng (0 pipeline/engine); adapter read-only (mode=ro); provenance by-source — trên DB THẬT d2l_p1: 0 runtime item sourced-from-gold, counts khớp bảng (1608/458), gold injectable=False → Directional-Lock GIỮ. 108 gold-target trùng agent = AGREEMENT (không phải leak). Quarantine 404 ở cockpit mode. Claude tự chạy 91 test pass. Deviation translations-keyed-by-config (S0/S1) chấp nhận. APP-B01 kế. |
| APP-A02 | Runtime registry overlay: highlight memory agent-xây ở CẢ gốc + dịch (S0/S1), tô theo drift, hover runtime card, link term_id | DONE / PASS | CodeX | Claude re-verified: hình==số (forms_used từ report, scored flag), 0 gold leak (+ hover-card runtime-gated bằng code), perf block 0.047s/chapter 1.08s trên DB thật, word-boundary, 3+130 tests | CodeX implemented observe-only overlay composer + scoped block/chapter API + frontend source/target marks/status colors/sidebar runtime icons. Real perf fixed after full eager endpoint was too slow: block scope 0.107s, chapter scope 2.151s, full audit 16.939s. Backend tests 130/130 pass. Browser smoke on d2l_p1: source+target overlays visible, no app console errors; hover-card needs manual/Claude verification because Browser runtime lacked hover support. |
| APP-B01 | Observability cockpit: **Prompt/Context Inspector** + API calls + replay/provider cache + token + cost (`ObservabilityReadModel` RIÊNG endpoint) | DONE | pending | PASS (Claude, 2026-06-15) | Tái kiểm độc lập: chỉ app/ đụng; read-only; separation test riêng pass; quota=prompt+completion (cached KHÔNG trừ, LOCK gg); inspector nối pack_id→memory_packs + included/excluded/dropped; multi-DB cache đúng; known-gap (replay-hit-rate) ghi trung thực. DB thật d2l_p1: 978 call, quota 4.24M, $3.77. Claude tự chạy 3+94 test pass. **Cảnh báo cho re-baseline:** totals = lịch sử cache tích lũy, cost-quality gate phải scope theo run_id/experiment. |
| APP-D01 | Score/report + **Consistency/Drift** hạng-nhất (`ScoreReadModel` đọc report scorer; 0 recompute) + provenance + export | DONE / PASS | Opus 4.6 (Antigravity) | Claude (re-verified: no-fabricate vs report thật, no-recompute, separation, 10+104 tests) | Theo LOCK (nn).6/.7. Report đã kiểm: `d2l_translation_metrics.json` có D `forms_used`={variant→count}/status; TI `*_pilot_consistency` có tar/fvr/ecs+oracle. UI render TỪ report, KHÔNG tự tính metric. Endpoint RIÊNG (scores ⊥ dataset ⊥ observability). Block-locate là display tùy chọn. |
| APP-C01 | Run control + live LOG-stream + run provenance (trigger script freeze + tail log; UI không tự tính) | DONE / PASS | CodeX (rework) | Claude re-verified from code: -m+cwd launch (real-script smoke), cost-gate one-time token bound to argv, real prompt-preview, exit→failed; deviation preflight-only accepted; 21+125 tests | Rework: launch thật bằng `python -m pipeline.scripts.<name>` + cwd `THESIS_RUNTIME_TOOL`; cost gate one-time confirm token bound to argv; prompt-preview thật for `run_translate`; failed exit ≠ done; Cockpit Run Control UI added. Tests: `21/21` targeted + `125/125` backend pass; browser smoke prompt-preview pass. No API calls. |
| RUN-EVENT-01 | Structured sidecar event log (live per-window observe) — engine append `run_events/<attempt_id>.jsonl` per window; UI tail file; **DB vẫn commit cuối** | DONE / PASS | CodeX | Claude re-verified: 2-DB-clone determinism, emit additive (output-path intact), #3 argv-digest handshake, path-from-registry, no read-model reads sidecar; 34+250 tests | Implemented sidecar `EventSink` off-by-default + RunControl event log wiring + UI event tail. DB commit policy unchanged. Event payload bounded (hash/token/summary; no full prompt dump), `committed:false` for previews, event flags included in prompt-preview argv digest. Tests: `pipeline\tests\test_translate_runner.py` 11/11, `app\backend\tests\test_thesis_runs.py` 23/23, full `pipeline\tests app\backend\tests` 250/250, `py_compile` pass, CLI help exposes `--event-log`/`--run-id`/`--attempt-id`. No API calls. |
| EV-D2L-02 | Vệ sinh D-scorer deterministic (longest-match + mask URL/code + honor case_sensitive + full per-term) + đổi tên `D_surface_v2`; chấm lại 0 API trên output cũ | DONE / PASS | pending | PASS (Claude, 2026-06-17) | Tái kiểm: D S1 0.701→0.739 (drift 213→184), AI source-blocks 80→11, calculus vẫn drift (→EV_D2L_03), terms_all→green reachable, 257 tests, mask không ăn prose (1 flip math). **Reviewer fix:** §5 sai khi đổ B-drop cho masking — thực do re-score THIẾU `--gold-variants` (gốc §4 spec thiếu); regenerate report kèm cờ → B hygiene-neutral S1 0.832→0.834. |
| EV-D2L-03 | Phân tầng `constraint_strength` (a-priori) + dọn glossary EVAL-overlay (FREEZE-safe) + headline D_surface_v2 = hard-tier; 0 API re-score | DONE / PASS | pending | PASS (Claude, 2026-06-17) | Tái kiểm: classifier a-priori (không đọc output), frozen DB hash trước==sau, A/B parity, determinism khớp. Headline S0 0.713→S1 0.841 trên 414 hard-term (hard=1001/1608, KHÔNG gaming — derivatives drift vẫn trong headline); calculus→consistent (fix), AI→soft, rules→ignore. 261 tests. **Caveat:** lists biên sau-khi-thấy-benchmark → nên validate held-out chương (chưa chặn vì headline_ready=false). Follow-up: paths tuyệt-đối trong report→relative. |
| EV-D2L-03b | Shared surface-matcher v2.2 + Vietnamese segmentation (TARGET-only) + audit-gộp S0+S1 + gate | DONE / PASS | pending | PASS (Claude, 2026-06-17) | Tái kiểm: determinism khớp, frozen hash trước==sau, metric v2_2, EN/VI tách+no-silent-fallback, overlay==scorer cấu trúc, 272 tests. **Bug FIXED:** learning hết "học máy"; set tập 12→6. **GATE: reviewer-audit 0/60 FP → segmentation ĐỦ headline; alignment hạ thành trục-2.** D S0 0.750→S1 0.886. Residual=biến thể hình thái (các/word-order)→follow-up `EV_D2L_03d_morph_norm` (0-API, KHÔNG alignment). |
| EV-D2L-05 | OCC_ALIGN_PILOT_01: alignment occurrence-level PILOT 1 chương — 2 nhánh ngang hàng (SimAlign ⟂ self-report+verify) audit-người-quyết, post-hoc frozen (KHÔNG re-translate) | DONE / PASS impl | (this commit) | PASS (Claude, 2026-06-19): frozen DB hash UNCHANGED, tự chạy 12 tests, production==test (SimAlign thật vào align_independent), ngữ cảnh GIỮ khi sample-scoped (rủi-ro #1 loại), gate a-priori + single→diagnostic, sampler stratified 5-tier/4-status, 0/60 at-risk first-match. Deviation sample-scoped CHẤP NHẬN (spec cost ~$0.05 SAI; thực 2041occ/$2.45-config). Pilot execution (self-report run + gold người + audit) = bước USER; solo ⇒ diagnostic_only tới khi có annotator-2 | CodeX: occ_align.py+3 scripts+tests, simalign==0.4, artifacts gold60/S0-32/S1-28, self-report preview-only, 284 full suite pass |
| EV-D2L-06 | MEMORY_TRADEOFF_01: định lượng memory tradeoff trên 57 override qua panel LLM-judge MÙ 4 họ (Gemini/Anthropic/Zhipu) | DONE / PASS impl + FINDING | (commit) | PASS (Claude): machinery (frozen, 16 tests, blind, swap, judge≠translator). **FINDING 2026-06-20 (panel CLOSED 4 cold họ, GLM pre-registered cuối):** per-judge harm **21–39%** (mean 32%) ≈ improve (mean 32.5%) — **harm≈improve, KHÔNG thắng ròng**; majority 3/4 = harm21/improve25/**lateral54%** (đa số contested); 4-họ-đồng-thuận 6 hại vs 9 giúp. GLM+2.5flash dễ(21%), Opus/3.1pro gắt(37-39%), 3.1pro-web-curated outlier(47%). **MỌI judge=LLM, NO human-expert (limitation); human.json=gemini-web-curated (PROVENANCE.md).** → biện minh S1_better. | CodeX machinery; USER chạy 4 model lạnh. Follow-up EV-07 + scorer panel-aware |
| EV-D2L-07a | RULER-FIX: bake-off 3 localizer (find_spans[0]/allocate_spans/SimAlign) trên GOLD người + sửa rep-occ selection; vá worksheet EV-06, 0-API | DONE/PASS | Claude | §6-final | Human gold complete (118 rows). L10 reconciliation pass (`checked=114`, `edge_checked=4`). SimAlign re-scored with source-sentence/full-target window: Metric A `22/116`, Metric B `1/2`; winner by L4 remains `longest_match` (`107/116`, no regression_fail). Canonical `--apply --localizer longest_match` regenerated 57/57 worksheet items. No API / no re-translate / D-scorer headline untouched. REOPENED post-§6: report localizer_bakeoff.json STALE (L10 re-run trên gold+override đã-commit FAIL 4; report ghi 0). Gold mt_mnist/mt_offset SAI occurrence (b126/b011 non-override thay vì canonical b137/scratch_b035). SimAlign window-correspondence bug bắt+sửa trong review (test fake-aligner che); winner longest_match. PENDING (ngoài commit): sửa mark bất đối xứng mt_mnist («MNIST»→«tập dữ liệu MNIST») + re-judge cold-panel HẸP {mt_mnist, mt_offset}; 55 item panel còn lại valid. Worksheet apply + cache HELD chưa commit  · SimAlign LOẠI (0.19 exact/0.88 region, nặng); winner longest_match; evidence localizer_compare.html |
| EV-D2L-07b | Cascade localizer T1(runtime-registry,unique-only,conflict-aware)→T2 LLM blind→T3 human; eval-only; 118=DEV | DONE / PASS | Claude | (this commit) | Implemented through bounded 8-case DEV gate only. T1 DEV 78/118 resolve, 40 residual (warning; no corpus run). Prompt v1 8 calls + v2 8 calls, total 8,051 token / $0.003271. Position re-anchor lifted cached v1 exact 1/8→4/8; general prompt v2 reached 5/8 exact (S0 3/6, S1 2/2). Remaining: 2 boundary over-extensions + 1 adjacent-term ownership error. T3/EV-07c required; no D_hybrid headline. 11 targeted + 326 full tests pass; frozen DB unchanged; no commit/push. **§6 PASS (Claude):** tự re-derive trên artifact đã commit — 5/8 exact + 0/8 hallucination, T1 78/118 (reasons multiple20/short16/conflict3/none1), conflict_count=122, DB hash DA0F687894090D43 unchanged, full suite 326 pass (tự chạy lại). Refactor localizer.py gỡ hardcode→edge_kind gate KHÔNG lật winner (longest_match bất biến, regression_fail=[]). 8/8 offset từ re-anchor (model-offset path chưa pilot); report dev là replay (cost thật ở §5). T3 + EV-07c held-out vẫn bắt buộc. |
| EV-D2L-07b-2 | Localizer T2 v3: blind generalized prompt (no hardcode) + drop model offsets (code owns location via quote+left_context anchor) + fix occurrence-trust bug; re-score all prior runs on corrected gold | DONE / PASS (diagnostic) | Claude | (this commit) | DIAGNOSTIC-on-DEV, NOT generalization (L1). Gold fix membership:S0 526→537 "sự thuộc về" (verified slice). Pre-checked shared-gold guard: legacy_longest_failures=8, EV-07a recommendation=longest_match 0.9310 UNCHANGED. Re-scored prior runs on corrected gold: baseline none/temp0=5/8, none/temp1=6/8 (temp1 non-reproducible), low/max4096=4/8. v3 = none/temp0 deterministic, prompt v3 + code-anchor; trust number deferred to EV-07c. **§6 PASS (Claude):** tự re-derive v3=5/8 trên gold đã sửa; L4 không còn đường dùng offset model (schema bỏ start/end), L2 prompt verbatim 0 hardcode, L5 longest 108/116 + legacy 8 UNCHANGED, DB hash DA0F687894090D43, 328 tests pass. **FINDING: left_anchor (ý Claude) net-harmful** — counterfactual chứng minh position-alone đúng cả 2 ca regression (annotation:S1, elementwise:S0); v3 prompt là gain thật (sửa membership+target). EV-07c (a-priori, đo trên held-out): adopt prompt v3 + offsets-in-code, **DROP left_anchor** → position primary, conflict→T3; KHÔNG re-measure trên 8. |
| EV-D2L-07b-3 | Locator fix: position-primary, bỏ left_anchor decider (code định vị đúng quote của LLM); pure code-fix trên model output cố định (replay, 0 API mới) | DONE / PASS | Claude | (this commit) | CodeX §5 complete. Locator now unique→position→ambiguous; validator bumped code_anchor_v3_position; prompt/schema unchanged. Replay-only run: 8/8 API-cache hits, 0 tokens, $0. DEV diagnostic 5/8→7/8; sole residual elementwise-multiplication over-extension. **§6 PASS (Claude):** re-derived 7/8 trên gold đã sửa (annotation:S1+elementwise:S0 hồi phục via position); offset_sources={position,unique_quote} KHÔNG còn left_anchor; replay 8/8 api-cache 0 token mới; shared-gold 108/116+legacy8 + frozen DB DA0F687894090D43 unchanged; 328 tests tự chạy lại pass. |
| EV-D2L-08 | Occurrence-level adherence scorer: thay occurrence_weighted (block any-match × source-count) bằng count + JOINT cross-term allocation, min(src,tgt); audit render từ CÙNG allocation | DONE / PASS | Claude | (this commit) | §5 complete, 0 API/re-translate. Legacy guard giữ nguyên B S0/S1 `0.740504/0.802426` trên 3133 occurrence. Corrected joint denominator 2967 (loại 166 source overlap): B S0 `0.717223`, S1 `0.771149`, gap `+0.053926`, không collision. A S1 lower/upper `0.813351..0.918395` trên 8149 occurrence, residual capacity 856. APP-D01 headline + per-chapter đọc lower/upper mới, legacy vẫn xem được. Audit 32,280 record; DB hash `DA0F687894090D43` bất biến; 49 targeted + 332 full tests pass. **§6 PASS (Claude):** tự re-derive headline TỪ audit CSV (cùng allocation với highlight, 32280 record) — B-gold S0 2128/2967=0.717223, S1 2288/2967=0.771149, A-reg 6628/8149+884 collision band [0.8134,0.9184], khớp tuyệt đối; legacy replicate 3133→0.7405/0.8024; denom 2967/8149 re-derived KHÔNG ép; min-cap đúng (2372>2128); consumer headline=adherence_lower + metric_version; DB hash + 332 tests tự chạy lại. Audit CSV/HTML 22MB regenerable KHÔNG commit. |
| EV-D2L-07a-reopen | Reopen EV-D2L-07a: canonicalize stale gold occurrence rows and block stale reports | WAITING_USER_REVERIFY | CodeX | local | Moved exactly 4 rows (`mt_mnist_dataset_923cfe4011` S0/S1, `mt_offset_53a610e925` S0/S1) to override `rep_block_id` (`d2l_introduction_index_b137`, `d2l_linear_networks_linear_regression_scratch_b035`), cleared target spans for human re-verify, preserved the other 116 gold rows. Added build-gold rep-block/sentence assertion and report fingerprint/L10 stale guard. Removed stale `data/reports/localizer_bakeoff.json` and stale SimAlign cache. Current L10 on disk: `checked=114`, `edge_checked=4`, `failures=[]`; final score intentionally blocked until user fills 4 target spans. |
| EV-D2L-07a-reopen-score | Re-score after user reverified 4 canonical gold rows | REWORK_BLOCKED | CodeX | local | User spans filled (`bộ dữ liệu MNIST`, `tập dữ liệu MNIST`, `độ lệch`, `độ dời`). L10 on current gold+override: `checked=114`, `edge_checked=4`, `failures=[]`; report hash guard passes. `--score --with-simalign` produced a valid non-stale report but `recommendation=null`: `first_match` and `longest_match` both 108/116 but fail regression `mt_mnist_dataset_923cfe4011:S1` because they localize `MNIST` instead of full phrase `tập dữ liệu MNIST`; `simalign` 22/116. No `--apply` run because L4 has no eligible winner. |
| EV-D2L-07a-final | Finalize corrected occurrence gold, documented localizer limit, human-gold worksheet apply, and stale-safe bakeoff | REVIEW | CodeX | local | L10 `114+4`, failures `[]`; report hash matches current gold+override. Automatic bakeoff: first/longest `108/116=0.9310`; `mt_mnist:S1` recorded as noun-phrase-expansion limitation, not hidden or used to disqualify. L4 recommends `longest_match`; SimAlign `22/116`, Metric B `1/2`. Frozen worksheet uses user-verified full phrases for MNIST/offset; apply `57/57`, unresolved `0`. Tests `43/43` targeted + `315/315` full. No API/re-translation/DB write/commit/push. Narrow human re-judge `{mt_mnist, mt_offset}` remains. |
| EV-D2L-09 | Ambiguous-assignment probe: true-collision + variant-stealing; position_narrow vs LM Studio embeddings (LaBSE/BGE-M3/E5) with real model identity/cache instrumentation | DONE (DEV) | CodeX+Claude | local | Machinery PASS (§6). Gold human-labeled 102/102 + verified, probe run (62.5s, DB DA0F687894090D43 unchanged). DECISION (§7, DEV not trust): embedding models REJECTED for scorer — free position_narrow 0.765 vs models 0.50-0.545 hard-subset; adopt position_narrow+abstain→band (M2: 21 false-credits→abstain, prec 1.0). Tier-1 sentence-hit: top-1 bge-m3/e5 0.983, top-3 all 1.0; misses gold@rank-2 → top-2 escalation; bge-m3 = tier-1 model (only usable margin gate). Candidate-coverage gap: ~10 non-candidate gold spans (allocator limit). Held-out + literary/reorder validation pending. Follow-ups: position_narrow→headline integration (new task); tier-2 LLM localize. |
| EV-D2L-10 | Cascade occurrence localize+adherence 4 tầng (T1 region bge-m3 → T2 luật code → T3 LLM-GPT residual → T4 human gold); CHẠY-để-đo %T2-resolved/%residual/%human/đội-lốt-thật, không ước lượng | PASS Phase A/B (Claude §6) · REWORK build_residual_gold trước Phase D | (this commit) | PASS (Claude 2026-06-26) | Spec `TASK_EV_D2L_10_localize_adherence_cascade.md`. Phân phase: A driver(T3 stub)→B chạy T1+T2 0-LLM ra residual thật→C T3-GPT trên residual (prompt user-duyệt trước, halt-and-audit token)→D human gán residual=gold validate T3→E report. Tái dùng region_align/occurrence_adherence/localizer_cascade. Scope=registry ruler S0+S1. Đội-lốt đo trên gold CÓ not_rendered (102 cũ all-rendered không đo được; chỉ 23/428 at-risk trùng). KHÔNG đổi headline tới khi §6 adopt; frozen DB ro; T3 chỉ GPT vòng này; DEV không phải trust number. §5 done: Phase B full S0/S1 0 LLM, DB hash DA0F687894090D43, outputs `cascade_localize_*`, residual worksheet generated. **§6 PASS Phase A/B (Claude re-derived từ decisions[] trên đĩa):** S0 8149→t2 4786/resid 3363/masq 115, S1 8149→5417/2732/103, partition khớp; T2 dùng allocation EV-08 thật; frozen hash tự tính khớp; 11 tests; 0 GPT (tier_max>2 raise). Finding: S1 đẩy về C1_primary (5154 vs 4122), residual S0 41%/S1 34%. **REWORK trước Phase D:** build_residual_gold bỏ mọi t2_credit → KHÔNG đo masquerade cùng-câu credited (C1_variant_flagged 664/263); thêm control credited + score credited-masquerade; worksheet 6095 là đầu-vào-T3 không phải human (đừng gán 5992). Own: spec control-sample của Claude under-target. Big JSON 19/19/41MB KHÔNG commit (regenerable). **REWORK-1 Part A PASS (Claude §6.8, re-derived):** pilot 20 GPT thật = 17/20 (3 sai đều lệch-biên KHÔNG sai nghĩa: `*mục tiêu*`/span rộng) → đúng-nghĩa ~20/20; ca đội-lốt user→né khách hàng ✓; annotation C0→GPT tìm ra chú thích ✓; cost $0.0053/20 (447 tok/call, không bloat); 0 key leak, frozen hash khớp, 7 tests; loader fix offset-vs-quote. Giới hạn: pilot CHỈ kiểm localization (103 toàn rendered, chưa kiểm omitted/not_found); confidence high cả 20 (chưa calibrate); chấm exact-span ngặt→dưới-báo. Next = Part B (not_rendered gold + verify UI + chuẩn-hóa chấm). **REWORK-2 spec READY (Claude, design chốt với user):** T3 đổi sang **LOCATE-ONLY** (LLM chỉ định vị, CODE chấm containment vs accepted_forms → adherent/off_glossary/not_rendered; measurement-ngoài-model; bỏ not_applicable; reasoning none = 1 việc). **Prompt `d2l_locate_only_v4` do Claude thiết kế + validate ($0.0062), CodeX DÙNG VERBATIM.** Claude tự test: over-wide PASS nhờ containment (17/20 adherent), competition→off_glossary, predictions→not_rendered, bỏ candidate-hint sửa lỗi cross-term v3. Polysemy (bit→một chút) ra off_glossary = bounded, fix gốc Builder scope-down; "đối xứng S0/S1" phải ĐO. Part B gold thêm strata C1_variant_flagged + verify UI tương tác. CodeX điền §5.7, STOP. **REWORK-2 PASS (Claude §6.9 re-derived):** prompt `d2l_locate_only_v4` VERBATIM đúng (yêu cầu cứng user); `_score_locate_only_by_code` deterministic dùng find_spans EV-08 → measurement NGOÀI model ✓; pilot locate 20/20, code 17 adherent/3 off_glossary (target/annotation=registry thiếu form, set genuine); over-wide hết nhờ containment; cost $0.00515; worksheet 7022 có control:C1_variant_flagged=927 (vá lỗ REWORK-1) + verify UI 4-nhãn; frozen hash + metrics + 9/352 tests + key-no-log OK. Chưa tin: pilot localization-only (hard-set off_glossary/not_rendered chưa đo = human label kế); occurrence_index ca lặp + confidence chưa exercise. Commit code + locate_pilot.json; worksheet 8.6MB không commit. **REWORK-3 spec READY (Claude phát hiện bug khi chạy b003):** scorer containment T3 `_best_accepted_form_in_quote` dùng find_spans segment-aware → trượt khi cụm LLM có chữ đệm bao quanh (`find_spans('từ nguyên lý cơ bản','nguyên lý cơ bản')->[]`) → off_glossary OAN → thổi phồng %off_glossary headline. Fix = boundary-aware normalized substring (chỉ trong scorer T3, không đụng find_spans T2). Pilot 17/3 cũ không dính. CodeX điền §5.8, STOP. (b003 cũng cho bằng chứng đẹp: user→người dùng né khách hàng; rules S0 quy tắc→S1 luật = memory→canonical). **REWORK-3 PASS (Claude §6.10):** fix đúng scope (`_find_form_in_located_quote` boundary-substring chỉ T3; find_spans T2 giữ); Claude tự test hàm (bug "từ nguyên lý cơ bản"⊃"nguyên lý cơ bản" khớp offset đúng; "mụcđích"⊅"mục" None; "cạnh tranh"⊅"cuộc thi" None) + chạy lại b003 (cached): S1 "từ nguyên lý cơ bản" LẬT off_glossary→adherent ✓, S0 "những nguyên lý đầu tiên" giữ off_glossary đúng; 10 targeted+frozen hash+metrics+353 full OK. Next = Part D (lấy mẫu strata + human label → %off_glossary S0/S1 thật) |
| BUILDER-V2 | Builder D2L v2: trích độc lập (recall) + sổ-tay-có-lọc (memory-pack lọc theo window) + code consolidation QUYỀN CUỐI; sửa gốc phình (1608 term, số-ít/số-nhiều tách đôi do casefold-merge) | Stage C1 PASS (Claude §16); C2/D chờ | (this commit) | PASS Stage A+B+C1 (Claude 2026-06-29, re-derived) | Spec `TASK_BUILDER_V2.md`. Lộ trình A(merge-probe 0-API/0-DB)→B(render prompt v8+pack 0-API)→C(pilot 1ch artifact JSON, không ghi DB)→D(migration source_variants_json+consumers fallback). **§5 CodeX Stage A:** concept_key bảo thủ (DONT/ss/irregular/regular; cấm train/training, general/generalization) + offline probe đọc glossary_entries ro, xuất JSON+CSV. **§6 PASS (Claude re-derive ĐỘC LẬP, grouping riêng, khớp chính xác):** 1608→1486 (−122 ≈7.6%); occ 13252→13252 bảo toàn; rematch mismatch 0; **29 conflict** (24 target_divergence + 5 plural-only); helper adversarial ALL OK (train≠training); 6 tests; frozen hash DA0F687894090D43; data/jobs sạch (0 API/0 DB). **Stage B PASS:** render-only prompt v8 + pack proxy_chronological cho preliminaries, 50 windows, max prompt 4266<6000, max pack 1500, total prompt 185201, upper bound 245201; future-leak guard 0 vi phạm; alphabet-tail drop exposed. **Stage C1 CodeX §15:** online consolidation engine + pack-slim + read-only sim, 12 tests pass, frozen hash unchanged, 1608→1484 notebook (+2 rejected), 122 concept merges, 8 conflicts, occurrence/evidence conserved; pack-slim preliminaries total prompt 185201→141317, dropped_by_budget 133→16. **§16 PASS (Claude re-derived ĐỘC LẬP):** 12 tests không-nông (polysemy loss→conflict_pending+target rỗng, stoplist heading-exception R3, canonical bất biến, conservation, determinism, integration sim) · sim tái lập 1608→1484/122 merge/2 reject/8 conflict (5 plural+3 polysemy) · re-derive từ DB khớp (concept_key −122 = Stage A) · **soi "occurrence conserved" nghi-fudge → TRUNG THỰC**: 71 entry v7 count=0-có-evidence → bump 0→max(1,evidence) +119 (13252→13371), check đúng dạng effective==notebook+rejected (13371=13127+244), report giữ cả raw+effective+note · pack-slim R1 đạt (pack KHÔNG evidence_ids, JSON nén) + cắt-theo-ưu-tiên thật (`_candidate_priority_sort`: conflict→occ desc→multiword→exact, alphabet tiebreak cuối) sửa lỗi §13 · determinism byte · frozen hash bất biến, 0 API/0 DB. **Trung thực phạm vi:** giảm bloat C1 gần như TOÀN BỘ number-merge (122), lọc-từ-phổ-thông chỉ 2 (set/problem) vì replay entry giàu occurrence + guard concept_key-đã-có → giá trị stoplist CHƯA chứng minh tới C2 (caveat R2); stoplist code = 12 từ restraint của prompt (không gồm feature/function/model/layer); `_looks_polysemous` heuristic hẹp ổn cho cơ-chế-gắn-cờ. C2: notebook chronological v2 (không bê v1), cost-gate, đo recall-vs-gold DEV. |
