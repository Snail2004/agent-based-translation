# Python
__pycache__/
*.pyc
.venv/
.env
.pytest_cache/

# Secrets — KHÔNG BAO GIỜ commit
API-KEY.txt
OPENAI-KEY-*.txt
GEMINI-KEY.txt
GEMINI-BASE-URL.txt
*.key

# AI-LAB track — KHÔNG thuộc thesis repo (two-tracks separation)
AILAB_HANDOFF/
ailab/
AILAB_SOURCES_RAW/

# Runtime/generated data
THESIS_RUNTIME_TOOL/projects/
**/data/jobs/
THESIS_RUNTIME_TOOL/data/cache/
THESIS_RUNTIME_TOOL/data/eval/human_ratings.csv
*.sqlite3
*.sqlite3-journal
.chroma/

# D2L raw clone — track provenance only, not the 214MB repo
THESIS_RUNTIME_TOOL/data/sources/d2l-vi/

# D2L token-bloat baseline (audit-only, has cache copy) + intermediate prepass artifacts
THESIS_RUNTIME_TOOL/data/_baseline/
THESIS_RUNTIME_TOOL/data/prepass/

# Localizer bake-off intermediates — regenerable via `localizer_bakeoff --score`; SimAlign rejected (EV-D2L-07a)
THESIS_RUNTIME_TOOL/data/eval/localizer_simalign_cache/
# Blind-leak duplicate of KEY/override_set.csv written by build (build:50 follow-up) — keep only the KEY/ copy under version control
THESIS_RUNTIME_TOOL/data/eval/memory_tradeoff/override_set.csv
