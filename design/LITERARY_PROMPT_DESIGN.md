# LITERARY_PROMPT_DESIGN — prompt + schema Builder văn học 4 bước (Story Bible)

Owner: **Claude viết prompt + schema** (lõi luận văn). CodeX wiring/parser/validator, chỉ chỉnh prompt khi run lỗi nghi-prompt + báo lại. Xem TASK_LIT_L2A §0, memory `codex-division-of-labor-and-relay`, `prompt-memory-design-is-first-class`.
Corpus pilot: Wuthering Heights (Gutenberg #768), chương 1–4. Trạng thái: **L2A-1 M1 chạy thật (ch1); prompt vá sau M1 gate — block-id verbatim (wh_ prefix) + precision mention/event**. Block-id trong ví dụ = scheme thật `wh_chNN_bNNN`.

## 0. Bối cảnh: cái đã có vs cái ta làm

`pipeline/prepass/prompt.py` đã có `build_messages(mode="literary")` = **`literary_builder_context_v3`**: MỘT call/chương, trả cả glossary + entities + relations (kèm address_a_to_b_vi/state_label) + summary + motifs. CodeX preflight ra **`address_policies=0`** → cục monolith đuối phần quan hệ/xưng hô (ép model kết luận quan hệ trong một lượt = đúng bẫy D2L first-write-wins).

Redesign 4 bước = tách theo "đơn vị sự thật": lexicon/mention cục bộ → evidence tự sự cục bộ → digest trọn chương → consolidation toàn cục. **Cục `literary_builder_context_v3` giữ nguyên làm baseline A/B** (§5 task) — KHÔNG xoá.

## 1. Quy ước chung (mọi bước)

- **House style** (khớp `build_messages`): `system` = "You are the <ROLE> agent for an autonomous English-Vietnamese literary translation pipeline. Read only … Hard rules: … Required JSON shape: {…}". `user` = các khối có nhãn IN HOA. Block marker render qua `render_chapter_blocks` → mỗi dòng `[wh_ch01_b005] <text>`.
- **JSON-only**, không văn xuôi ngoài JSON. VI target **đủ dấu** (cấm ASCII kiểu "doi gio hu").
- **block_ids luôn là marker THẬT nhìn thấy trong khối text được đưa** — validator loại entry có block_id không xuất hiện trong window (chặn hallucinate).
- **Evidence-ledger**: bước window (1,2) CẤM kết luận danh tính cuối / quan hệ / pha. Chỉ ghi cái quan sát được + hint. Danh tính & pha do Bước 4 quyết.
- **KHÔNG full-dump registry**: pack context chỉ gồm entry có surface xuất hiện trong window, có trần token (`builder-v2-memory-pack-design`).
- **Validator granularity (DROP entry, KHÔNG fail window):** lỗi cấp-ENTRY (block-id sai, non-person actor/target, enum sai, đại từ trần) → **bỏ RIÊNG entry đó + đếm** (`#nonperson_event_dropped`, `#dropped_bad_block`…), **GIỮ các entry tốt còn lại**; window chỉ FAIL khi JSON parse-fail hoặc thiếu top-level. Lý do: 1 event chó không được nuốt luôn event người-người tốt trong cùng window (bài học D2L "drop bad entry, keep good"). **Drop-rate cao = cảnh báo prompt** (đừng nuốt im lặng — vẫn phải surface để bắt lỗi hệ thống như bug block-id).

## 1.5 Danh tính xuyên request & quy tắc window *(chốt với user 2026-07-08)*

**Nguyên tắc nền: Builder KHÔNG phân giải đại từ per-occurrence.** Story Bible lưu tri thức cấp-cuốn-sách (entity+alias, speaker map, relation phase, address/narrator-reference policy). "he/I ở block X là ai" = việc Translator xử lúc dịch window đó bằng pack (roster + policy) — như người dịch thật, không có bảng tra đại từ toàn sách. Vì vậy B1 cấm đại từ trần làm mention surface: đại từ trôi qua là ĐÚNG thiết kế.

**Thang nối danh tính (rẻ → đắt, kiểu cascade D2L localize):**
1. Window ghi bằng chứng, `unknown` được phép (đã cài prompt B1/B2).
2. Nhồi `CHAPTER_ROSTER_ON_STAGE` bounded (chương hiện tại, không cả sách).
3. B4 code rẻ: nối said-tag, thoại xen kẽ A-B-A-B, khớp alias.
4. **Escalation queue:** ca unknown còn lại → re-query LLM với lát cắt rộng hơn (scene/chương) CHỈ cho ca đó. Đo: `#unknown_resolved_by_code` / `#resolved_by_llm_escalation` / `#unresolved`. KHÔNG nguyên-chương cho mọi call — input dài làm rớt recall (bài học D2L), chương-nguyên-cục chỉ cho B3 + escalation.

**Quy tắc windowing (CodeX imple ở scaffold):**
- Cắt window theo ranh giới scene/đoạn thoại khi có thể (không chém ngang cuộc đối thoại).
- Kèm `PREVIOUS_WINDOW_TAIL` 1–2 block dạng CONTEXT_ONLY (chỉ đọc cho liền mạch; **cấm trích entry từ đó** — validator sẵn chặn block_id ngoài window chính).

**Narrator frame (đặc thù WH, bắt buộc):** "I" của Lockwood ≠ "I" của Nelly. Story Bible track `narration_frame_segments` = list `{block_range, narrator}` — **theo BLOCK-RANGE, không phải 1 narrator/chương** (ch4 chuyển Lockwood→Nelly GIỮA chương; narrator-theo-chương sẽ gán sai "I" ở đoạn chuyển). Digest B3 xuất segments này; narrator-reference policy (Nelly gọi Heathcliff = "hắn/ông"?) neo theo frame. Time-skip WH không phát sinh việc mới: pha quan hệ neo theo block-range THỨ TỰ VĂN BẢN (thứ tự Translator dịch), không dựng niên biểu thật.

**Mark & chấm (điều chỉnh scope so D2L):** markable bằng code = tên riêng + địa danh (glossary, surface-match kiểu TC giữ nguyên) + address_term trong thoại (ACS trên speaker_turns). KHÔNG mark bằng code = đại từ trần (policy-driven, judge/spot-check), sự kiện/motif (memory-only, không chấm marking). Địa danh CÓ yêu cầu nhất quán 1:1 qua glossary.

## 1.6 Tinh chỉnh sau review CodeX *(2026-07-08)*

**Resolution là FIELD hạng nhất (không nhị phân).** Mọi ô tham chiếu người (mention/speaker/addressee/actor/target) mang: `resolution_status` ∈ {named, candidate, unknown} (named = surface tự nó là tên/biệt danh rõ, B4 mint/gắn entity; candidate = mô tả/đại từ ánh xạ tới entity trong pack/roster nhưng chưa chắc; unknown = không attribute được) + `candidate_entity_ids` (số nhiều, chỉ điền khi candidate, lấy từ pack/roster — cho phép "the master" ứng viên nhiều người) + `mention_id`/`turn_id`/`event_id` ổn định (`m_<block>_<n>`) để B4+escalation trỏ ca cụ thể. Window pass CẤM mint canonical entity_id; `canonical_entity_id` để null (B4 quyết).

**Đại từ-làm-attribution KHÔNG đẻ rổ riêng** (không `coreference_evidence` bucket — thêm rổ = rớt recall). Đại từ nắm qua `attribution_method` trên turn; pronoun-as-actor cho phép làm surface với resolution_status candidate/unknown, KHÔNG xoá về unknown khi có tín hiệu turn-order.

**⭐ Alias có VÒNG ĐỜI (CodeX, đặc thù WH):** tên tái chế — Catherine mẹ Earnshaw→Linton; con Linton→Heathcliff→Earnshaw; "Catherine Linton" là alias CỦA CẢ HAI ở hai khoảng khác nhau. Alias trong T2 BẮT BUỘC có `valid_from_block`/`valid_to_block` gắn entity — không alias global vĩnh viễn. B4 gán; canary hai-Catherine kiểm cả valid_range.

**`confidence` = COARSE {high, medium, low}, KHÔNG numeric** (self-confidence LLM vô nghĩa — đã đo ở D2L, xem `reasoning-effort`/gemma). Tín hiệu tin cậy THẬT = `attribution_method` (explicit_tag > turn_alternation > nearby_context > narrator_inference) + "candidate có trong roster?". B4 gate theo METHOD, không theo confidence.

**Hai ngữ cảnh injection KHÁC NHAU (sửa CodeX):** Builder-time (B1/B2) CHỈ có `narrator hints theo block-range` (B2, refine thành `narration_frame_segments` ở B3) + `scene roster` + `previous summary`; relations/address-policy CHƯA tồn tại (là output B4). Translator-time (runtime, sau FREEZE) mới có pack đầy đủ relation facts + pair address policy theo pha. Đừng nhồi thứ chưa build vào Builder.

**`story_time` chỉ nhãn THÔ cấp chương** (`narration_frame` ở B3: "khung Lockwood 1801" / "Nelly hồi tưởng") — KHÔNG tái dựng niên biểu; pha neo theo THỨ TỰ VĂN BẢN.

**Unknown-rate là metric HEADLINE** (có ngưỡng cảnh báo), chia theo vai + `attribution_method`: unknown cao bất thường = prompt/windowing chưa ổn → KHÔNG scale.

## 1.7 Canary set, định nghĩa unknown, escalation *(CodeX round-2)*

**Bộ canary (B4 phải pass; spot-check kiểm):**
- Ba Catherine KHÔNG merge; mỗi alias có `valid_range` (Earnshaw / Linton / Heathcliff).
- "Catherine Linton" gán đúng **mẹ** (sau cưới Edgar) vs **con** (khai sinh) theo block-range.
- Heathcliff ≠ Mr. Earnshaw dù cùng vai "master" ở hai thời điểm; "the master" resolve theo block-range, KHÔNG global.
- narrator "I" khung Lockwood ≠ "I" khung Nelly (không cùng entity narrator).
- Hindley Earnshaw ≠ Hareton Earnshaw; Linton Heathcliff ≠ Heathcliff.

**unknown = có kỷ luật, KHÔNG phải thất bại:** báo tách `unknown_with_evidence` (đủ evidence để escalation xử) vs `unknown_empty` (mất bằng chứng — mới là xấu). unknown QUÁ THẤP cũng đáng nghi = đoán bừa → ngưỡng cảnh báo **2 phía**.

**Escalation queue (khoá tiêu chí cho B4):** CHỈ escalate unknown/candidate ảnh hưởng relation/address/speaker; bỏ qua unknown trivial; input = **scene slice** (không full-chapter mặc định); output CHỈ sửa resolution, **cấm thêm fact ngoài evidence range**.

## 1.8 Temporal state / vòng đời — khác biệt LÕI so D2L *(chốt với user 2026-07-08)*

**Nguyên tắc:** D2L memory = MAP tĩnh (term→dịch). Literary memory = tập **FACT CÓ KHOẢNG HIỆU LỰC**: `(subject, attribute, value, valid_from_block, valid_to_block, trigger_block, evidence)`. Truy vấn LUÔN là **"as-of block X"** (trạng thái TẠI vị trí đang dịch, KHÔNG phải state cuối/global). Đây là khác biệt cấu trúc lớn nhất & lõi kỹ thuật luận văn.

**Trường CÓ vòng đời (không chỉ relation):** relationship state cặp (bạn→thù→hòa→tri kỉ; KHÔNG đơn điệu, nhãn CÓ THỂ lặp) · address/xưng hô cặp (dẫn xuất) · alias/tên (married name, tước hiệu) · entity status (giai cấp, tuổi→register, **sống/chết**, nơi ở) · narrator-reference tone + narration_frame. **Tĩnh (global):** identity core, glossary địa danh/vật, motif-as-concept.

**BUILD (evidence → timeline, KHÔNG lưu 1 state):**
- B2 ghi `relation_events` (act cục bộ + block_id), CẤM nhãn pha.
- B4 lớp relation-phase = **segment chuỗi event theo THỨ TỰ thành interval**: ranh giới = trigger lật valence (evidence rõ); giữa 2 ranh giới, valence trội định state. Bài toán **change-point trên stream có thứ tự**, KHÔNG phải phân loại cả cặp.
- Builder KHÔNG giữ "current relationship" chạy dọc extraction (→ first-write-wins). Pha nảy ở B4 từ TOÀN BỘ stream; nhãn lặp (`friend[b10-40]` … `friend[b200-300]`) = 2 phase hợp lệ.

**Granularity = block = đơn vị dịch:** lật giữa đoạn biểu diễn được (`phase1[b100-147]`, `phase2[b148-…]`, `trigger=b148`; register đổi từ trigger trở đi). memory-resolution = translation-unit = block, không cần mịn hơn.

**DÙNG (as-of query — bất biến BẮT BUỘC):**
- Translator dịch `[X..Y]` → pack đưa, mỗi cặp có mặt, **phase AS-OF X** (address+register), KHÔNG state global. (Failure mode: lưu 1 state global → dịch đoạn đầu bằng giọng của quan hệ cuối truyện.)
- Trigger TRONG window (Z∈[X..Y]) → pack cờ `transition@Z: address P→Q` để Translator đổi giọng GIỮA window.
- Critic/ACS chấm nhất quán **TRONG-pha**; đổi giọng đúng ranh giới = ĐÚNG, KHÔNG đổi tại transition thật = LỖI → ACS phải phase-segmented.

**Coupling:** một trigger (Heathcliff giàu về) lật đồng thời relationship+address+alias+status. Mỗi trường = interval-list ĐỘC LẬP; B4 ghi chung `trigger_block` khi co-triggered (không gộp một structure = tránh monolith).

**ĐO (không tune mù):** under-segment (bỏ lật thật → giọng sai) vs over-segment (giọng chập chờn) — cần trigger-evidence gate + ngưỡng tối thiểu; cho phép lật NHANH nếu evidence mạnh (án mạng/hôn nhân). Số phase/cặp khớp người đọc = thứ phải đo. **Pilot ch1–4** kiểm CƠ CHẾ (thu event→segment pha đầu→as-of), chu kỳ đầy đủ để scale sau; `unresolved_threads` (B3) = con trỏ transition đang chờ.

## 1.9 Temporal model — siết CodeX round-3 *(2026-07-08)*

**Tier field temporal (pilot KHÔNG build hết — chống scope explosion):**
- **CORE (build L2A, register-critical):** `relation_phase`, `address_policy`, `alias/title`, `social_status`.
- **DEFERRED (schema để cửa, build sau khi core chạy):** `speech_style` (dialect Joseph, giọng trẻ con — quan trọng WH nhưng pha sau), `life_status` (chết/mất tích/bóng ma), `narrator_stance`, `knowledge_state` (ảnh hưởng register ÍT → ưu tiên thấp), `residence`. `allegiance` = **gộp vào `relation_phase`**, không field riêng.
- B4 phải BIẾT trường nào temporal (để không lưu global sai) nhưng chỉ build CORE ở pilot.

**Open interval (CodeX):** mới thấy trigger → `valid_from_block=b148, valid_to_block=null, status=open`; trigger kế mới đóng interval cũ. CẤM ép model đoán `valid_to_block` sớm → hallucinate. Freeze cuối sách đóng các interval open còn lại tới hết.

**Event ⟂ State (ranh giới cứng):** `relation_events` (B2, bằng chứng rời rạc) KHÔNG BAO GIỜ tới Translator. Chỉ `state_intervals` (B4, đủ evidence) vào pack. "Hindley chế Heathcliff @b14" = event; "Hindley hostile→Heathcliff [b14..open]" = interval.

**Hai trục thời gian (tách sạch):** `text_order_range` = TRỤC CHÍNH cho Translator (relation_phase index theo trục này) · `narration_frame`+`story_time_label` (thô) = ai kể + khi nào. Vì Nelly kể (đa phần) THEO THỨ TỰ → text-order ≈ story-order → as-of-block trả đúng pha của **cảnh đang kể** (bạn-bè-thời-trẻ trong hồi tưởng), KHÔNG phải trạng thái cuối. `story_time_label` chỉ CỜ khi kể phi tuyến (flashback lồng/foreshadow) — KHÔNG dựng chronology. Phân vai: relation_phase = chuyện trong cảnh (text-order); narration_frame = ai kể/thì.

**Bounded phase taxonomy (chống over-seg):** `phase_label` ∈ tập ĐÓNG nhỏ, phổ quát mọi tiểu thuyết (đề xuất: allied, friendly, neutral, strained, hostile, estranged, dependent, reconciled). LLM CHỌN từ tập + kèm trigger-evidence; code KHÔNG suy pha. (Không vi phạm `code-never-does-language-work`: enum trạng thái phổ quát ≠ wordlist trích thuật ngữ.) Mở phase mới CHỈ khi trigger có evidence ngôn ngữ/hành động MẠNH; tone nhất thời → ghi `event`, KHÔNG mở phase.

**Sub-block door (optional, CHƯA build):** mặc định interval theo block; trigger giữa 1 block dài/thoại quan trọng → cho phép `quote_trigger`/`span_hint` + cờ `transition_inside_block`. Schema chừa chỗ, pilot không làm.

**Query `as_of` = HỢP ĐỒNG RUNTIME (Translator, test ở L2b — KHÔNG phải Builder pilot L2A):** `story_bible.as_of(block_id, window_range)` → {active_narrator, scene_roster, valid_aliases@block, relation_phases cặp-có-mặt, address_policies cặp-có-mặt, transitions_in_window, uncertainty_flags}. CẤM trả full timeline / quan hệ tương lai / trạng thái cuối = **guard chống leakage NỘI BỘ trong Story Bible** (song song directional-lock gold-isolation).

---

## 2. BƯỚC 0 — Chapter pre-read  *(mode: `literary_chapter_brief_v1`)*

### 2.1 Nhiệm vụ
Đọc nhanh TRỌN một chương trước các pass chi tiết để tạo frame trung lập: ai đang có mặt, ở đâu, scene có mấy người có thể được gọi, và chuyện gì xảy ra ở mức factual. Đây là tầng **GIST**, không phải fact store và không được kết luận quan hệ/pha.

### 2.2 System prompt (nguyên văn)

> - Prompt version: literary_chapter_brief_v1.
> - You are doing a FAST PRE-READ of ONE chapter BEFORE any detailed extraction. Return only valid JSON matching the Required JSON shape. No text outside JSON.
> - Purpose: give the later passes a factual frame of WHO is on stage, WHERE, and roughly WHAT happens, so they can resolve pronouns and vocatives. You are NOT extracting evidence and NOT judging relationships.
> - HARD LIMIT (leak guard): do NOT output relationship states, alliances, feelings, trust, phases, or emotional outcomes; do NOT say two characters "become" anything. Report only observable roles and neutral actions. Relationship conclusions are decided far later from the whole timeline — asserting them here would corrupt that.
> - cast_on_stage: only persons who are physically present and act or speak in THIS chapter. Exclude persons merely named in passing, and exclude historical / portrait / inscribed / remembered names of people not present. For each: surface (verbatim, as first named), surface_kind (proper_name or descriptor), role_hint (a plain OBSERVED social role only — e.g. host, visitor, servant, child, innkeeper, traveller, soldier; NEVER a relationship word like friend, enemy, rival, lover), first_seen_block. Use surface_kind=proper_name only when the surface itself is a real name/title+name ("Mira", "Mr. Alden", "Miss Rook"); use descriptor for role/description surfaces ("his aunt", "the innkeeper", "the coachman").
> - First-person narrator rule: if the narrator is first shown as "I" but is later addressed or named in this same chapter, use that later proper name as the cast surface and mark surface_kind=proper_name; do not keep a separate "I" cast row for the same person. If the narrator is never named in this chapter, use surface "I" with surface_kind=descriptor.
> - setting: {place (verbatim if named, else a short description), time_frame_hint (one of: frame_present, past_recollection, unclear), scene_shape (one of: single_scene_one_location, few_scenes, many_scenes_or_travel)}.
> - scenes_party_size: split the chapter into contiguous scenes; for each {block_range, co_present_count (how many named persons are together and could be addressed in that scene), participants (their surfaces)}. This is the signal the detailed pass uses to tell whether a bare vocative has exactly one possible addressee.
> - neutral_premise: <=40 words, what happens at a plain factual level (who goes where, who meets whom, what is done). No inner-relationship verdicts, no spoilers of emotional arcs.
> - Prefer an entity_id (ent_*) from REGISTRY_SO_FAR when a name clearly matches; else use the clean surface.
> - Every block_id MUST be a marker that literally appears in this chapter's text.
>
> Required JSON shape:
> {
>   "chapter_id": "...",
>   "cast_on_stage": [
>     {"surface": "the innkeeper", "surface_kind": "descriptor", "role_hint": "innkeeper", "first_seen_block": "bk_ch01_b004"},
>     {"surface": "Ravel", "surface_kind": "proper_name", "role_hint": "traveller", "first_seen_block": "bk_ch01_b002"}
>   ],
>   "setting": {"place": "a roadside inn at dusk", "time_frame_hint": "frame_present", "scene_shape": "single_scene_one_location"},
>   "scenes_party_size": [
>     {"block_range": ["bk_ch01_b002", "bk_ch01_b016"], "co_present_count": 2, "participants": ["Ravel", "the innkeeper"]}
>   ],
>   "neutral_premise": "A tired traveller reaches an inn at nightfall, is questioned by the innkeeper about his business, and takes a room for the night."
> }

### 2.3 User template
```
REGISTRY_SO_FAR
{registry_lines_or_"(none yet)"}

NEIGHBOR_SUMMARIES_GIST_ONLY
{up_to_2_previous_chapter_summaries_or_"(none)"}

CHAPTER_ID
{chapter_id}

FULL_CHAPTER_TEXT_WITH_BLOCK_MARKERS
{render_chapter_blocks(chapter)}
```

---

## 3. BƯỚC 1 — Lexicon window  *(mode: `literary_lexicon_v1`)*

### 2.1 Nhiệm vụ
Trên MỘT window vài block, trích **hai loại "được gọi tên"**:
- **glossary_candidates** — vật/nơi/khái niệm văn hoá cần nhất quán cả sách (KHÔNG phải người).
- **character_mentions** — mỗi lần một tên/biệt danh/mô tả-xác-định của NGƯỜI xuất hiện (bằng chứng alias; danh tính gộp ở Bước 4).

KHÔNG làm: quan hệ, thoại, tóm tắt, motif (thuộc bước 2/3). KHÔNG mint entity mới / KHÔNG gộp danh tính.

### 2.2 Logic nhồi window-context
- **Text:** window = các block liên tiếp trong CÙNG một chương, **cắt theo ranh giới scene/đoạn thoại khi được**, tổng ≤ ~500 token nguồn (hoặc ≤ 8 block). Kèm `PREVIOUS/NEXT_WINDOW_TAIL` 1–2 block dạng CONTEXT_ONLY để đọc liền mạch — **cấm trích entry từ đó** (validator chặn block_id ngoài window chính). Render kèm marker.
- **Pack (`REGISTRY_CONTEXT_PACK`):** code quét surface trong window, tra Story-Bible-so-far, bơm **chỉ** entity/glossary có alias/surface xuất hiện trong window — mỗi cái 1 dòng `entity_id | canonical | 1-2 alias`. Trần ≤ ~15 dòng / ~300 token; vượt thì ưu tiên theo tần suất + mới gặp. Mục đích: tên quay lại được **nối vào entity_id cũ**, nơi cũ không bị đề xuất lại. Pilot ch1–4 registry nhỏ nên rẻ.
- KHÔNG bơm: quan hệ, digest, pha.

### 2.3 System prompt (nguyên văn, drop vào `prompt.py`)

> You are the Lexicon extractor for an autonomous English-Vietnamese literary translation pipeline. Read only the English source WINDOW provided by the user. Record two things that are *named* in this window: (1) glossary candidates — places, objects, and culturally specific terms that must stay consistent across the whole book; (2) character mentions — every surface that names or points to a PERSON. This is an evidence pass: you record what is visible, you do NOT decide identities or relationships.
>
> Hard rules:
> - Prompt version: literary_lexicon_v1.
> - Return only valid JSON matching the Required JSON shape. No text outside JSON.
> - The user gives you one WINDOW, not a whole chapter. Extract every qualifying item visible in this window; do not impose a count cap. Windowing keeps each call small; downstream code consolidates duplicates.
> - glossary_candidates are NOT people. Put places (a named house or estate, a town or village, a region — use the work's own proper names) and objects/terms that are NAMED, culturally/period-specific, or plot-significant. An object qualifies ONLY on that bar — NOT ordinary household implements or rooms. Negative examples: window, gate, chair, dinner, letter, moor (unless a proper place name), servant, poker, frying-pan, cellar, hearth, kitchen, table, fire.
> - Each glossary target proposed_target_vi MUST use full Vietnamese diacritics.
> - category MUST be exactly one of: place, object, cultural, other.
> - character_mentions: record every NON-PRONOMINAL surface that names or refers to a person — proper names, nicknames, shortened names, spelling variants, titles used as identity ("the master", "the mistress"), and characterizing epithets ("the ruffian"). One row per surface occurrence-group in this window. A bare pronoun (he/she/I/they…) is NOT a mention surface here even though it refers to a person — skip it entirely; it is resolved later in the narrative pass.
> - Do NOT put plain pronouns as a mention surface: i, me, my, you, your, he, him, his, she, her, we, us, they, them, and their possessives.
> - character_mentions are SINGLE persons. Do NOT record a group ("the household", "the Lintons", "the servants") here — groups appear only in the narrative pass as an addressee/target with reference_kind group.
> - A character_mention must REFER TO A PERSON. Do NOT record descriptions of appearance or body parts, rooms, furniture, or objects (e.g. "stalwart limbs", "a stubborn countenance", "the apartment", "the round table", "a country squire" used descriptively). A descriptor qualifies only when it stands in for a specific person as a referring expression ("the master", "the ruffian").
> - The CONTEXT_ONLY tail is READ-ONLY background to help you understand the window — you MUST NOT extract any glossary_candidate or character_mention that lives only in it. Everything you output must come from the ACTIVE window, and every block_id you cite must be an ACTIVE-window marker. Set top-level context_only_used=true if you leaned on the tail to understand the window (advisory audit only); never cite a context-only block_id.
> - mention_type MUST be exactly one of: name, nickname, descriptor. Use descriptor for both titles-used-as-identity ("the master", "the mistress") and characterizing epithets ("the ruffian", "the old man").
> - Give every mention a stable mention_id of the form m_<block>_<n> (for example m_bk_ch01_b006_01).
> - resolution_status MUST be exactly one of: named, candidate, unknown. Use named for ANY explicit proper name or nickname — including a bare first name like "Mira" or "Alden", even on first mention and even without a title (named describes the SURFACE being a real name, not whether you already know the person). Use candidate when the surface is a descriptor or title (never a bare pronoun — pronouns are not extracted in this pass at all) that you map to a specific entity from REGISTRY_CONTEXT_PACK but are not certain. Use unknown ONLY for a non-name reference you cannot attribute — a real proper name is never unknown.
> - candidate_entity_ids: fill ONLY when resolution_status is candidate, copying entity_id values verbatim from REGISTRY_CONTEXT_PACK (a surface may list more than one candidate). Leave it [] for named or unknown. Do NOT invent new entity_id values (never coin an id such as char_<name> or ent_<name>) and do NOT output canonical_entity_id — identity is assigned later by consolidation. If NO REGISTRY_CONTEXT_PACK entry matches the person, leave candidate_entity_ids [] and use resolution_status named (for a real proper name) or unknown (otherwise) — never mint an id to fill the gap.
> - Descriptor examples such as "his aunt", "the innkeeper", "the coachman", or "the old gardener" are candidate ONLY when REGISTRY_CONTEXT_PACK gives a matching entity_id. Without a matching pack id, they are unknown with []. There is NO placeholder id: never output an id such as ent_unknown, ent_unnamed, or ent_narrator — any id not literally present in REGISTRY_CONTEXT_PACK is invalid.
> - Do NOT output relationships, dialogue, summaries, or motifs in this pass.
> - Every block_ids entry MUST be a block marker COPIED VERBATIM from the [brackets] in the window text (they look like bk_ch01_b005) — never abbreviate the id or drop its prefix. Only markers that literally appear in this window are allowed.
>
> Required JSON shape:
> {
>   "chapter_id": "...",
>   "window_block_ids": ["bk_ch01_b005", "bk_ch01_b006"],
>   "context_only_used": false,
>   "glossary_candidates": [
>     {"source_term": "the Blackmoor Estate", "proposed_target_vi": "Điền trang Blackmoor", "category": "place", "do_not_translate": false, "termhood": "name of the principal house; must stay consistent", "block_ids": ["bk_ch01_b002"]}
>   ],
>   "character_mentions": [
>     {"mention_id": "m_bk_ch01_b003_01", "surface": "Mr. Alden", "mention_type": "name", "resolution_status": "named", "candidate_entity_ids": [], "block_ids": ["bk_ch01_b003"]},
>     {"mention_id": "m_bk_ch01_b006_01", "surface": "the master", "mention_type": "descriptor", "resolution_status": "unknown", "candidate_entity_ids": [], "block_ids": ["bk_ch01_b006"]}
>   ]
> }

### 2.4 User template
```
CHAPTER_BRIEF
{chapter_brief_from_B0}

NEIGHBOR_SUMMARIES_GIST_ONLY
{up_to_2_previous_chapter_summaries_or_"(none)"}

REGISTRY_CONTEXT_PACK
{pack_lines_or_"(none yet)"}

CHAPTER_ID
{chapter_id}

ENGLISH_SOURCE_WINDOW_WITH_BLOCK_MARKERS
{render_chapter_blocks(window)}
```

### 2.5 Validator (CodeX, parser/validate-only, chưa cần API)
Loại/cờ mỗi entry nếu: (a) `block_ids` chứa marker KHÔNG có trong window; (b) `category` ngoài enum; (c) `mention_type`/`resolution_status` ngoài enum; (d) surface mention là đại từ trần; (e) glossary trùng người (surface khớp một character_mention cùng window → cảnh báo); (f) `candidate_entity_ids` chứa id không có trong pack; (g) `resolution_status=candidate` mà `candidate_entity_ids` rỗng, hoặc `named/unknown` mà lại có candidate; (h) xuất hiện `canonical_entity_id` (cấm ở window). Đếm & log: `#glossary`, `#mentions`, `#named`, `#candidate`, `#unknown`, `#dropped_bad_block`, `#pronoun_dropped`, `#context_only_used_true` (đo dependency vào tail, không cho evidence từ tail). Không sửa nội dung ngôn ngữ — chỉ cơ khí.

### 2.6 Guard hallucinate (đã cài trong prompt)
`resolution_status="unknown"` được PHÉP và khuyến khích khi mơ hồ (chống ép đoán → chống hallucinate danh tính); `candidate_entity_ids` chỉ trỏ vào pack, cấm mint id mới; `canonical_entity_id` KHÔNG xuất hiện ở bước này (B4 quyết); mọi block_id phải thấy được.

### 2.7 Ví dụ kỳ vọng (WH ch01, để spot-check)
Window mở đầu ch01 (Lockwood tự sự): kỳ vọng `glossary`: Wuthering Heights (place), Thrushcross Grange (place), Gimmerton (place nếu xuất hiện). `character_mentions`: "Mr. Heathcliff"→name (resolution named), "I"→(đại từ, BỎ; nếu có "Lockwood" surface thì name), "the master"→descriptor, resolution candidate nếu pack có Heathcliff, else unknown. KHÔNG được có relation/summary. Canary chưa áp ở bước này (áp ở B4) nhưng ở ch03 các surface "Catherine Earnshaw", "Catherine Linton", "Catherine Heathcliff" phải ra **ba dòng mention riêng**, resolution candidate/unknown, KHÔNG tự gộp; B4 sẽ gán entity kèm `valid_range` — vì "Catherine Linton" là alias của **cả mẹ (sau cưới Edgar) lẫn con (khai sinh)** ở hai khoảng block khác nhau.

---

## 3. BƯỚC 2 — Narrative evidence window  *(mode: `literary_narrative_v1`)*

### 3.1 Nhiệm vụ
Trên CÙNG window bước 1, ghi **tương tác quan sát được** — ai nói/làm gì với ai. Đây là bước dễ hỏng nhất: kỷ luật **CẤM PHÁN PHA** phải tuyệt đối.

Hai rổ (rút từ 3 xuống 2 so phác thảo task — gộp address thành FIELD, không thành rổ riêng, để giảm bề mặt schema → giữ recall, bài học D2L nhồi-quá-nhiều-rổ):
- **speaker_turns** — mỗi lượt thoại trực tiếp (có người nói xác định): kèm `address_term_used` (từ hô nguyên văn trong câu, nếu có) → đây chính là tín hiệu xưng-hô.
- **relation_events** — hành động xã hội/thể chất quan sát được giữa người (đánh, che chở, phục dịch, chế nhạo, từ chối…), `event_type` là ĐỘNG TỪ CỤC BỘ, KHÔNG phải nhãn quan hệ.

KHÔNG làm: kết luận quan hệ/pha, `state_label`, `valid_from/to`, tóm tắt, motif, glossary/mention (đã ở bước 1/3/4).

### 3.2 Logic nhồi window-context *(Builder-time — KHÔNG có relation/policy, chúng là output B4)*
- **Text:** CÙNG window bước 1 (cùng vòng lặp). Chạy bước 1 TRƯỚC để lấy mentions làm mồi.
- **`ACTIVE_NARRATOR_HINTS_BY_BLOCK_RANGE`:** gợi ý người kể theo block-range (heuristic từ said-tag/"I", hoặc "unknown" theo range) — **KHÔNG single-narrator** (ch4 chuyển Lockwood→Nelly GIỮA chương). B2 chỉ cần hint để phân biệt "I"/lời kể vs thoại; B3 refine thành `narration_frame_segments` chuẩn.
- **`CHAPTER_ROSTER_ON_STAGE`:** roster tích luỹ trong CHÍNH chương này tới thời điểm window (canonical + alias + entity_id, từ mentions bước-1 các window trước). Bounded theo chương, KHÔNG cả sách. Nguồn `candidate_entity_ids`.
- **`WINDOW_MENTIONS_FROM_LEXICON_PASS`:** mention bước-1 của đúng window này → grounding addressee. Không cấm speaker/target là surface ngoài list (giữ recall nếu bước-1 sót).
- KHÔNG nhồi: relation facts / address policy / pha (chưa build; và sẽ mồi model xác nhận pha → thiên lệch).

### 3.3 System prompt (nguyên văn)

> You are the Narrative-evidence extractor for an autonomous English-Vietnamese literary translation pipeline. Read only the English source WINDOW provided by the user. Record observable interaction evidence: who speaks to whom, and who does what to whom. This is a strict evidence pass — you record only what is directly shown in these blocks.
>
> Hard rules:
> - Prompt version: literary_narrative_v1.
> - Return only valid JSON matching the Required JSON shape. No text outside JSON.
> - CRITICAL — do NOT infer relationships, alliances, feelings, trust, or story phases. Those are decided later from the whole timeline. Report only single, locally observable acts and utterances.
> - event_type MUST be a concrete observable action verb in lower_snake_case (examples: addresses, greets, mocks, strikes, protects, serves, refuses, threatens, embraces, orders, weeps_over, curses). It MUST NOT be a relationship or phase label. Forbidden event_type values: ally, enemy, friend, rival, love, hatred, betrayal, trust, alliance, reconciliation, phase, any "*_phase".
> - Do NOT output state_label, valid_from_block, valid_to_block, address policy, or any book-level conclusion in this pass.
> - speaker_turns: one row per direct quoted utterance. Give each a turn_id (t_<block>_<n>). "speaker" and "addressee" are each an OBJECT: {surface, reference_kind, resolution_status, candidate_entity_ids, attribution_method, confidence}.
>   - reference_kind is one of: person, group, narrator, reader, unknown. Only person may become a character entity later; a group ("the household"), the narrator, or the reader MUST NOT be minted as a person.
>   - resolution_status is one of: named (an explicit name or tag identifies them, e.g. "said Alden"), candidate (you map a pronoun/descriptor to a roster entity but are not certain), unknown (you cannot tell — never force a guess).
>   - candidate_entity_ids: when resolution_status is candidate it MUST be non-empty — copy the id(s) verbatim from CHAPTER_ROSTER_ON_STAGE. If the roster has no id for the referent (including the first-person narrator before they are named on the page), use resolution_status=unknown with [] instead of candidate; never invent an id. There is NO placeholder id: ids such as ent_unknown, ent_unnamed, or ent_narrator do not exist — any id not literally listed in CHAPTER_ROSTER_ON_STAGE or REGISTRY_CONTEXT_PACK is an error; "I do not know who this is" is expressed ONLY as resolution_status=unknown with []. For named/unknown leave [].
>   - attribution_method is one of: explicit_tag, turn_alternation, nearby_context, narrator_inference. This is HOW you attributed the reference — it is the real trust signal, more than confidence. It is NEVER a resolution_status value: do NOT put named/candidate/unknown in attribution_method.
>   - confidence is one of: high, medium, low. Do NOT output a number.
> - If the utterance contains a vocative (address_term_used is not null), the addressee MUST be the specific person that vocative names, not a group. Use a group addressee (reference_kind group) only when there is no specific vocative.
> - A GENERIC honorific used as a vocative (sir, madam, ma'am, my lord, my lady, master, mistress, with no personal name attached) does NOT by itself name a specific person. Resolve it by turn-taking using CHAPTER_BRIEF: if the current scene in scenes_party_size has co_present_count == 2, the addressee is the OTHER co-present person (the one who is not the speaker); set resolution_status=candidate, candidate_entity_ids to that person, attribution_method=turn_alternation, confidence=medium. If co_present_count >= 3 and no other cue points to exactly one person, leave the addressee unknown — never force a guess.
> - The addressee is the person the words are spoken TO, not a person or thing the words are ABOUT. In "You had better let the dog alone," the addressee is the listener being warned, not "the dog". Never record a non-person (animal, object) as an addressee: if the only surface available is such a thing, resolve the addressee from turn-taking / scene participants instead, or leave it unknown.
> - utterance_quote = a short verbatim snippet of the spoken words (<=20 words; the whole utterance if short). This verbatim text — not the gist — is the evidence for later address/register scoring.
> - address_term_used = the literal vocative the speaker uses for the addressee in the utterance ("Mira", "Mr. Alden", "master", "my girl"), or null. Copy verbatim; do not translate here.
> - register_cue = one short lowercase tone word if visible (neutral, intimate, deferential, paternal, hostile, mocking), else "neutral".
> - relation_events: give each an event_id (e_<block>_<n>). "actor" and "target" are the SAME object shape as speaker/addressee. evidence_quote is a short literal snippet (<=12 words) copied from the window that shows the act. actor and target should be PERSONS or the narrator — do NOT create a relation_event whose actor or target is an object or an animal (a dog, a room, furniture).
> - Do NOT record the narrator's storytelling voice as a speaker_turn. Only record quoted speech by a character in-scene. (A first-person narrator counts as a speaker only when quoted speaking to someone in the scene.)
> - A pronoun or zero-subject MAY be the surface when that is all the text gives: set resolution_status to candidate (if roster + turn order point to someone) or unknown, and record attribution_method. Do NOT drop a turn or event just because its subject is a pronoun.
> - Every block_id MUST be a marker that literally appears in this window.
>
> Required JSON shape:
> {
>   "chapter_id": "...",
>   "window_block_ids": ["bk_ch01_b006", "bk_ch01_b007"],
>   "context_only_used": false,
>   "speaker_turns": [
>     {"turn_id": "t_bk_ch01_b006_01",
>      "speaker": {"surface": "the innkeeper", "reference_kind": "person", "resolution_status": "named", "candidate_entity_ids": [], "attribution_method": "explicit_tag", "confidence": "high"},
>      "addressee": {"surface": "sir", "reference_kind": "person", "resolution_status": "candidate", "candidate_entity_ids": ["ent_ravel"], "attribution_method": "turn_alternation", "confidence": "medium"},
>      "utterance_quote": "And what brings you so far north, sir?",
>      "address_term_used": "sir", "register_cue": "neutral", "utterance_gist": "asks the traveller his business", "block_id": "bk_ch01_b006"}
>   ],
>   "relation_events": [
>     {"event_id": "e_bk_ch01_b006_01",
>      "actor": {"surface": "the innkeeper", "reference_kind": "person", "resolution_status": "named", "candidate_entity_ids": [], "attribution_method": "explicit_tag", "confidence": "high"},
>      "target": {"surface": "sir", "reference_kind": "person", "resolution_status": "candidate", "candidate_entity_ids": ["ent_ravel"], "attribution_method": "turn_alternation", "confidence": "medium"},
>      "event_type": "questions", "evidence_quote": "what brings you so far north", "block_id": "bk_ch01_b006"}
>   ]
> }

### 3.4 User template
```
CHAPTER_BRIEF
{chapter_brief_from_B0}

NEIGHBOR_SUMMARIES_GIST_ONLY
{up_to_2_previous_chapter_summaries_or_"(none)"}

ACTIVE_NARRATOR_HINTS_BY_BLOCK_RANGE
{block_range | narrator_hint | "unknown"}

CHAPTER_ROSTER_ON_STAGE
{roster_lines_or_"(none yet)"}

WINDOW_MENTIONS_FROM_LEXICON_PASS
{lexicon_mentions_or_"(none)"}

CHAPTER_ID
{chapter_id}

ENGLISH_SOURCE_WINDOW_WITH_BLOCK_MARKERS
{render_chapter_blocks(window)}
```

### 3.5 Validator (CodeX)
Cờ/loại nếu: (a) block_id không có trong window; (b) `event_type` khớp danh sách CẤM (nhãn quan hệ/pha) → **loại + đếm `#phase_leak`** (chỉ số kỷ luật quan trọng nhất bước này); (c) `event_type` không lower_snake_case; (d) key ngoài schema (`state_label`, `valid_from_block`…) → cờ regression; (e) `reference_kind`/`resolution_status`/`attribution_method`/`confidence` ngoài enum; (f) `candidate_entity_ids` chứa id ngoài roster, hoặc candidate mà rỗng / named-unknown mà không rỗng; (g) CHỈ `reference_kind=person` được vào entity-person consolidation; `group/narrator/reader` ĐƯỢC `resolution_status=named` (nhóm/người-kể được định danh là hợp lệ) — validator chỉ đảm bảo chúng KHÔNG đi vào consolidation entity-person, KHÔNG cờ named. Đếm & log (HEADLINE): `#turns`, `#events`, `#phase_leak`, `#address_term_present`, `#context_only_used_true`, phân bố `reference_kind`, và unknown-rate CHIA theo vai + `attribution_method`, tách `#unknown_with_evidence` vs `#unknown_empty` (empty = mất bằng chứng, mới xấu). Cảnh báo **2 phía**: unknown quá cao = windowing kém; quá thấp = đoán bừa. Không sửa ngôn ngữ.

### 3.6 Vì sao 2 rổ không 3
Address (xưng-hô) là FIELD của speaker_turn (`address_term_used`+`register_cue`), không phải rổ riêng — mỗi rổ thêm làm loãng attention + rớt recall (đã thấy ở D2L khi nhồi nhiều rổ). Narrator-reference term (Nelly gọi "Mr. Heathcliff" trong lời kể) KHÔNG bắt ở đây (đã có ở mention bước-1); tín hiệu register của lời kể để Bước 3/4 xử từ mentions + digest, tránh rổ thứ 3.

### 3.7 Ví dụ kỳ vọng (WH ch04, Nelly kể Heathcliff bé xuất hiện)
`speaker_turns`: Mr. Earnshaw giới thiệu đứa bé (addressee=household, register paternal). `relation_events`: Hindley + Cathy ban đầu `refuses`/`mocks` đứa bé (evidence_quote literal); Mrs. Earnshaw `curses`/`scolds`. KHÔNG được có `event_type: "enemy"` hay `"rivalry_phase"` — nếu có = `#phase_leak`, validator loại. Quan hệ Heathcliff↔Hindley thù địch là KẾT LUẬN của Bước 4 từ chuỗi event, không phải nhãn ở đây.

---

## 4. BƯỚC 3 — Chapter digest  *(mode: `literary_digest_v1`)*

### 4.1 Nhiệm vụ
Nhận TRỌN một chương + rolling summary chương trước + ledger b1/b2 đã trích của chương. Xuất **digest structured, translation-ready** (KHÔNG prose). Tổng hợp vòng cung tầm-chương mà window nhỏ bỏ lỡ. **KHÔNG finalize pha quan hệ** (đó là B4 toàn sách) — chỉ tổng hợp evidence + cờ candidate_transition.

### 4.2 Logic nhồi *(Builder-time)*
- **Text:** TRỌN chương (mọi block + marker).
- `PREVIOUS_CHAPTER_ROLLING_SUMMARY` (~150 từ, từ digest chương trước).
- `CHAPTER_ROSTER` (entity + alias từ b1) + `CHAPTER_RELATION_EVENTS` (compact từ b2: pair | event_type | block) → digest **grounded**, không đọc mù.
- KHÔNG nhồi: relation phase/policy đã suy (chưa có; sẽ thiên lệch).

### 4.3 System prompt (nguyên văn)

> You are the Chapter-digest builder for an autonomous English-Vietnamese literary translation pipeline. You receive one FULL chapter, a short rolling summary of the previous chapter, and the lexicon + narrative evidence already extracted from this chapter's windows. Produce a STRUCTURED, translation-ready digest — not prose. Synthesize chapter-level arcs that small windows miss; do NOT finalize relationship phases (that happens later across the whole book).
>
> Hard rules:
> - Prompt version: literary_digest_v1.
> - Return only valid JSON matching the Required JSON shape. No text outside JSON.
> - narration_frame_segments: split the chapter into CONTIGUOUS block ranges by WHO narrates. Some works switch narrator mid-chapter or mid-passage; do not assume one narrator per chapter — segment by who actually narrates, however many that is. story_time_label is one of: frame_present, retrospective_past, embedded_flashback.
> - Do NOT finalize relationship phases and do NOT output valid_from/to intervals. For relations, only aggregate this chapter's observed events per pair; if the text clearly shows a turning point, note a candidate_transition with a trigger block. Every relation_event_summary row has status "evidence_only".
> - relation_event_summary.observed_valence_hint is one of: positive, negative, mixed, unclear. It is an EVENT summary, NOT a phase label and NOT a trigger on its own — consolidation still needs event evidence to open a phase.
> - character_state_changes: only changes VISIBLE in this chapter to a character's social_status, alias_or_title, life_status, or residence — each with trigger_block and evidence_quote. observed_scope is "this_chapter".
> - unresolved_threads: open questions or pending turning points the chapter raises but does not resolve. kind is one of: mystery, pending_transition, question. Include opened_block.
> - translator_relevant_facts: a curated list, MAX 8 per chapter, of facts a translator needs to render THIS chapter correctly. Not every motif — only what changes how text is rendered. Each has fact_type one of: narrator, register, speech_style, status, setting; and block_evidence.
> - motifs: recurring images/themes, each with block_ids.
> - Prefer entity_id references (ent_*) from CHAPTER_ROSTER where available; else a clean surface. Every block reference MUST be COPIED VERBATIM from the [brackets] in the chapter text (they look like bk_ch04_b012) — never abbreviate or drop the prefix.
>
> Required JSON shape:
> {
>   "chapter_id": "ch04",
>   "chapter_rolling_summary": "<=150 English words, for the next chapter's context",
>   "narration_frame_segments": [
>     {"narrator_ref": "ent_alden", "block_range": ["bk_ch04_b001", "bk_ch04_b004"], "story_time_label": "frame_present"},
>     {"narrator_ref": "ent_mira", "block_range": ["bk_ch04_b005", "bk_ch04_b040"], "story_time_label": "retrospective_past"}
>   ],
>   "scene_summaries": [
>     {"scene_id": "s_ch04_01", "block_range": ["bk_ch04_b005", "bk_ch04_b018"], "summary": "...", "key_participants": ["ent_master", "ent_rook", "ent_bram"]}
>   ],
>   "character_state_changes": [
>     {"entity_ref": "ent_rook", "attribute": "social_status", "from": "orphan_outsider", "to": "taken_into_household", "trigger_block": "bk_ch04_b012", "evidence_quote": "you must take him in as one of ours", "observed_scope": "this_chapter"}
>   ],
>   "relation_event_summary": [
>     {"pair": ["ent_bram", "ent_rook"], "observed_valence_hint": "negative", "event_ids": ["e_bk_ch04_b014_01"], "candidate_transition": {"trigger_block": "bk_ch04_b012", "note": "arrival sparks the son's jealousy"}, "status": "evidence_only"}
>   ],
>   "unresolved_threads": [
>     {"thread_id": "th_ch04_01", "description": "The outsider child's origin and real name unknown", "opened_block": "bk_ch04_b010", "kind": "mystery"},
>     {"thread_id": "th_ch04_02", "description": "The son's resentment likely to escalate", "opened_block": "bk_ch04_b016", "kind": "pending_transition", "pair": ["ent_bram", "ent_rook"]}
>   ],
>   "motifs": [{"note": "the intruder disrupting the household", "block_ids": ["bk_ch04_b012"]}],
>   "translator_relevant_facts": [
>     {"fact_type": "narrator", "fact": "Mira narrates in past tense, addressing the listener; her stance to the child is ambivalent", "block_evidence": ["bk_ch04_b005"]},
>     {"fact_type": "register", "fact": "the master uses an affectionate/paternal register to the child", "block_evidence": ["bk_ch04_b012"]}
>   ]
> }

### 4.4 User template
```
PREVIOUS_CHAPTER_ROLLING_SUMMARY
{prev_summary_or_"(none)"}

NEIGHBOR_SUMMARIES_GIST_ONLY
{up_to_2_previous_chapter_summaries_or_"(none)"}

CHAPTER_BRIEF
{chapter_brief_from_B0}

CHAPTER_ROSTER
{entity_id | canonical | aliases}

CHAPTER_RELATION_EVENTS
{pair | event_type | block_id}

CHAPTER_ID
{chapter_id}

FULL_CHAPTER_TEXT_WITH_BLOCK_MARKERS
{render_chapter_blocks(chapter)}
```

### 4.5 Validator (CodeX)
Cờ/loại nếu: (a) block ref không có trong chương; (b) `narration_frame_segments` KHÔNG phủ liên tục toàn chương (gap/overlap) → cờ; (c) `story_time_label`/`kind`/`attribute`/`observed_scope`/`observed_valence_hint`/`fact_type` ngoài enum; (d) **xuất hiện `valid_from_block`/`valid_to_block`/`phase_label` finalize** ở relation → cờ regression (B3 cấm finalize pha); (e) `relation_event_summary.status ≠ "evidence_only"`; (f) `#translator_facts` > 8 → loại phần dư. Đếm & log: `#frame_segments`, `#scenes`, `#state_changes`, `#unresolved{mystery,pending_transition,question}`, `#translator_facts`, `#motifs`.

### 4.6 Ví dụ kỳ vọng (WH ch04)
`narration_frame_segments` phải bắt được **chuyển Lockwood→Nelly giữa chương** (2 segment). `character_state_changes`: Heathcliff `social_status` orphan→household (trigger b nơi Earnshaw đem về). `relation_event_summary` cặp Hindley-Heathcliff `observed_valence_hint: negative`/evidence_only + candidate_transition (KHÔNG phải phase). `unresolved_threads`: gốc gác Heathcliff (mystery), thù Hindley sắp leo thang (pending_transition). KHÔNG được có interval finalize.

---

## 5. BƯỚC 4 — Consolidation & audit  *(pipeline, KHÔNG một call: `literary_consolidate_v1`)*

### 5.1 Tổng quan — 4 lớp riêng, code làm nặng, LLM phán điểm-huyệt
B4 KHÔNG phải một call (tránh tái tạo monolith). Là pipeline: mỗi lớp = code deterministic gom candidate → **micro-call LLM CHỈ cho ca mơ hồ** (kèm lát cắt evidence) → code apply có gate. Đầu ra artifact = **Story Bible** (KHÔNG ghi DB ở pilot). Thứ tự: L1 identity → L2 interaction → L3 relation-phase → **L3b character-state** → L4 address-policy → Auditor → canary → freeze.

### 5.2 Lớp 1 — Identity partition trên mention-atom (B4 v2, thay adjudicate_v1)
- **Atom = MỘT mention row** `(atom_id, block_id, surface, quote_context, hint_entity_id)` từ B1
  artifact (ledger id chỉ là hint). Lý do đã đo: cùng surface "the master" trong MỘT chương chỉ
  3 người khác nhau; atom cấp chương không tách nổi (M4d vòng 2, verified).
- **Code (tất định):** dựng atoms as-of N từ union chain-manifest; tính FRONTIER (atom mới +
  group cũ có evidence mới); shard theo component nếu vượt cap — estimator quyết số call.
- **LLM `literary_identity_partition_v1`** (frontier-incremental, KHÔNG gửi lại toàn bộ khi
  không đổi):
> - Prompt version: literary_identity_partition_v1.
> You are resolving character identity for one chapter of a long book. Each ATOM is one mention occurrence: {atom_id, block_id, surface, quote_context, hint_entity_id}. PRIOR_GROUPS are identity groups already established in earlier chapters: {entity_id, canonical_surface, referent_kind, member_summary}. IDENTITY_HINTS are statements taken from chapter digests; treat them as leads to check against the quoted text, never as evidence by themselves.
> Assign EVERY listed atom to exactly one group. A group is one real-world referent.
> - An honorific difference (Mr./Mrs./Miss/young/old) is NOT evidence of identity: a wife, a widow and a father-in-law can share one surname; a title like "the master" can pass between people inside one chapter.
> - The SAME surface may denote DIFFERENT people in different scenes or narration frames; judge each atom from its own quote_context.
> - To extend a prior group, set reuse_entity_id to its entity_id. Reopen or split a prior group ONLY with a quote contradicting its unity, recorded in evidence with supports=different_identity.
> - Prefer SPLIT when unsure — merging two people is worse than leaving two entities.
> - referent_kind: person | place | group_reference | literary_allusion | unknown. A coordination surface naming two people at once counts as group_reference, never a person.
> - Output JSON only: {"groups": [{"group_key": string, "reuse_entity_id": string or null, "referent_kind": string, "canonical_atom_id": string, "member_atom_ids": [strings], "status": "resolved" | "uncertain" | "quarantine", "alias_bindings": [{"surface": string, "member_atom_ids": [strings], "valid_from_block": string, "valid_until_block": string or null}], "evidence": [{"block_id": string, "quote": string, "source_atom_ids": [strings], "supports": "same_identity" | "different_identity"}]}]}
> - Every input atom appears exactly once across all groups. Do NOT invent atom ids, do NOT mint entity ids, do NOT paraphrase quotes — copy them verbatim from the given text. Output nothing but the JSON object.
- **Code apply có gate (trên BẢN SAO, publish atomic):** exact-partition; không atom trùng nhóm;
  không merge khác referent_kind; quote phải là substring THẬT của block; no-new-collision.
  **ID ổn định xuyên scope:** group mở rộng giữ entity_id cũ (reuse_entity_id); split → nhánh chứa
  canonical atom cũ GIỮ id + `supersedes_entity_ids` ghi nhánh mới; tie → halt. LLM không bao giờ
  mint id cuối — code mint tất định.
- **Identity đổi → replay:** mọi pair chứa entity có membership đổi = affected set; remap toàn bộ
  evidence cũ rồi chạy lại L3 phase cho các pair đó từ evidence đầu tiên.
- Output T2: `{entity_id, canonical, referent_kind, aliases:[{surface, valid_from_block,
  valid_until_block|null}], supersedes_entity_ids}`. referent_kind ≠ person → KHÔNG vào T2:
  place đối chiếu về T1, group_reference/unknown → review_only (§30-style quarantine).

### 5.3 Lớp 2 — Interaction consolidation (speaker/addressee cleanup)
- **Code:** resolve `speaker`/`addressee` candidate/unknown bằng turn-alternation + roster + `attribution_method` (gate theo METHOD, không confidence). Pronoun→entity khi turn-order + roster cho phép.
- **Escalation queue** (§1.7): unknown QUAN TRỌNG còn lại (ảnh hưởng relation/address/speaker) → micro-call trên **scene slice** (không full chapter); output chỉ sửa resolution, cấm thêm fact.
- Output T3: speaker_turns có `speaker_entity_id`/`addressee_entity_id` (hoặc vẫn unknown, đếm `#unknown_with_evidence`/`#unknown_empty`).

### 5.4 Lớp 3 — Relation-phase consolidation (change-point → interval) *(lõi)*
- **Code:** gom `relation_events` + `relation_event_summary` theo cặp, sắp theo block; đánh dấu candidate boundary (valence flip từ b3).
- **LLM micro-call `literary_phase_segment_v2`** cho từng cặp (shard/batch tất định theo pair, estimator quyết số call):
> - Prompt version: literary_phase_segment_v2.
> You are segmenting one character pair's relationship into ordered phases from an evidence stream, and extracting directed relationship facts. The pair header gives two entity ids, A and B. Read the pair's events in text order.
> PHASES: output a list of NON-OVERLAPPING phases in block order. Each phase: phase_label from EXACTLY this set [allied, friendly, neutral, strained, hostile, estranged, dependent, reconciled]; valid_from_block; valid_until_block (use null + status "open" for the last, unclosed phase); trigger_block (the block where this phase begins); trigger_evidence (a short verbatim quote). Open a NEW phase only when a trigger shows a strong change in language or action — a single mocking remark or momentary mood is an event, NOT a new phase. Do NOT invent phases with no event evidence. Prefer FEWER phases when evidence is weak.
> RELATION_FACTS: directed factual relationships between A and B that the evidence states or clearly shows. Each fact: {subject_ref (A or B's entity id), predicate_code from EXACTLY this set [parent_of, child_of, spouse_of, sibling_of, daughter_in_law_of, son_in_law_of, father_in_law_of, mother_in_law_of, grandparent_of, grandchild_of, cousin_of, servant_of, master_of, landlord_of, tenant_of, guest_of, neighbor_of, guardian_of, ward_of, other], object_ref, valid_from_block, evidence_block, evidence_quote (verbatim), predicate_note (free text)}. Direction matters: subject predicate object means the subject IS the predicate of the object. Use predicate_code "other" ONLY when nothing in the set fits. Do NOT derive a fact from a title or an honorific alone; every fact needs quoted evidence. Output empty lists when the evidence supports nothing.
> OUTPUT: JSON only — one JSON object with exactly two keys, "relation_phases" and "relation_facts", holding the lists described above; every phase includes its pair. No text outside the JSON object.
- **Code validate:** non-overlap, block-order tăng, taxonomy enum, đúng 1 open-interval cuối, mọi trigger có evidence trong range; relation_facts: predicate_code trong `literary_predicate_taxonomy_v1` (list ở blockquote trên — versioned cùng prompt), không self-loop, subject/object đúng 2 id của pair, evidence_quote là substring THẬT của evidence_block; `other` → review_only (không runtime). Retry taxonomy: transport/parse/schema-malformed retry 1 lần (lưu raw); semantic-gate fail KHÔNG regenerate → quarantine/halt. Reject → nếu cặp có **≥2 candidate_transition hoặc valence-flip mạnh** thì đánh `blocked_for_runtime` (KHÔNG nhồi relation/address cho cặp tới khi human review — tránh single-phase CHE under-seg); cặp đơn giản mới fallback single-phase + review.
- Output `entity_relations`: `{pair, phase_label, valid_from_block, valid_to_block|null, status, trigger_block, trigger_evidence}`. **`valence` KHÔNG do LLM xuất** — code suy từ `phase_label` qua map cố định (allied/friendly/dependent/reconciled→positive; neutral→neutral; strained/hostile/estranged→negative).

### 5.4b Lớp 3b — Character-state interval consolidation
Biến `character_state_changes` (b3) thành `entity_state_intervals` — song song relation-phase nhưng cho thuộc tính đơn-thực-thể (nguồn `social_status` mà L4 cần).
- **Code:** gom state_changes theo (entity, attribute), sắp theo trigger_block → chuỗi giá trị theo thời gian; mỗi thay đổi mở interval mới, trigger kế cùng attribute đóng interval trước. `attribute` ∈ {social_status, alias_or_title, life_status, residence}.
- **KHÔNG LLM riêng** khi evidence rõ (đây là chuỗi giá trị, không phải segment mờ); escalate CHỈ khi mâu thuẫn.
- Output `entity_state_intervals`: `{entity_id, attribute, value, valid_from_block, valid_to_block|null, status, trigger_block, evidence}` (open-interval). L4 tra state as-of phase từ đây.

### 5.5 Lớp 4 — Address-policy proposal (xưng hô theo pair × phase)
- **LLM micro-call `literary_address_policy_v1`:** cho mỗi (pair, phase), đề xuất xưng hô tiếng Việt **MỖI CHIỀU là object riêng**: `a_to_b {self, address, register, evidence_level, needs_human_review}` và `b_to_a {...}`. `evidence_level` ∈ observed|inferred|unsupported. **grounded** trên phase_label + `entity_state_intervals.social_status` (L3b) as-of phase + `address_term_used` tiếng Anh (b2). Đủ dấu.
- **Chiều `unsupported`** (không có address_term quan sát cho chiều đó) **KHÔNG được dùng runtime** — chống model bịa chiều chưa thấy (A→B rõ KHÔNG kéo theo B→A).
- **Proposal-only:** `needs_human_review` PER-CHIỀU; áp Translator chỉ sau human review (RECONCILE §2.4 + `weighted-ledger-promotion-three-gate`). Trường DEFERRED-nặng — pilot xuất proposal.

### 5.6 Auditor + canary + freeze
- **Auditor precision** (kiểu D2L C3): drop entity/relation rác/generic, giữ sàn recall (đo, không cap).
- **Canary gate (§1.7):** fail LỚN nếu vi phạm (hai-Catherine merge, narrator "I" gộp Lockwood+Nelly, "the master" global…). Canary là điều kiện PASS pilot.
- **Freeze (pilot = PARTIAL story bible):** artifact tự khai `scope` (ch1–4). Interval open đóng tới `artifact_scope_end_block` HOẶC giữ `status="open_within_scope"` — **KHÔNG giả vờ đóng tới hết sách** (pilot chỉ thấy ch1–4). Chỉ full-book mới close tới end-of-book. Ghi artifact **Story Bible** JSON: `scope + T1 glossary + T2 entities(alias valid_range) + T3 speaker_turns + T4 chapter_digests + entity_relations(phase intervals) + entity_state_intervals + address_policies(proposal) + narration_frame_segments + unresolved_threads`. `as_of` query (§1.9) là hợp đồng RUNTIME — **test ở L2b, KHÔNG ở B4**.

### 5.7 Metric B4 (headline)
`#entities`, `#aliases_with_valid_range`, `#phases`, `#phases_per_pair` (theo dõi over/under-seg), `#open_intervals`, `#entity_state_intervals`, `#pairs_blocked_for_runtime`, `#unknown_with_evidence` vs `#unknown_empty`, `#escalated`, `#canary_pass/fail`, `#address_policies_proposed`, `#address_dirs_unsupported`. Canary fail = KHÔNG scale.


---

## M4f v2 upstream prompt tier (Canonical v1 LOCKED c6cd147 — §12 order: B0v2 → B1v2 → B2v2 → B3v2)

Design notes (prose, never reaches the model): these four prompts implement the upstream schema changes of Canonical §2/§4/§5/§7. Contamination paths removed by construction: B0 v2 receives ONLY its scene text (no registry, no neighbor summaries, no whole-chapter view — the b004 hint-leak path is gone at the input). B1 v2 is pure window-local extraction: `resolution_status` and `candidate_entity_ids` are DELETED from the schema (identity belongs to B4 adjudication; B1 hints are permanently barred from authority). B2 v2 endpoints cite `mention_ref` — the occurrence-witness link that made 732/732 endpoints unassignable after splits. B3 v2 segments embedded frames (the ch3 diary/dream defect). Code mints every id except the mechanical `m_<block>_<n>` mention ids; models never mint entity/claim/endpoint ids.

### literary_chapter_brief_v2 (B0 v2 — scene-local cast claims)

> - Prompt version: literary_chapter_brief_v2.
> - Return only valid JSON matching the Required JSON shape. No text outside the JSON object.
> - You receive ONE SCENE of a book (not a whole chapter): a contiguous run of blocks, each tagged with its block_id marker. You know nothing about the rest of the book and must not guess beyond this scene.
> - Your job: list every distinct referent that is present or directly acting/speaking in THIS scene, as CAST CLAIMS. A claim is an observation, not an identity decision — a later stage adjudicates identity.
> - Include non-humans when they act in the scene (an animal, a ghost or apparition treated as a character). This matters: report a dog as referent_kind_claim=animal, never as person.
> - For each claim: surface = the referring expression VERBATIM as it first appears in this scene ("Mira", "the innkeeper", "the grey mare"). surface_kind = proper_name only when the surface itself is a real name or title+name ("Mira", "Mr. Alden"); otherwise descriptor.
> - referent_kind_claim MUST be exactly one of: person, animal, nonhuman_character, place, group_reference, object, unknown. Choose from the text of this scene only. Use unknown when the scene genuinely does not tell you.
> - role_hint = a plain OBSERVED social role only (host, visitor, servant, child, innkeeper, traveller). NEVER a relationship word (friend, enemy, lover, rival) and never an invented backstory.
> - source_block_ids = every block in THIS scene where the referent appears; all ids must be markers from this scene. quote = one verbatim sentence-or-shorter span from a listed block that contains the surface.
> - Do NOT output any id fields for the claims (no cast_claim_id, no entity ids) — ids are assigned by the pipeline, not by you.
> - First-person narrator: if the narrator is only ever "I" in this scene, emit surface "I", surface_kind descriptor; if the scene names the narrator, use that name as the surface with surface_kind proper_name.
> - Required JSON shape: {"scene_id": "<copy from input>", "cast_claims": [{"surface": "...", "surface_kind": "proper_name|descriptor", "referent_kind_claim": "person|animal|nonhuman_character|place|group_reference|object|unknown", "role_hint": "...", "source_block_ids": ["bk_ch01_b004"], "quote": "..."}]}
> - Example claim (book-neutral): {"surface": "the grey mare", "surface_kind": "descriptor", "referent_kind_claim": "animal", "role_hint": "mount", "source_block_ids": ["bk_ch01_b003"], "quote": "the grey mare stamped twice at the gate"}.

### literary_lexicon_v2 (B1 v2 — occurrence mentions + referent_kind_claim; no hints)

> - Prompt version: literary_lexicon_v2.
> - Return only valid JSON matching the Required JSON shape. No text outside the JSON object.
> - The user gives you one WINDOW of ACTIVE blocks plus an optional READ-ONLY tail of earlier blocks (CONTEXT_ONLY). Extract every qualifying item visible in the ACTIVE window; do not impose a count cap. You MUST NOT extract anything that lives only in the CONTEXT_ONLY tail, and every block_id you cite must be an ACTIVE-window marker.
> - This pass is EXTRACTION ONLY. You do not decide who anyone is, you do not link mentions to each other, and you never output entity ids or candidate ids. Identity is adjudicated by a later stage from your occurrences.
> - character_mentions: one row per OCCURRENCE of a referring expression that stands in for a specific character-like referent (named person, nickname, or a descriptor used as identity such as "the master", "the ruffian"). Do NOT record: bare pronouns (he/she/I/you — never extracted in this pass), plain descriptions of appearance/body parts, rooms, furniture, or objects mentioned in passing.
> - A mention that refers to a non-person acting as a character (an animal, an apparition) IS recorded, with the honest referent_kind_claim — report a dog as animal, never as person, even when the text uses a personifying expression ("madam" for a lapdog stays referent_kind_claim=animal if this window shows it is the dog).
> - Per mention: mention_id = m_<block>_<n> (stable, e.g. m_bk_ch01_b006_01); block_id (ACTIVE marker); surface VERBATIM; mention_type exactly one of name, nickname, descriptor (descriptor covers titles-used-as-identity and epithets); referent_kind_claim exactly one of person, animal, nonhuman_character, place, group_reference, object, unknown — judged from THIS window's text only, unknown when the window does not tell you; quote = one verbatim span (a full clause or sentence) from the SAME block that contains the surface AND, when present in that block, the wording that reveals what kind of thing the referent is. Never truncate the quote mid-clause.
> - glossary_candidates: unchanged from literary_lexicon_v1 rules — terms/phrases a translator must render consistently (places, titles, dialect forms, recurring objects); NOT people. Fields: surface (verbatim), block_id, note (one line, observed function only).
> - Set top-level context_only_used=true if you leaned on the tail to understand the window (advisory only); never cite a context-only block_id.
> - Required JSON shape: {"chapter_id": "...", "window_block_ids": ["bk_ch01_b001"], "character_mentions": [{"mention_id": "m_bk_ch01_b006_01", "block_id": "bk_ch01_b006", "surface": "...", "mention_type": "name|nickname|descriptor", "referent_kind_claim": "person|animal|nonhuman_character|place|group_reference|object|unknown", "quote": "..."}], "glossary_candidates": [{"surface": "...", "block_id": "bk_ch01_b004", "note": "..."}], "context_only_used": false}
> - Example (book-neutral): {"mention_id": "m_bk_ch01_b006_02", "block_id": "bk_ch01_b006", "surface": "the innkeeper", "mention_type": "descriptor", "referent_kind_claim": "person", "quote": "the innkeeper wiped the counter and nodded toward the stairs"}.

### literary_narrative_v2 (B2 v2 — turns & events with occurrence-witness endpoints)

> - Prompt version: literary_narrative_v2.
> - Return only valid JSON matching the Required JSON shape. No text outside the JSON object.
> - The user gives you one WINDOW of ACTIVE blocks, an optional READ-ONLY CONTEXT_ONLY tail, and WINDOW_MENTIONS: the list of character-mention occurrences already extracted from this same window (mention_id, block_id, surface). Extract speaker turns and relation events from the ACTIVE window only; cite only ACTIVE block_ids.
> - speaker_turns: one row per quoted dialogue turn. relation_events: one row per concrete interaction between two referents that the text narrates in this window (an order given, a greeting, an attack, an act of service). Do not invent events from description.
> - ENDPOINTS carry the evidence link. Every actor/target/speaker/addressee endpoint has: surface = the referring expression VERBATIM as used at that spot (may be a pronoun — pronoun endpoints are allowed HERE, unlike the mention pass); referent_kind_claim (same enum as the mention pass, judged from this window); mention_ref = the mention_id from WINDOW_MENTIONS that refers to the same occurrence of the same referent, or null when no listed mention corresponds (e.g. a bare pronoun); attribution_method exactly one of explicit_tag, turn_alternation, narrator_inference, vocative; confidence low|medium|high.
> - mention_ref rules: link ONLY when you are confident the endpoint and the mention pick out the same referent at the same place in the text; when unsure, use null — a null is honest, a wrong link is not. Never invent mention ids: mention_ref must be copied verbatim from WINDOW_MENTIONS or be null.
> - You never output entity ids, candidate ids, or identity guesses beyond the endpoint's own surface + kind claim. Identity is adjudicated later.
> - address_term_used: when a turn contains a vocative (a term the speaker uses TO address the listener: a name, "sir", "madam", an endearment or insult used in address), record it verbatim on that turn with the block_id. Only genuine address counts — a person merely TALKED ABOUT is not an address term. When in doubt, omit.
> - evidence_quote per turn/event: one verbatim span from the cited block containing the narrated interaction. Never truncate mid-clause.
> - Required JSON shape: {"chapter_id": "...", "window_block_ids": ["bk_ch01_b001"], "speaker_turns": [{"turn_id": "t_bk_ch01_b006_01", "block_id": "bk_ch01_b006", "speaker": {"surface": "...", "referent_kind_claim": "...", "mention_ref": "m_bk_ch01_b006_01|null", "attribution_method": "...", "confidence": "..."}, "addressee": {...same fields...}, "address_term_used": "...|null", "evidence_quote": "..."}], "relation_events": [{"event_id": "e_bk_ch01_b006_01", "block_id": "bk_ch01_b006", "event_type": "...", "actor": {...endpoint fields...}, "target": {...endpoint fields...}, "evidence_quote": "..."}]}
> - Example endpoint (book-neutral): {"surface": "she", "referent_kind_claim": "person", "mention_ref": null, "attribution_method": "turn_alternation", "confidence": "medium"}.

### literary_b2_event_semantic_audit_v2 (post-B2 event authority)

The executable book-neutral prompt is defined in
`pipeline/literary/b2_event_authority_prompts_v2.py`. This marker is distinct
from `literary_b2_event_semantic_audit_v1`; V1 bytes and replay remain frozen.

V2 keeps the existing `keep|revise|split|pending|reject` exact-cover decision
and adds one ordered assessment per effective event:

```text
directionality = one_way | reciprocal | self_directed | unknown
actuality = occurred | reported | hypothetical_or_negated | uncertain
endpoint_status = resolved | partial | pending
```

The prompt explicitly preserves genuine self-directed actions while forbidding
ordinary pairwise authority for self-directed, nonactual, endpoint-uncertain,
or direction-unknown observations. Candidate cards remain possibilities rather
than answers; no global entity, relation phase, translation, or source fact may
be invented.

### literary_digest_v2 (B3 v2 — chapter digest with embedded-frame segmentation)

> - Prompt version: literary_digest_v2.
> - Return only valid JSON matching the Required JSON shape. No text outside the JSON object.
> - You receive one full chapter with block markers plus the structured extractions for it. Produce the chapter digest: a rolling summary, relation event summary, translator-relevant fact claims, and NARRATION FRAME SEGMENTS.
> - narration_frame_segments is the load-bearing field. Segment the chapter by WHO is narrating and WHAT story-time layer the text belongs to. A chapter is usually NOT one segment: an embedded diary, a letter being read, a dream or vision, a story told aloud by another character — each is its OWN segment with its own boundaries, nested inside the surrounding narration.
> - Per segment: block_range = [first_block_id, last_block_id] (markers from this chapter; ranges may sit inside a larger surrounding segment — list BOTH the outer segment and the inner one, outer first); narrator_surface = the narrator's referring expression VERBATIM as this chapter shows it ("I", a name, "the housekeeper") — never an invented name; story_time_label exactly one of: frame_present, retrospective_past, embedded_document, dream, vision, letter, tale_told_aloud; evidence_quote = the verbatim cue that marks the segment boundary (the sentence where the diary begins, the waking line that ends a dream). These are CLAIMS — a later checker confirms them; when the text is ambiguous, still segment but choose the more conservative label and quote the ambiguity.
> - relation_event_summary and translator_relevant_facts: each row must carry pair/subject-object as SURFACES (verbatim referring expressions), the block_id(s) of the evidence, and one verbatim evidence_quote. State only what the text itself states or narrates as happening. Do NOT upgrade an inference into a statement: an order given ("Ravel, take the horse") is an event, not a declaration of a servant relationship — if you record an inferred relationship, its evidence_quote must be the inference source and the row keeps inference_basis="derived"; a row may use inference_basis="stated" ONLY when the text declares the relationship in words ("Mrs. Hale is my daughter-in-law" style).
> - rolling_summary: at most 8 sentences, chapter-level, spoiler-free with respect to later chapters you have not seen (you only know this chapter and the provided context).
> - Required JSON shape: {"chapter_id": "...", "rolling_summary": "...", "narration_frame_segments": [{"block_range": ["bk_ch01_b002", "bk_ch01_b028"], "narrator_surface": "I", "story_time_label": "frame_present|retrospective_past|embedded_document|dream|vision|letter|tale_told_aloud", "evidence_quote": "..."}], "relation_event_summary": [{"pair_surfaces": ["...", "..."], "block_ids": ["bk_ch01_b006"], "summary": "...", "evidence_quote": "..."}], "translator_relevant_facts": [{"subject_surface": "...", "predicate": "...", "object_surface": "...", "block_id": "bk_ch01_b006", "evidence_quote": "...", "inference_basis": "stated|derived"}]}
> - Example segment (book-neutral): {"block_range": ["bk_ch01_b011", "bk_ch01_b019"], "narrator_surface": "the diary hand", "story_time_label": "embedded_document", "evidence_quote": "the margin held a cramped, faded hand: I begin my account of that winter"}.

## M4f v3 executable upstream prompts (S5C Phase A)

The B1-B3 markers below supersede the stale v2 blockquotes for Builder-v3 real
execution. The v2 prompts and B0 v3 remain as design history. Active runtime
must not load B0 after the 2026-07-13 no-B0 D0 amendment.

### literary_chapter_brief_v3 (HISTORICAL - retired from active Builder-v3)

> - Prompt version: literary_chapter_brief_v3.
> Return exactly one valid JSON object and no prose outside it.
> You receive CHAPTER_BLOCKS for one complete chapter in source order. Analyze only those blocks. Do not use or invent information from another chapter.
> Produce a chapter-level structural brief. CAST CLAIMS are untrusted observations, never identity decisions. Do not merge surfaces and do not emit any id, entity, anchor offset, status, confidence, or candidate field.
> scenes_party_size MUST exact-cover every non-heading block with ordered, non-overlapping contiguous block ranges. Never emit a scene for a heading or structural-only block, and both endpoints of every block_range must be non-heading source block ids. For a one-block scene, repeat the same block id in both positions. Split a scene when the co-present participant set changes materially; participants are verbatim surfaces concurrently present in that scene, and co_present_count is the number of concurrently present referents, not the total cast in the chapter.
> Each cast claim is one source-grounded kind/role observation. Do not output scene_range: code derives it from the uniquely located evidence block and the exact-cover scene partition. source_block_ids must contain exactly the one block from which evidence_quote is copied. surface and anchor_text must be the same exact verbatim source string; never reinterpret possession, title, capitalization, or referent in surface. evidence_quote is one contiguous verbatim span that contains anchor_text and supports the kind/role claim. Never join text from multiple blocks, insert ellipses, or paraphrase inside evidence_quote. Use occurrence_hint only when the same anchor_text appears more than once inside that one evidence_quote.
> surface_kind is proper_name or descriptor. referent_kind_claim is person, animal, nonhuman_character, place, group_reference, object, or unknown. role_hint is an observed role only, never a relationship or hidden identity. Use unknown rather than guessing.
> setting.time_frame_hint is frame_present, past_recollection, or unclear. setting.scene_shape is single_scene_one_location, few_scenes, or many_scenes_or_travel. neutral_premise is a short source-grounded gist and is not evidence for identity or relationships.
> Required JSON shape: {"chapter_id":"...","cast_claims":[{"surface":"...","surface_kind":"proper_name|descriptor","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","role_hint":"...","source_block_ids":["bk_ch01_b002"],"anchor_text":"...","evidence_quote":"...","occurrence_hint":1}],"setting":{"place":"...","time_frame_hint":"frame_present|past_recollection|unclear","scene_shape":"single_scene_one_location|few_scenes|many_scenes_or_travel"},"scenes_party_size":[{"block_range":["bk_ch01_b001","bk_ch01_b004"],"co_present_count":2,"participants":["..."]}],"neutral_premise":"..."}.
> Omit occurrence_hint when unnecessary. Empty cast_claims is allowed only when the chapter has no character-like referent. Every other top-level field is required.

### literary_lexicon_v3

> - Prompt version: literary_lexicon_v3.
> Return exactly one valid JSON object and no prose outside it.
> You receive ACTIVE_WINDOW_BLOCKS plus CONTEXT_ONLY_TAIL. Extract only occurrences whose block_id is active. Context-only text may clarify a surface but must never be cited or extracted as an active item.
> This is occurrence extraction, not identity resolution. Do not emit mention_id, entity_id, candidate ids, status, confidence, source offsets, or anchors. Code locates spans and mints ids after validation.
> Emit one character_mentions row per qualifying occurrence. Pronouns are handled by Narrative endpoints, not here. surface and anchor_text are verbatim; evidence_quote is a larger verbatim span in the same active block and contains anchor_text. Never cut the clause that reveals whether a personified term refers to an animal or other non-person. occurrence_hint is optional and only disambiguates repeated anchor_text inside evidence_quote.
> mention_type is name, nickname, or descriptor. referent_kind_claim is person, animal, nonhuman_character, place, group_reference, object, or unknown.
> glossary_candidates are translator consistency items, not people. source_term is verbatim. proposed_target_vi may be empty when no stable Vietnamese proposal is justified. category is place, object, cultural, or other. do_not_translate is a boolean. block_ids contain active block markers only.
> Required JSON shape: {"chapter_id":"...","window_block_ids":["bk_ch01_b001"],"context_only_used":false,"character_mentions":[{"surface":"...","mention_type":"name|nickname|descriptor","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","anchor_text":"...","evidence_quote":"...","occurrence_hint":1,"block_id":"bk_ch01_b001"}],"glossary_candidates":[{"source_term":"...","proposed_target_vi":"...","category":"place|object|cultural|other","do_not_translate":false,"block_ids":["bk_ch01_b001"]}]}.
> Omit occurrence_hint when unnecessary. Do not omit any top-level field; empty item lists are valid.

### literary_narrative_v3

> - Prompt version: literary_narrative_v3.
> Return exactly one valid JSON object and no prose outside it.
> You receive ACTIVE_WINDOW_BLOCKS, CONTEXT_ONLY_TAIL, and WINDOW_MENTIONS. Extract speaker turns and concrete relation events from active blocks only. Context-only rows may assist interpretation but must not be cited as active evidence.
> Do not emit turn_id, event_id, endpoint_id, address_occurrence_id, entity ids, status, confidence, source offsets, anchors, or candidate ids. Code locates spans, validates references, and mints ids.
> Every endpoint carries its own verbatim anchor_text and evidence_quote. anchor_text is the exact source expression that refers to that endpoint (a name, pronoun, title, descriptor, or vocative), never a resolved name or an action phrase; code fills surface from anchor_text. evidence_quote is one contiguous verbatim span containing anchor_text; never insert ellipses or paraphrase. If no referring expression exists in the active block, omit that event or use null for an optional addressee rather than inventing an anchor. reference_scope is individual, group, narrator, reader, or unknown and describes discourse cardinality/voice ONLY. Never copy ontology labels such as person, animal, nonhuman_character, place, group_reference, or object into reference_scope; use unknown when scope is unclear. referent_kind_claim is person, animal, nonhuman_character, place, group_reference, object, or unknown and describes ontology ONLY. mention_ref copies a same-window WINDOW_MENTIONS id only when it is the exact same textual occurrence (same anchor span); otherwise null. A pronoun or descriptor without that exact B1 occurrence uses null even if you infer its referent. attribution_method is explicit_tag, turn_alternation, nearby_context, narrator_inference, or vocative.
> speaker_turns contain actual spoken or written utterances, not silent gestures; nonverbal responses belong only in relation_events. Include zero or more genuine address_terms, register_cue, and active block_id. register_cue is neutral, intimate, deferential, paternal, hostile, or mocking. Each address term has verbatim anchor/evidence and addressee_ref speaker or addressee. A person merely discussed is not an address term.
> relation_events are concrete narrated interactions. event_type is a concise source-grounded action phrase; code normalizes its formatting to lower_snake_case. Never use relationship-phase labels such as friend, enemy, hostile, strained, rival, dependent, or reconciled.
> Required JSON shape: {"chapter_id":"...","window_block_ids":["bk_ch01_b001"],"context_only_used":false,"speaker_turns":[{"speaker":{"reference_scope":"individual|group|narrator|reader|unknown","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","mention_ref":null,"attribution_method":"explicit_tag|turn_alternation|nearby_context|narrator_inference|vocative","anchor_text":"...","evidence_quote":"...","occurrence_hint":1},"addressee":null,"utterance_quote":"...","address_terms":[{"anchor_text":"...","evidence_quote":"...","occurrence_hint":1,"addressee_ref":"speaker|addressee"}],"register_cue":"neutral|intimate|deferential|paternal|hostile|mocking","block_id":"bk_ch01_b001"}],"relation_events":[{"actor":{"reference_scope":"individual|group|narrator|reader|unknown","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","mention_ref":null,"attribution_method":"explicit_tag|turn_alternation|nearby_context|narrator_inference|vocative","anchor_text":"...","evidence_quote":"..."},"target":{"reference_scope":"individual|group|narrator|reader|unknown","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","mention_ref":null,"attribution_method":"explicit_tag|turn_alternation|nearby_context|narrator_inference|vocative","anchor_text":"...","evidence_quote":"..."},"event_type":"...","evidence_quote":"...","block_id":"bk_ch01_b001"}]}.
> Omit occurrence_hint when unnecessary. Do not omit any top-level field; empty item lists are valid.

### literary_digest_v3

> - Prompt version: literary_digest_v3.
> Return exactly one valid JSON object and no prose outside it.
> You receive CHAPTER_BLOCKS, an occurrence roster, concrete B2 events with endpoint ids, and up to two prior rolling summaries. Every semantic claim must cite current-chapter source blocks, occurrence ids, endpoint ids, or event ids supplied in the request.
> Do not emit entity ids, pair surfaces, final pair ids, phase labels, address policy, active/verified status, source offsets, anchors, or candidate ids. Code locates frame boundaries and mints segment ids after validation.
> chapter_rolling_summary is at most eight source-grounded sentences.
> narration_frame_segments form one complete nested tree over every non-heading chapter span. local_segment_key is request-local. parent_local_key is another local key or null. frame_kind is primary_narration, embedded_document, letter, diary, dream, vision, tale_told_aloud, or quoted_report. story_time_label is frame_present, retrospective_past, or anterior_past. narrator_ref copies an occurrence id or is null. status is proposed or uncertain. start_boundary/end_boundary are null at block edges or {anchor_text,evidence_quote,occurrence_hint?} for an in-block boundary. evidence_quote is verbatim source support.
> relation_observations copy one event_id and exactly its ordered actor,target endpoint_refs. observed_valence_hint is positive, negative, mixed, or unclear. transition_hint is absent or {trigger_event_id,note}; it is not a phase label.
> character_state_changes use subject_ref/trigger_ref from supplied occurrences/events. attribute is social_status, alias_or_title, life_status, or residence.
> unresolved_threads kind is mystery, pending_transition, or question. translator_relevant_facts fact_type is narrator, register, speech_style, status, or setting; inference_basis is stated or derived. motifs are source-grounded observations, not identity decisions.
> Required JSON shape: {"chapter_id":"...","chapter_rolling_summary":"...","narration_frame_segments":[{"local_segment_key":"...","parent_local_key":null,"narrator_surface":"...","narrator_ref":null,"frame_kind":"primary_narration|embedded_document|letter|diary|dream|vision|tale_told_aloud|quoted_report","story_time_label":"frame_present|retrospective_past|anterior_past","block_range":["bk_ch01_b001","bk_ch01_b010"],"start_boundary":null,"end_boundary":null,"status":"proposed|uncertain","evidence_quote":"..."}],"relation_observations":[{"event_id":"...","endpoint_refs":["actor_endpoint_id","target_endpoint_id"],"observed_valence_hint":"positive|negative|mixed|unclear","block_id":"bk_ch01_b001","evidence_quote":"...","transition_hint":{"trigger_event_id":"...","note":"..."}}],"character_state_changes":[{"subject_ref":"...","attribute":"social_status|alias_or_title|life_status|residence","from_value":"...","to_value":"...","trigger_ref":"...","evidence_quote":"..."}],"unresolved_threads":[{"thread_local_id":"...","description":"...","opened_block":"bk_ch01_b001","kind":"mystery|pending_transition|question","subject_refs":["..."]}],"translator_relevant_facts":[{"fact_type":"narrator|register|speech_style|status|setting","fact":"...","block_evidence":["bk_ch01_b001"],"inference_basis":"stated|derived","subject_ref":"...","event_ids":["..."]}],"motifs":[{"note":"...","block_ids":["bk_ch01_b001"],"subject_refs":["..."]}]}.
> Optional nested fields may be omitted only where the typed schema declares them optional. All eight top-level fields are required; empty lists are valid.

### literary_identity_retrieval_slice_v1

> - Prompt version: literary_identity_retrieval_slice_v1.
> Return exactly one valid JSON object and no prose outside it.
> This is read-only candidate retrieval for an identity experiment. You receive TARGETS, a complete GLOBAL_LIGHT_ROSTER, source-grounded target cards, and an explicitly untrusted frame proposal. You do not decide final identity and you do not create ids.
> For every target, select OTHER occurrence ids from GLOBAL_LIGHT_ROSTER that need detailed comparison before deciding whether they refer to the same real referent. Candidate selection may use different surfaces, epithets, pronouns, local/frame context, and source evidence. Do not treat any prior hint, canonical surface, or frame proposal as authoritative.
> status is selected when you can identify a comparison set, including an empty set for an unmatched/new referent. Use unknown only when the supplied context is too ambiguous to select candidates safely. evidence_refs copy ground_item_ids visible in this request.
> Required JSON shape: {"targets":[{"target_occurrence_id":"...","candidate_occurrence_ids":["..."],"status":"selected|unknown","evidence_refs":["..."]}]}.
> Return every target exactly once. Do not emit entity, binding, decision, witness, active, candidate-group, or model-invented ids.

### literary_identity_proposal_slice_v1

> - Prompt version: literary_identity_proposal_slice_v1.
> Return exactly one valid JSON object and no prose outside it.
> This is a read-only identity proposal experiment. You receive each TARGET plus every detailed CANDIDATE_CARD selected for it, source-grounded evidence items, and an untrusted or unknown frame proposal. None of those observations is authority. Decide from source evidence only.
> For a proposed row, same_referent_occurrence_ids includes the target exactly once and contains only occurrences in that target's selection universe. different_referent_occurrence_ids is disjoint and also uses only visible occurrence ids. Use unknown rather than forcing a merge or split. referent_kind is person, animal, nonhuman_character, place, group_reference, object, or unknown. canonical_surface_guess is advisory display text only and never an id or merge key.
> evidence_refs copy ground_item_ids visible in this request. For unknown, both occurrence lists and evidence_refs are empty.
> Required JSON shape: {"proposals":[{"target_occurrence_id":"...","status":"proposed|unknown","same_referent_occurrence_ids":["..."],"different_referent_occurrence_ids":["..."],"referent_kind":"person|animal|nonhuman_character|place|group_reference|object|unknown","canonical_surface_guess":"...","evidence_refs":["..."]}]}.
> Return every target exactly once. Do not emit entity, binding, decision, witness, active, verified, or model-invented ids. This response is never a production state mutation.

### literary_frame_tree_adjudicator_v1 (S5B frame-tree adjudicator)

> - Prompt version: literary_frame_tree_adjudicator_v1.
> - You are the FRAME-TREE ADJUDICATOR for an autonomous literary translation pipeline. Return one JSON object only. Do not add prose outside JSON.
> - You receive every source block in ONE bounded unit, in source order, plus an OCCURRENCE_CHOICES catalog. Reconstruct the narration-frame tree from this source evidence. Do not assume that a prior extractor's frame labels are correct; no prior semantic answer is authoritative.
> - A frame is a contiguous source interval with one narration mode and story-time layer. Embedded documents, letters, diaries, dreams, visions, tales told aloud, and quoted reports are child frames inside their surrounding narration. Multiple top-level intervals are allowed; code attaches them to one synthetic root that you do not output.
> - response_status MUST be proposed or uncertain. Use uncertain only when you cannot support one complete tree from the source. When uncertain, segments MUST be an empty list. Do not return a partial tree.
> - For a proposed tree, segments MUST be non-empty and together cover all non-heading source blocks. local_segment_key values are request-local labels only. parent_local_key is another local key or null for a top-level segment. Do not output a synthetic root.
> - frame_kind MUST be exactly one of: primary_narration, embedded_document, letter, diary, dream, vision, tale_told_aloud, quoted_report.
> - story_time_label MUST be exactly one of: frame_present, retrospective_past, anterior_past.
> - narrator_occurrence_ref MUST be copied from OCCURRENCE_CHOICES or be null. It is an occurrence witness, not an entity id. An outer-frame introduction may identify the narrator of a later child frame.
> - block_range MUST copy two source block ids from this unit. Use start_boundary or end_boundary only when the interval starts or ends inside its boundary block. A boundary is {anchor_text, evidence_quote, occurrence_hint?}; copy text verbatim. Use null when the frame begins at character 0 of the first block or ends at the end of the last block.
> - Every segment needs at least one evidence row {block_id, evidence_quote}. Copy quotes verbatim from the declared block. Evidence supports the frame judgment; boundaries define the interval.
> - Do not output entity_id, segment_id, frame_id, binding_id, decision_id, witness_id, verified, corroborated, or active. Do not infer character identity.
> - Required JSON shape: {"response_status":"proposed|uncertain","segments":[{"local_segment_key":"...","parent_local_key":null,"block_range":["bk_ch01_b001","bk_ch01_b010"],"start_boundary":null,"end_boundary":null,"frame_kind":"primary_narration|embedded_document|letter|diary|dream|vision|tale_told_aloud|quoted_report","story_time_label":"frame_present|retrospective_past|anterior_past","narrator_occurrence_ref":null,"narrator_surface":"...","evidence":[{"block_id":"bk_ch01_b001","evidence_quote":"..."}]}]}.

### literary_frame_tree_checker_v1 (S5B independent frame-tree checker)

> - Prompt version: literary_frame_tree_checker_v1.
> - You are the INDEPENDENT FRAME-TREE CHECKER for an autonomous literary translation pipeline. Return one JSON object only. Do not add prose outside JSON.
> - Derive the complete narration-frame tree directly from the supplied SOURCE_BLOCKS and OCCURRENCE_CHOICES. You are not reviewing another answer: no adjudicator response, prior frame labels, local segment keys, or semantic signature is provided. Do not guess what another model returned.
> - A frame is a contiguous source interval with one narration mode and story-time layer. Embedded documents, letters, diaries, dreams, visions, tales told aloud, and quoted reports are child frames inside their surrounding narration. Multiple top-level intervals are allowed; code attaches them to one synthetic root that you do not output.
> - response_status MUST be proposed or uncertain. Use uncertain only when you cannot support one complete tree from the source. When uncertain, segments MUST be an empty list. Do not return a partial tree.
> - For a proposed tree, segments MUST be non-empty and together cover all non-heading source blocks. local_segment_key values are request-local labels only. parent_local_key is another local key or null for a top-level segment. Do not output a synthetic root.
> - frame_kind MUST be exactly one of: primary_narration, embedded_document, letter, diary, dream, vision, tale_told_aloud, quoted_report.
> - story_time_label MUST be exactly one of: frame_present, retrospective_past, anterior_past.
> - narrator_occurrence_ref MUST be copied from OCCURRENCE_CHOICES or be null. It is an occurrence witness, not an entity id. An outer-frame introduction may identify the narrator of a later child frame.
> - block_range MUST copy two source block ids from this unit. Use start_boundary or end_boundary only when the interval starts or ends inside its boundary block. A boundary is {anchor_text, evidence_quote, occurrence_hint?}; copy text verbatim. Use null when the frame begins at character 0 of the first block or ends at the end of the last block.
> - Every segment needs at least one evidence row {block_id, evidence_quote}. Copy quotes verbatim from the declared block. Evidence supports the frame judgment; boundaries define the interval.
> - Do not output entity_id, segment_id, frame_id, binding_id, decision_id, witness_id, verified, corroborated, or active. Do not infer character identity.
> - Required JSON shape: {"response_status":"proposed|uncertain","segments":[{"local_segment_key":"...","parent_local_key":null,"block_range":["bk_ch01_b001","bk_ch01_b010"],"start_boundary":null,"end_boundary":null,"frame_kind":"primary_narration|embedded_document|letter|diary|dream|vision|tale_told_aloud|quoted_report","story_time_label":"frame_present|retrospective_past|anterior_past","narrator_occurrence_ref":null,"narrator_surface":"...","evidence":[{"block_id":"bk_ch01_b001","evidence_quote":"..."}]}]}.
### literary_phase_segment_draft_v1 (draft M3 phase/fact - occurrence-grounded, every event accounted)

> - Prompt version: literary_phase_segment_draft_v1.
> Return exactly one valid JSON object and no prose outside it.
> You segment ONE character pair's relationship into ordered phases and extract directed relationship facts, working only from the supplied evidence. The pair header gives two entity ids, A and B, and an EVENTS list in source order. Each event carries an event_id, the ordered actor and target endpoints that tie it to A and B, a block_id, and a verbatim evidence quote. A compact PRIOR_TIMELINE may be supplied when replaying; treat it as context to extend, never as authority. Never read any other pair, prior hint, provisional label, or canonical answer.
> ACCOUNT FOR EVERY EVENT. considered_event_ids MUST set-equal the event_ids supplied in EVENTS. event_dispositions MUST hold exactly one row per supplied event_id, each row carrying one or more outcomes from EXACTLY this set [phase_support, fact_support, no_change, blocked]: phase_support when the event opens or closes a phase; fact_support when it grounds a relationship fact; no_change when it is a real interaction that shifts nothing; blocked when the event cannot be used (for example, an endpoint is too ambiguous to rely on). Give a short reason whenever an outcome is blocked. No supplied event may be dropped or merged away.
> PHASES: relation_phases is a list of NON-OVERLAPPING phases in block order, each a half-open interval [valid_from_block, valid_until_block). phase_label is EXACTLY one of [allied, friendly, neutral, strained, hostile, estranged, dependent, reconciled]. For every phase except the last, valid_until_block equals the valid_from_block of the next phase and status is "closed"; the last, unclosed phase has valid_until_block null and status "open". Adjacent phases may share a boundary block, but intervals must never overlap. trigger_event_id copies the supplied event_id whose interaction opens the phase, and trigger_evidence_quote copies that event's evidence verbatim. Open a NEW phase only when a trigger shows a strong, durable change in language or action - a single remark or a momentary mood is an event, not a phase. Prefer FEWER phases when the evidence is weak, and never invent a phase without a triggering event.
> RELATION_FACTS: directed factual relationships between A and B that the evidence states or clearly shows. For each fact: subject_ref and object_ref are the two supplied ids A and B and must differ; predicate_code is EXACTLY one of [parent_of, child_of, spouse_of, sibling_of, daughter_in_law_of, son_in_law_of, father_in_law_of, mother_in_law_of, grandparent_of, grandchild_of, cousin_of, servant_of, master_of, landlord_of, tenant_of, guest_of, neighbor_of, guardian_of, ward_of, other]; source_event_id copies one supplied event_id and evidence_quote copies that event's quote verbatim; inference_basis is "stated" when the evidence names the relationship in words and "derived" when you conclude it from what is shown. Never label a derived fact as stated. "subject predicate object" means the subject IS the predicate of the object; direction matters. Use "other" ONLY when nothing in the set fits, and then fill predicate_note. Do not derive a fact from a title or an honorific alone; every fact needs a supporting event and a verbatim quote. Output an empty list when nothing is supported.
> Do not emit entity ids, phase ids, fact ids, valence, or any id the request did not supply. Copy A, B, and event_ids exactly as given; code assigns ids and derives valence after validation.
> Required JSON shape: {"pair":{"a_entity_id":"...","b_entity_id":"..."},"considered_event_ids":["..."],"event_dispositions":[{"event_id":"...","outcomes":["phase_support|fact_support|no_change|blocked"],"reason":"..."}],"relation_phases":[{"phase_label":"allied|friendly|neutral|strained|hostile|estranged|dependent|reconciled","valid_from_block":"bk_ch01_b001","valid_until_block":null,"status":"open|closed","trigger_event_id":"...","trigger_evidence_quote":"..."}],"relation_facts":[{"subject_ref":"...","predicate_code":"parent_of|child_of|spouse_of|sibling_of|daughter_in_law_of|son_in_law_of|father_in_law_of|mother_in_law_of|grandparent_of|grandchild_of|cousin_of|servant_of|master_of|landlord_of|tenant_of|guest_of|neighbor_of|guardian_of|ward_of|other","object_ref":"...","source_event_id":"...","evidence_quote":"...","inference_basis":"stated|derived","predicate_note":"..."}]}.
> All five top-level keys are required. considered_event_ids and event_dispositions must be present even when relation_phases and relation_facts are empty. Omit reason and predicate_note when they are not needed.

### literary_chapter_orient_v3 (B0 recall orientation for stable registry)

> - Prompt version: literary_chapter_orient_v3.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Each block has a block_id, block_type, and source text. Read the whole supplied chapter once. Do not use knowledge from another chapter or book.
> - This is an orientation and recall pass, not an identity authority. Do not merge surfaces, bind references, create aliases, infer relationship phases, partition scenes, assign persistent ids, or predict future facts.
> - gist is at most two concise sentences about observable chapter movement. Preserve uncertainty.
> - narrator_hypotheses are limited hypotheses. surface is a verbatim expression when explicit, otherwise null; note explains the hypothesis; block_ids cite only supplied support blocks. These rows never establish narrator identity.
> - salient_registry_checklist is a recall floor, never an answer key or discovery ceiling. stable_named_referent covers salient stable names for people, animals, nonhuman characters, groups, places, institutions, and individualized named objects. important_unnamed_referent covers an individualized unnamed being or object that acts, speaks, causes a material interaction, recurs, or otherwise matters. translation_sensitive_term covers important cultural, technical, or institutional terminology.
> - For an unnamed referent, copy the most specific verbatim referential noun phrase available in the supplied chapter. Do not use a bare pronoun or reusable form of address when a more descriptive supplied phrase exists. This phrase is only a recall handle and never a proposed global name.
> - Every checklist surface must occur verbatim in its declared block_id. Code locates it and mints ids. Do not output a quote, span, offset, scene range, kind, identity, alias, event, relation, confidence, or persistent id.
> - Exclude incidental generic nouns and ordinary unindividualized creatures or objects. Empty lists are valid when the supplied chapter supports none. Never invent a row to appear complete.
> - Required JSON shape: {"gist":"...","narrator_hypotheses":[{"surface":null,"note":"...","block_ids":["chapter_block_id"]}],"salient_registry_checklist":[{"surface":"...","block_id":"chapter_block_id","checklist_class":"stable_named_referent|important_unnamed_referent|translation_sensitive_term","importance_note":"..."}]}

### literary_stable_registry_delta_v3_1 (B1 stable-name and term delta)

> - Prompt version: literary_stable_registry_delta_v3_1.
> - Return exactly one valid JSON object with EXACTLY the three top-level lists in the Required JSON shape. Do not add prose outside the JSON object.
> - You receive B0_GIST, B0_CHECKLIST_ROWS_FOR_ACTIVE_BLOCKS, ACTIVE_WINDOW_BLOCKS, an optional read-only CONTEXT_ONLY_PRECEDING_TAIL, the latest WORKING_REGISTRY_REVISION_HASH, code-prejoined SURFACE_CANDIDATE_PACKETS, optional bounded UNMATCHED_RECENCY_CARDS, RELEVANT_OPEN_TICKETS, and selection/cap manifests. A later window sees the latest validated provisional revision only through these bounded packets.
> - Candidate packets are mechanical retrieval hints, not identity answers. The packet says that a source surface overlaps one or more registry surfaces. Candidate count, candidate order, exact spelling, a shared surname, or exactly one candidate never proves identity. Fresh contradictory kind, title, or description may justify a distinct proposal plus a ticket.
> - Build a stable registry DELTA, not an occurrence inventory. If a supplied entity or glossary candidate already represents the item and fresh evidence adds no stable conflict, emit nothing for it. Do not repeat known profiles, support blocks, ordinary appearances, or open tickets.
> - Emit semantic rows only from ACTIVE_WINDOW_BLOCKS. The preceding tail is read-only and cannot be sole support. Copy each surface verbatim and copy only supplied active block ids. Code locates spans, computes source order, mints ids, hashes rows, and publishes validated decisions.
> - new_entities contains only newly recognized stable named referents. Eligible name_class values are proper_name, stable_nickname, and title_plus_name. A stable name identifies one referent beyond a single local utterance. Bare pronouns, forms of address, reusable roles, descriptions, kinship words, epithets, insults, and other block-dependent references are not stable B1 entities merely because they refer to someone locally.
> - Named places, institutions, groups, and individualized named objects are entities. short_description is compact and stable: kind plus a distinguishing role, trait, or identity-defining fact. Exclude temporary action, mood, evolving relationship, scene summary, and unsupported biography.
> - If fresh evidence supports a distinct referent with a spelling already present among candidates, preserve the new provisional entity and open same_name_collision. If a new stable name may belong to an existing entity, preserve recall as needed and open possible_alias. Never merge, bind, or author an alias.
> - new_glossary_items contains only translation-sensitive cultural, technical, object, place, or institution terms. Exclude generic common nouns. Do not propose a translation, variant, or do-not-translate policy. Do not emit the same surface as both entity and glossary item.
> - tickets report genuine uncertainty or conflict and never mutate a row. Model ticket types are same_name_collision, possible_alias, important_unnamed_referent, kind_conflict, profile_conflict, importance_review, surface_class_review, and glossary_collision. Candidate ids must be copied from supplied packets. Kind and proposed description are required fields but may be null unless the requested resolution needs them.
> - important_unnamed_referent preserves an individualized important referent without pretending its contextual descriptor is a stable name. Its ticket surface must copy the specific supplied descriptor so code can prove checklist coverage; retaining this evidence surface never promotes it to a global alias. surface_class_review is allowed when a contextual expression may matter but must not become a global row. If an item may be important, open importance_review rather than silently dropping it.
> - Code-owned candidate_overflow, unlocatable_surface, missing_salient_surface, and alias_scope_review tickets cannot be authored by you. When a packet says candidate_overflow, do not make an identity attachment or duplicate proposal for that surface; leave code to open the safety ticket.
> - When TARGETED_CHECKLIST_ROWS are supplied, this is a target-only recall call. Re-read each cited active block, but emit rows only for those targeted checklist rows; all other source content, candidates, and context are read-only. Do not repeat an unrelated known or new entity, glossary item, or ticket. Checklist inclusion is a recall instruction, not evidence of kind, identity, importance, or ownership.
> - In target-only mode, a stable_named_referent may produce a matching new entity or a matching review ticket, but never important_unnamed_referent. An important_unnamed_referent may produce its matching typed ticket and must not be promoted into a stable name merely to satisfy recall. A translation_sensitive_term may produce a matching glossary row or matching review/collision ticket. Every targeted output must copy the supplied target surface and block so code can attribute it mechanically.
> - Do not output aliases, local bindings, occurrences, speakers, addressees, turns, events, relations, phases, states, narrator decisions, tool calls, retrieval requests, confidence, evidence_quote, anchor_text, occurrence_hint, offsets, spans, commit instructions, or any unsupplied persistent id.
> - Three empty lists are valid only after reading the full active window and every supplied targeted checklist row, and only when there is no new stable row or genuine exception.
> - Required JSON shape: {"new_entities":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|unknown","short_description":"...","source_block_ids":["chapter_block_id"]}],"new_glossary_items":[{"surface":"...","category_claim":"cultural_term|technical_term|place_name|object_name|institution_name|other","short_description":"...","source_block_ids":["chapter_block_id"]}],"tickets":[{"ticket_type":"same_name_collision|possible_alias|important_unnamed_referent|kind_conflict|profile_conflict|importance_review|surface_class_review|glossary_collision","surface":"...","source_block_ids":["chapter_block_id"],"candidate_entity_ids":[],"candidate_glossary_ids":[],"referent_kind_claim":null,"proposed_short_description":null,"reason":"..."}]}

### literary_registry_exception_audit_v2 (chapter exception Auditor)

> - Prompt version: literary_registry_exception_audit_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive B0 orientation and checklist, a compact full-chapter roster, one bounded connected component of owned tickets, the referenced provisional rows and candidate cards, exact cited SOURCE_BLOCKS with bounded preceding context, the WORKING_REGISTRY_REVISION_HASH, and closed action/cap manifests. You do not receive unrelated full profiles, an occurrence inventory, relation/event history, another model's answer key, or a tool loop.
> - Audit only the supplied ticket component. ticket_dispositions must exact-cover every owned ticket id once. Copy supplied ids exactly; foreign, duplicate, omitted, or invented ids are forbidden.
> - confirm_distinct_entity keeps one supplied provisional subject as a separate stable referent. merge_as_alias merges one supplied provisional source entity into one supplied target only when the evidence establishes shared identity; spelling or candidate uniqueness alone is insufficient. The merged surface is still routed independently through the code alias-scope gate.
> - create_unnamed_entity is for an individualized important referent without a stable name. It requires a non-unknown kind and stable description. The resulting entity has zero global aliases unless a separately validated stable name later passes the alias gate.
> - promote_global_alias is allowed only for a supplied proper name, stable nickname, or title-plus-name supported as co-referential. Forms of address, reusable roles, descriptions, kinship words, epithets, insults, and local references are not global aliases even when they are the only available handle. Every promotion and merge still passes the independent commit-time alias gate.
> - confirm_distinct_glossary keeps a supplied translation-sensitive term; merge_glossary merges two supplied glossary rows only when they represent the same term. revise_profile changes only a supplied target involved in a kind/profile conflict. defer_to_b2 preserves a contextual reference for later local resolution without creating an entity, alias, or binding. reject_noise removes an unsupported or generic proposal. remain_pending carries uncertainty forward.
> - The compact roster supplies chapter-wide collision awareness, but it is not authority. Never merge or revise unrelated confirmed rows. Do not infer turns, events, relations, phases, address policy, narrator identity, translation, or future facts.
> - Action-dependent ids and fields must follow the supplied action manifest. Use null for inapplicable fields. You cannot invent a surface, source block, candidate, profile, id, or commit instruction. Empty ticket_dispositions is valid only when no ticket is supplied; normally the Auditor is not called for an empty manifest.
> - Required JSON shape: {"ticket_dispositions":[{"ticket_id":"ticket_id_supplied","action":"confirm_distinct_entity|merge_as_alias|create_unnamed_entity|promote_global_alias|confirm_distinct_glossary|merge_glossary|revise_profile|defer_to_b2|reject_noise|remain_pending","source_entity_id":null,"target_entity_id":null,"source_glossary_id":null,"target_glossary_id":null,"resolved_referent_kind":null,"revised_identity_summary":null,"name_class":null,"resolution_note":"..."}]}

### literary_chapter_orient_v4 (B0 advisory chapter orientation)

> - Prompt version: literary_chapter_orient_v4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Each supplied block has a block_id, block_type, and source text. Use only this chapter. Do not rely on another chapter, another book, a registry, or outside knowledge.
> - This is a provisional orientation pass, not an identity, glossary, event, relation, or scene authority. Do not create or merge entities; classify a referent or term; bind a surface; assign an alias; partition scenes; infer relation phases; mint ids; or prescribe a later action.
> - orientation_draft is a compact, source-grounded draft of at most 300 words. It may summarize the chapter's main observable movement, apparent point of view, named or important unnamed participants, salient animals or nonhuman referents, important places, individualized objects, institutions, translation-sensitive terms, and major observable events. Preserve uncertainty. Mentioning something here never requires B1 to create or retain it.
> - narrator_hypotheses are provisional hypotheses for later stages. surface is a verbatim expression when the narrator is explicit, otherwise null. note states only the hypothesis and its uncertainty. source_block_ids cite supplied support blocks. These rows are not sent to B1 by default and never establish narrator identity.
> - attention_items are optional recall aids. Each surface is copied verbatim from every declared source block. why_noticed is at most 240 characters and states only an observable reason that the surface may deserve closer reading. It must not state or imply an entity kind, glossary category, identity, importance disposition, merge, alias, binding, or Auditor action.
> - Attention is supplementary and potentially wrong. It is neither a checklist B1 must cover nor a ceiling on B1 discovery. Empty lists are valid. Never invent an item to appear complete.
> - Do not output quotes, spans, offsets, occurrence hints, scene ranges, confidence, persistent ids, expected answers, or commit instructions. Code verifies source membership and mints immutable attention-observation ids.
> - Required JSON shape: {"orientation_draft":"...","narrator_hypotheses":[{"surface":null,"note":"...","source_block_ids":["chapter_block_id"]}],"attention_items":[{"surface":"...","source_block_ids":["chapter_block_id"],"why_noticed":"..."}]}

### literary_chapter_orient_v4_1 (B0 chapter-level orientation)

> - Prompt version: literary_chapter_orient_v4_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Each supplied block has a block_id, block_type, and source text. Use only this chapter. Do not rely on another chapter, another book, a registry, or outside knowledge.
> - This is a provisional chapter-orientation pass, not an identity, glossary, event, relation, scene, or frame authority. Do not create or merge entities; classify a referent or term; bind a surface; assign an alias; partition scenes; map a narrator across blocks; infer relation phases; mint ids; or prescribe a later action.
> - orientation_draft is a compact, source-grounded draft of at most 220 words. Summarize the chapter's main observable movement, dominant narrative perspective, named or important unnamed participants, salient animals or nonhuman referents, important places, institutions, translation-sensitive terms, and only the major events needed to understand why a referent may matter. Preserve uncertainty. Mentioning something never requires B1 to create or retain it.
> - narrative_context describes only the dominant chapter-level narrative mode. mode is first_person, third_person_external, second_person, mixed_or_nested, documentary_or_epistolary, or uncertain. note is at most 240 characters and explains the dominant mode or uncertainty without identifying an author as an in-story entity. support_block_ids cite one or more representative source blocks; they are evidence samples, not a block-range assignment or an exact cover of the chapter. For mixed or nested narration, summarize the condition rather than mapping every transition.
> - attention_items are compact, optional recall aids. Prefer salient names, stable identity-bearing expressions, important individualized unnamed referents, important places or institutions, and translation-sensitive terms. Exclude ordinary one-off props, generic common nouns, bare pronouns, reusable forms of address, and incidental scenery unless the source clearly individualizes them or makes them materially important to the chapter.
> - Each attention surface is copied verbatim. source_block_ids list only supplied blocks in which that exact surface occurs. why_noticed is at most 240 characters and states only an observable reason that the surface may deserve closer reading. It must not state or imply an entity kind, glossary category, identity, importance disposition, merge, alias, binding, or Auditor action.
> - Attention is supplementary and potentially wrong. It is neither an exhaustive entity inventory, a checklist B1 must cover, nor a ceiling on B1 discovery. Empty attention_items is valid. Never invent an item to appear complete.
> - Do not output quotes, spans, offsets, occurrence hints, scene ranges, narrator block ranges, confidence, persistent ids, expected answers, or commit instructions. Code verifies source membership, drops only invalid advisory attention rows with an audit counter, and mints immutable attention-observation ids.
> - Required JSON shape: {"orientation_draft":"...","narrative_context":{"mode":"first_person|third_person_external|second_person|mixed_or_nested|documentary_or_epistolary|uncertain","note":"...","support_block_ids":["chapter_block_id"]},"attention_items":[{"surface":"...","source_block_ids":["chapter_block_id"],"why_noticed":"..."}]}

### literary_chapter_entity_inventory_exp_v1 (B0 full-chapter entity inventory experiment)

> - Prompt version: literary_chapter_entity_inventory_exp_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Use only this chapter. Do not rely on another chapter, another book, a registry, an answer key, or outside knowledge.
> - This is an isolated entity-inventory experiment. Spend the response on finding stable translation-memory candidates, not on a chapter summary, narrator analysis, scenes, events, relations, or translation choices.
> - entity_candidates contains one row per hypothesized stable referent that would materially help later literary translation: named people, important individualized unnamed people or animals, nonhuman characters, groups, places, institutions, or individualized named objects. Exclude bare pronouns, reusable forms of address, scene-local roles, rhetorical beings, generic groups, ordinary props, incidental animals, and background geography unless the chapter clearly individualizes them or makes their continuity materially important.
> - canonical_surface is one verbatim source surface suitable for identifying the candidate in this chapter. source_surfaces are distinct verbatim expressions that you judge refer to that same candidate. Group surfaces only when the chapter itself supports shared identity. If identity is not safe to group, preserve the expression under unresolved_referents instead of guessing.
> - source_surfaces are references observed in this chapter, not global aliases. A form of address, role phrase, description, epithet, or insult may appear as a source reference for an important unnamed referent, but that does not make it a stable name.
> - referent_kind_claim is person, animal, nonhuman_character, group_reference, place, object, institution, or unknown. name_class is proper_name, stable_nickname, title_plus_name, or null. Use null for an unnamed referent or when no stable name class is established.
> - identity_summary is one concise, stable description useful for distinguishing the referent. Do not include temporary mood, current action, exact age, evolving relation state, a scene summary, or facts not supported by this chapter.
> - glossary_candidates contains only translation-sensitive cultural, regional, technical, object, place, or institution terms whose local meaning should persist. Do not include generic common nouns or propose a target translation.
> - unresolved_referents preserves an important expression when the chapter establishes salience but not safe stable identity. issue is identity_ambiguous, unnamed_but_salient, or surface_scope_uncertain.
> - Copy every surface verbatim. Do not output block ids, quotes, spans, offsets, confidence, entity ids, aliases, bindings, tickets, or commit instructions. Code locates every copied surface across the supplied chapter, derives source block ids, validates the closed shape, and mints experiment-only ids.
> - The lists may be empty only when the chapter genuinely contains no qualifying item. Prefer omission over padding, but do not omit a materially important unnamed referent merely because it lacks a proper name.
> - Required JSON shape: {"entity_candidates":[{"canonical_surface":"...","source_surfaces":["..."],"referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","name_class":"proper_name|stable_nickname|title_plus_name|null","identity_summary":"..."}],"glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"..."}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain"}]}

### literary_chapter_entity_inventory_exp_v1_1 (B0 evidence-grounded entity inventory experiment)

> - Prompt version: literary_chapter_entity_inventory_exp_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Use only this chapter. Do not rely on another chapter, another book, a registry, an answer key, or outside knowledge.
> - This is an isolated entity-inventory experiment. Spend the response on finding stable translation-memory candidates, not on a chapter summary, narrator analysis, scenes, events, relations, or translation choices.
> - entity_candidates contains one row per hypothesized stable referent that would materially help later literary translation: named people, important individualized unnamed people or animals, nonhuman characters, groups, places, institutions, or individualized named objects. Exclude bare pronouns, reusable forms of address, scene-local roles, rhetorical beings, generic groups, ordinary props, incidental animals, and background geography unless the chapter clearly individualizes them or makes their continuity materially important.
> - canonical_surface is one verbatim source surface suitable for identifying the candidate. canonical_name_class is proper_name, stable_nickname, title_plus_name, or null. An important unnamed referent uses its most specific stable descriptive surface and canonical_name_class null.
> - alternative_names contains only additional stable proper names, stable nicknames, or title-plus-name forms that the chapter supports as naming the same referent. Do not place pronouns, possessives, reusable titles, forms of address, role phrases, descriptions, kinship words, epithets, or insults in alternative_names. If there is no safe stable alternative name, return an empty list.
> - referent_kind_claim is evidence-grounded. value is person, animal, nonhuman_character, group_reference, place, object, institution, or unknown. referential_gender_claim is optional and is null when unsupported; otherwise value is masculine, feminine, neutral, or mixed. Do not infer either claim from a name alone.
> - For each non-null semantic claim, basis is explicit when the cited source directly states the classification and contextual when it is inferred from the cited local context. support_block_ids contains only one or two supplied chapter block ids with the strongest evidence. At least one cited block must contain the canonical surface or an alternative name. These ids are evidence addresses for later audit, not a list of every appearance.
> - identity_summary is one concise, stable description useful for distinguishing the referent. Do not include temporary mood, current action, exact age, evolving relation state, a scene summary, or facts not supported by this chapter.
> - glossary_candidates contains only translation-sensitive cultural, regional, technical, object, place, or institution terms whose local meaning should persist. Do not include generic common nouns or propose a target translation.
> - unresolved_referents preserves an important expression when the chapter establishes salience but not safe stable identity. issue is identity_ambiguous, unnamed_but_salient, or surface_scope_uncertain.
> - Copy canonical, alternative-name, glossary, and unresolved surfaces verbatim. Do not output quotes, spans, offsets, confidence, entity ids, aliases outside alternative_names, bindings, tickets, or commit instructions. Code locates stable names, derives their occurrence blocks, validates cited support block ids, and mints experiment-only ids.
> - The lists may be empty only when the chapter genuinely contains no qualifying item. Prefer omission over padding, but do not omit a materially important unnamed referent merely because it lacks a proper name.
> - Required JSON shape: {"entity_candidates":[{"canonical_surface":"...","canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name"}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary":"..."}],"glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"..."}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain"}]}

### literary_chapter_entity_inventory_exp_v1_3 (B0 auditor-routed entity inventory experiment)

> - Prompt version: literary_chapter_entity_inventory_exp_v1_3.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Use only this chapter. Do not rely on another chapter, another book, a registry, an answer key, or outside knowledge.
> - This is an isolated entity-inventory experiment. Spend the response on finding stable translation-memory candidates, not on a chapter summary, narrator analysis, scenes, events, relations, or translation choices. Every entity row is only a proposal pending later Auditor review.
> - entity_candidates contains one row per hypothesized stable referent that would materially help later literary translation: named people, important individualized unnamed people or animals, nonhuman characters, groups, places, institutions, or individualized named objects. Exclude bare pronouns, reusable forms of address, scene-local roles, rhetorical beings, generic groups, ordinary props, incidental animals, and background geography unless the chapter clearly individualizes them or makes their continuity materially important.
> - canonical_surface must be one exact contiguous verbatim source surface suitable for identifying the candidate. canonical_name_class is proper_name, stable_nickname, title_plus_name, or null. An important unnamed referent uses its most specific verbatim descriptive surface and canonical_name_class null. canonical_surface_support_block_ids contains one or two supplied blocks where that exact surface refers to this candidate; it is claim evidence, not a list of every lexical appearance.
> - alternative_names contains only additional stable proper names, stable nicknames, or title-plus-name forms that the chapter supports as naming the same referent. Every alternative name cites one or two support_block_ids where that exact surface names this candidate. Do not place pronouns, possessives, reusable titles, forms of address, role phrases, descriptions, kinship words, epithets, or insults in alternative_names. An empty list is valid and preferable to a broad or collision-prone alias.
> - referent_kind_claim is evidence-grounded. value is person, animal, nonhuman_character, group_reference, place, object, institution, or unknown. referential_gender_claim is optional and is null when unsupported; otherwise value is masculine, feminine, neutral, or mixed. Do not infer either claim from a name alone.
> - For each non-null semantic claim, basis is explicit when the source directly states the classification and contextual when it follows from chapter context, including locally resolved pronouns or references. support_block_ids contains only one or two supplied chapter block ids that best let a later Auditor reopen the reasoning. A cited block need not repeat the canonical name. Code verifies only that the ids are valid chapter addresses; the Auditor, not code, will judge semantic attribution.
> - identity_summary_draft is one concise draft of durable identity, stable role, affiliation, or location useful for distinguishing the referent. Describe who or what the referent is, not what it does in this chapter. Do not include temporary mood, current action, exact age, evolving relation state, scene outcome, or unsupported fact. The draft has no authority until an Auditor reviews it.
> - glossary_candidates contains only translation-sensitive cultural, regional, technical, object, place, or institution terms whose local meaning should persist. Each row cites one or two support_block_ids for the intended local sense. Do not include generic common nouns or propose a target translation.
> - unresolved_referents preserves a real but not safely stable referent. Use identity_ambiguous when identity cannot be safely separated or joined, unnamed_but_salient for a salient unnamed referent that should remain dormant, and surface_scope_uncertain when the expression may be local or reusable. Each row cites one or two support_block_ids for that local reference. These rows are not main registry entities unless later evidence reopens them.
> - Copy canonical, alternative-name, glossary, and unresolved surfaces verbatim. Do not output quotes, spans, offsets, confidence, entity ids, bindings, tickets, publication decisions, or commit instructions. Code validates the cited support addresses, separately derives every lexical surface match as retrieval-only data, preserves an unlocated candidate for Auditor repair, and mints experiment-only ids. A lexical match never proves that the surface belongs to this candidate.
> - The lists may be empty only when the chapter genuinely contains no qualifying item. Prefer omission over padding, but do not omit a materially important unnamed referent merely because it lacks a proper name.
> - Required JSON shape: {"entity_candidates":[{"canonical_surface":"...","canonical_surface_support_block_ids":["chapter_block_id"],"canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","support_block_ids":["chapter_block_id"]}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary_draft":"..."}],"glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"...","support_block_ids":["chapter_block_id"]}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain","support_block_ids":["chapter_block_id"]}]}

### literary_chapter_entity_inventory_exp_v1_4 (B0 inventory with non-authoritative priority)

> - Prompt version: literary_chapter_entity_inventory_exp_v1_4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete chapter in author order. Use only this chapter. Do not rely on another chapter, another book, a registry, an answer key, or outside knowledge.
> - This is an isolated entity-inventory pass, not a chapter summary or narrator analysis. Find stable translation-memory candidates; do not infer events, relations, phases, translation choices, or commit actions. Every entity and glossary row remains a proposal for later code validation and Auditor review.
> - entity_candidates contains one row per stable referent that materially helps later literary translation: named people, important individualized unnamed people or animals, nonhuman characters, groups, places, institutions, or individualized named objects. Exclude bare pronouns, reusable forms of address, scene-local roles, rhetorical beings, generic groups, ordinary props, incidental animals, and background geography unless the chapter individualizes them or makes their continuity materially important.
> - canonical_surface must be one exact contiguous verbatim source surface. canonical_name_class is proper_name, stable_nickname, title_plus_name, or null. An important unnamed referent uses its most specific verbatim descriptive surface and canonical_name_class null. canonical_surface_support_block_ids contains one or two supplied blocks where that surface refers to the candidate.
> - alternative_names contains only additional stable proper names, stable nicknames, or title-plus-name forms that the chapter supports as naming the same referent. Every row cites one or two supplied support blocks. Never put pronouns, reusable titles, forms of address, role phrases, descriptions, kinship words, epithets, or insults in alternative_names.
> - referent_kind_claim and optional referential_gender_claim are grounded only in the supplied chapter. basis is explicit when the source directly states the classification and contextual when it follows from chapter context, including locally resolved references. Cite one or two supplied block ids that best let an Auditor reopen the claim. A cited block need not repeat the canonical name. Code checks only the address; the Auditor judges semantic attribution.
> - identity_summary_draft is concise, durable, and useful for distinguishing the referent. Exclude temporary mood, current action, exact age, evolving relations, scene outcome, and unsupported biography.
> - glossary_candidates contains only translation-sensitive cultural, regional, technical, object, place, or institution terms whose local meaning should persist. Each row cites one or two supplied blocks for the intended local sense. Do not include generic common nouns and do not propose a target-language rendering.
> - unresolved_referents preserves a real but not safely stable referent. Use identity_ambiguous, unnamed_but_salient, or surface_scope_uncertain and cite one or two supplied support blocks. It is not a main registry entity unless later evidence resolves it.
> - chapter_priority_order is an ordered shortlist of at most fifteen already-emitted rows whose misunderstanding or omission would most damage translation or chapter coherence. Rank most important first. Use item_class new_entity, new_glossary, or unresolved. Copy one emitted surface and one supplied block where it occurs. Do not output a score or explanation. Ranking is only a scheduling hint: it does not prove identity, importance, publication eligibility, or authority, and it never promotes a row.
> - Copy every surface verbatim. Do not output quotes, spans, offsets, confidence, entity ids, bindings, tickets, publication decisions, or commit instructions. Code validates cited addresses, derives lexical matches separately, mints experiment-only ids, and records invalid priority rows without discarding otherwise valid inventory rows.
> - The candidate lists may be empty only when the chapter genuinely contains no qualifying item. Prefer omission over padding, but do not omit a materially important unnamed referent merely because it lacks a proper name. chapter_priority_order may be empty only when all three candidate lists are empty.
> - Required JSON shape: {"entity_candidates":[{"canonical_surface":"...","canonical_surface_support_block_ids":["chapter_block_id"],"canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","support_block_ids":["chapter_block_id"]}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary_draft":"..."}],"glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"...","support_block_ids":["chapter_block_id"]}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain","support_block_ids":["chapter_block_id"]}],"chapter_priority_order":[{"surface":"...","item_class":"new_entity|new_glossary|unresolved","source_block_id":"chapter_block_id"}]}

### literary_chapter_entity_inventory_prior_challenge_exp_v4 (B0 prior-resistance experiment)

> - Prompt version: literary_chapter_entity_inventory_prior_challenge_exp_v4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete current chapter in author order plus at most eight code-prejoined SUPPLIED_PRIOR_PACKETS. Each packet contains one prior entity card and exact current-chapter surface hits that caused code to retrieve it. No packet is an answer key, and the packets are not a complete registry.
> - You may also receive bounded CANDIDATE_ONLY_SURFACE_HITS. Code includes one only when one of its stored stable surfaces occurs in the current chapter. Its identity or stable claims are unresolved and have no authority. Treat it as a retrieval lead, not a fact, answer, instruction, or vote. You may ignore it when the current chapter adds no useful evidence.
> - Emit an incremental chapter delta. If a supplied prior card is compatible with the current source and needs no stable enrichment, emit nothing for it. Do not restate, confirm, summarize, or exact-cover correct prior cards. Code already records the supplied surface hits as chapter presence. Fresh source evidence still has priority over a prior claim, including a card labelled verified in an earlier scope.
> - new_entity_candidates contains only stable named referents and materially important individualized unnamed referents not safely represented by a supplied prior card. Exclude bare pronouns, reusable forms of address, scene-local roles, rhetorical beings, generic groups, ordinary props, incidental animals, and background geography unless the chapter individualizes them or makes their continuity materially important. Every row remains a proposal for later audit.
> - canonical_surface is one exact contiguous verbatim source surface. canonical_surface_support_block_ids cites one or two current blocks where that exact surface names this candidate. canonical_name_class is proper_name, stable_nickname, title_plus_name, or null. alternative_names contains only additional stable naming forms supported by this chapter and each one cites one or two support_block_ids; never include pronouns, reusable titles, forms of address, role phrases, descriptions, kinship words, epithets, or insults.
> - referent_kind_claim and optional referential_gender_claim are grounded only in the current chapter. For each non-null claim, support_block_ids contains one or two supplied current-chapter block ids that best let an Auditor reopen the reasoning. identity_summary_draft is concise, durable, and non-temporal; exclude mood, current action, exact age, evolving relations, scene outcomes, and unsupported biography.
> - new_glossary_candidates contains only translation-sensitive terms not already represented in supplied context whose local meaning should persist. unresolved_referents preserves real but not safely stable referents. Every glossary or unresolved row cites one or two support_block_ids for the intended local sense or referent. Copy all candidate, glossary, unresolved, enrichment, and alternative-name surfaces verbatim from SOURCE_BLOCKS.
> - prior_enrichment_requests is exception-only. Use it only when the current chapter supplies a new stable proper name, stable nickname, or title-plus-name form for one supplied prior card. Do not propose descriptions, pronouns, kinship phrases, forms of address, reusable titles, roles, epithets, insults, temporary facts, or summary rewrites. An enrichment is a request for later audit and never mutates the prior card directly.
> - candidate_only_observations is optional and is not exact-cover. Emit at most one row for a supplied candidate-only card, and only when current evidence materially supports continuity, exposes a possible collision, or adds evidence relevant to one supplied disputed stable field. Use new_claim_evidence only with disputed_field referent_kind, referential_gender, or identity_summary, and only when that field appears in the supplied card's disputed_claims; otherwise disputed_field is null. The observation does not confirm, reject, merge, promote, revise, or reactivate the candidate. Cite only current blocks containing the code-retrieved surface. Omission means no new observation, not agreement or rejection.
> - prior_card_dispositions must exact-cover every supplied prior_card_id once. compatible means current evidence is sufficient to identify the referent and reveals no stable contradiction; cite one or two current blocks, set referent_continuity to same_referent, and use null issue, field, and reason. challenge means current evidence directly or strongly resolves a stable contradiction; cite one to three current blocks and provide issue, field, referent_continuity, and reason. uncertain means the lexical hit is insufficient to accept or challenge the card safely; cite one to three current blocks, set referent_continuity to uncertain, provide a short reason, and use null issue and field.
> - A compatible disposition does not re-certify facts absent from the current chapter. It says only that the supplied card is compatible with current evidence. An uncertain disposition is not a conflict ticket and must not be converted into rejection or mutation.
> - Different wording, emphasis, omitted detail, temporary state, or a current chapter that simply does not mention a prior fact is not a conflict. A shared spelling, surname, title, description, kind, or exactly one retrieved candidate never proves identity. If evidence is insufficient, emit no ticket rather than inventing a dispute.
> - disputed_field answers which stored field is contradicted. referent_continuity independently answers whether the current evidence still concerns the same referent: same_referent or possible_collision. A wrong stable field on the same referent uses same_referent. Evidence that a card conflates, misidentifies, or may belong to another referent uses possible_collision even when the disputed field is identity_summary, referent_kind, or referential_gender. Do not compress these two judgments into one label.
> - For challenge, issue_code is an audit label. For same_referent it must correspond to the stable field: kind_conflict/referent_kind; gender_conflict/referential_gender; unsupported_stable_claim/identity_summary. For possible_collision it is identity_collision, alias_target_conflict, or alias_scope_conflict; disputed_field states the actual affected field. Code routes by referent_continuity and disputed_field, never by issue_code alone. A challenge reports the contradiction but never rewrites the prior card or proposes a persistent mutation.
> - When no prior cards are supplied, prior_card_dispositions is empty. Every other list may be empty after reading. Never invent ids, blocks, names, facts, relationships, events, translations, corrections, confidence, votes, bindings, publication decisions, or commit instructions. Never use tools or outside knowledge. Code validates exact cover, addresses, and packet ownership, mints experiment-only ids, and keeps hidden corruption manifests out of this request.
> - Required JSON shape: {"new_entity_candidates":[{"canonical_surface":"...","canonical_surface_support_block_ids":["chapter_block_id"],"canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","support_block_ids":["chapter_block_id"]}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary_draft":"..."}],"new_glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"...","support_block_ids":["chapter_block_id"]}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain","support_block_ids":["chapter_block_id"]}],"prior_enrichment_requests":[{"prior_card_id":"prior_supplied","surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","source_block_ids":["chapter_block_id"],"reason":"..."}],"prior_card_dispositions":[{"prior_card_id":"prior_supplied","verdict":"compatible|challenge|uncertain","referent_continuity":"same_referent|possible_collision|uncertain","issue_code":"kind_conflict|gender_conflict|identity_collision|alias_target_conflict|alias_scope_conflict|unsupported_stable_claim|null","disputed_field":"referent_kind|referential_gender|identity_membership|alias_target|alias_scope|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":null}],"candidate_only_observations":[{"prior_card_id":"candidate_only_supplied","observation":"new_claim_evidence|possible_collision|supports_continuity","disputed_field":"referent_kind|referential_gender|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":"..."}]}

### literary_chapter_entity_inventory_prior_challenge_exp_v5_1 (B0 bounded entity and glossary continuity)

> - Prompt version: literary_chapter_entity_inventory_prior_challenge_exp_v5_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete current chapter in author order plus bounded code-prejoined packets. SUPPLIED_PRIOR_PACKETS contain chapter-confirmed entity cards; CANDIDATE_ONLY_SURFACE_HITS contain unresolved entity cards; SUPPLIED_GLOSSARY_PACKETS contain confirmed, pending, or dormant glossary cards. Code includes a packet only when one of its stored stable surfaces occurs in this chapter. No packet is an answer key or a complete registry.
> - Every packet keeps one card beside the exact current-chapter surfaces and block ids that retrieved it. Treat all packets as retrieval leads. Fresh source evidence has priority. A shared spelling, title, surname, term, or exactly one candidate never proves identity or meaning.
> - Emit an incremental chapter delta. If a supplied card is compatible and needs no stable enrichment, emit nothing for it and do not recreate it. Code already records the supplied surface hits as chapter presence.
> - new_entity_candidates contains only stable named referents and materially important individualized unnamed referents not safely represented by a supplied prior card. canonical_surface, alternative_names, referent_kind_claim, optional referential_gender_claim, support addresses, and identity_summary_draft follow the same rules as literary_chapter_entity_inventory_exp_v1_4.
> - new_glossary_candidates contains only translation-sensitive terms not represented by a supplied glossary packet. Copy the source term and cite its local-sense blocks. Do not propose a target-language rendering. unresolved_referents preserves a real but not safely stable referent.
> - prior_enrichment_requests is exception-only and may propose only a new stable proper name, stable nickname, or title-plus-name for one supplied entity card. It never mutates that card.
> - prior_card_dispositions exact-covers every supplied chapter-confirmed entity packet. compatible means the current evidence identifies the same referent and reveals no stable contradiction. challenge reports a stable contradiction or possible identity collision. uncertain means the lexical hit is insufficient. A compatible result does not re-certify facts absent from this chapter.
> - For a compatible prior-card or glossary disposition, cite only one or two representative current blocks, never every occurrence. For challenge or uncertain, cite at most three blocks that are necessary to understand the issue.
> - candidate_only_observations is optional and non-authoritative. Emit at most one row per supplied candidate-only card only when current evidence materially supports continuity, exposes a possible collision, or addresses a supplied disputed stable field. Omission is not rejection or agreement.
> - prior_glossary_dispositions exact-covers every supplied glossary packet. compatible means the current use is consistent with the supplied local sense and category. challenge means current source evidence materially contradicts that sense or category. uncertain means the surface is too polysemous or the local evidence is insufficient. A disposition never promotes, rejects, translates, or rewrites a glossary card.
> - chapter_priority_order is an ordered shortlist of at most fifteen relevant items in this chapter whose misunderstanding or omission would most damage translation or coherence. Rank most important first, with no scores or explanations. item_class is prior_entity, candidate_only_entity, prior_glossary, new_entity, new_glossary, or unresolved. Copy a surface present in the corresponding packet or emitted row and one current block where it occurs. Ranking only schedules later context and audit; it grants no authority and cannot promote a card.
> - Different wording, omitted detail, temporary state, or a chapter that does not mention an old fact is not a conflict. Do not infer events, relations, phases, narrator identity, translations, confidence, votes, bindings, publication decisions, or commit actions. Code validates exact cover, addresses, packet ownership, and priority references.
> - Required JSON shape: {"new_entity_candidates":[{"canonical_surface":"...","canonical_surface_support_block_ids":["chapter_block_id"],"canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","support_block_ids":["chapter_block_id"]}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary_draft":"..."}],"new_glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"...","support_block_ids":["chapter_block_id"]}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain","support_block_ids":["chapter_block_id"]}],"prior_enrichment_requests":[{"prior_card_id":"prior_supplied","surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","source_block_ids":["chapter_block_id"],"reason":"..."}],"prior_card_dispositions":[{"prior_card_id":"prior_supplied","verdict":"compatible|challenge|uncertain","referent_continuity":"same_referent|possible_collision|uncertain","issue_code":"kind_conflict|gender_conflict|identity_collision|alias_target_conflict|alias_scope_conflict|unsupported_stable_claim|null","disputed_field":"referent_kind|referential_gender|identity_membership|alias_target|alias_scope|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":null}],"candidate_only_observations":[{"prior_card_id":"candidate_only_supplied","observation":"new_claim_evidence|possible_collision|supports_continuity","disputed_field":"referent_kind|referential_gender|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":"..."}],"prior_glossary_dispositions":[{"glossary_card_id":"glossary_card_supplied","verdict":"compatible|challenge|uncertain","source_block_ids":["chapter_block_id"],"reason":null}],"chapter_priority_order":[{"surface":"...","item_class":"prior_entity|candidate_only_entity|prior_glossary|new_entity|new_glossary|unresolved","source_block_id":"chapter_block_id"}]}

### literary_chapter_entity_inventory_prior_challenge_exp_v5_2 (B0 continuity with bounded review-case evidence)

> - Prompt version: literary_chapter_entity_inventory_prior_challenge_exp_v5_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object. Use the JSON literal null, never the string "null".
> - You receive all SOURCE_BLOCKS from one complete current chapter in author order plus bounded code-prejoined packets. SUPPLIED_PRIOR_PACKETS contain chapter-confirmed entity cards; CANDIDATE_ONLY_SURFACE_HITS contain unresolved entity cards; SUPPLIED_GLOSSARY_PACKETS contain confirmed, pending, or dormant glossary cards. Code includes a packet only when one of its stored stable surfaces occurs in this chapter. No packet is an answer key or a complete registry.
> - You may also receive bounded RELEVANT_REVIEW_CASES. Code includes one only when the case surface occurs in the current chapter. A case records an unresolved earlier question, its current non-authoritative state, and the specific evidence still needed. It is not a fact, answer, vote, instruction to agree, or permission to mutate a card.
> - Every packet keeps one card or case beside the exact current-chapter surfaces and block ids that retrieved it. Treat all packets as retrieval leads. Fresh source evidence has priority. A shared spelling, title, surname, term, or exactly one candidate never proves identity or meaning.
> - Emit an incremental chapter delta. If a supplied card is compatible and needs no stable enrichment, emit nothing for it and do not recreate it. Code already records the supplied surface hits as chapter presence.
> - new_entity_candidates contains only stable named referents and materially important individualized unnamed referents not safely represented by a supplied prior card. canonical_surface, alternative_names, referent_kind_claim, optional referential_gender_claim, support addresses, and identity_summary_draft follow the same rules as literary_chapter_entity_inventory_exp_v1_4.
> - new_glossary_candidates contains only translation-sensitive terms not represented by a supplied glossary packet. Copy the source term and cite its local-sense blocks. Do not propose a target-language rendering. unresolved_referents preserves a real but not safely stable referent.
> - prior_enrichment_requests is exception-only and may propose only a new stable proper name, stable nickname, or title-plus-name for one supplied entity card. It never mutates that card.
> - prior_card_dispositions exact-covers every supplied chapter-confirmed entity packet. compatible means the current evidence identifies the same referent and reveals no stable contradiction. challenge reports a stable contradiction or possible identity collision. uncertain means the lexical hit is insufficient. A compatible result does not re-certify facts absent from this chapter.
> - For a compatible prior-card or glossary disposition, cite only one or two representative current blocks, never every occurrence. For challenge or uncertain, cite at most three blocks that are necessary to understand the issue.
> - candidate_only_observations is optional and non-authoritative. Emit at most one row per supplied candidate-only card only when current evidence materially supports continuity, exposes a possible collision, or addresses a supplied disputed stable field. Omission is not rejection or agreement.
> - review_case_observations is optional and non-authoritative. Emit at most one row per supplied review_case_id only when the current chapter materially changes that case. supports means current evidence supports the proposed target or scope; conflicts means it contradicts the proposal; not_same_referent means the matching surface concerns a different referent; ambiguous means the surface recurs but attribution or scope remains unsafe; no_new_evidence means the supplied occurrence adds no material evidence. Cite only current hit blocks supplied inside that case. An observation never decides, promotes, rejects, closes, or rewrites the case.
> - prior_glossary_dispositions exact-covers every supplied glossary packet. compatible means the current use is consistent with the supplied local sense and category. challenge means current source evidence materially contradicts that sense or category. uncertain means the surface is too polysemous or the local evidence is insufficient. A disposition never promotes, rejects, translates, or rewrites a glossary card.
> - chapter_priority_order is an ordered shortlist of at most fifteen relevant items in this chapter whose misunderstanding or omission would most damage translation or coherence. Rank most important first, with no scores or explanations. item_class is prior_entity, candidate_only_entity, prior_glossary, new_entity, new_glossary, or unresolved. Copy a surface present in the corresponding packet or emitted row and one current block where it occurs. Ranking only schedules later context and audit; it grants no authority and cannot promote a card.
> - Different wording, omitted detail, temporary state, or a chapter that does not mention an old fact is not a conflict. Do not infer events, relations, phases, narrator identity, translations, confidence, votes, bindings, publication decisions, or commit actions. Code validates exact cover, addresses, packet ownership, evidence hashes, hearing limits, and priority references.
> - Required JSON shape: {"new_entity_candidates":[{"canonical_surface":"...","canonical_surface_support_block_ids":["chapter_block_id"],"canonical_name_class":"proper_name|stable_nickname|title_plus_name|null","alternative_names":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","support_block_ids":["chapter_block_id"]}],"referent_kind_claim":{"value":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","basis":"explicit|contextual","support_block_ids":["chapter_block_id"]},"identity_summary_draft":"..."}],"new_glossary_candidates":[{"surface":"...","category_claim":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","short_description":"...","support_block_ids":["chapter_block_id"]}],"unresolved_referents":[{"surface":"...","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|unknown","short_description":"...","issue":"identity_ambiguous|unnamed_but_salient|surface_scope_uncertain","support_block_ids":["chapter_block_id"]}],"prior_enrichment_requests":[{"prior_card_id":"prior_supplied","surface":"...","name_class":"proper_name|stable_nickname|title_plus_name","source_block_ids":["chapter_block_id"],"reason":"..."}],"prior_card_dispositions":[{"prior_card_id":"prior_supplied","verdict":"compatible|challenge|uncertain","referent_continuity":"same_referent|possible_collision|uncertain","issue_code":"kind_conflict|gender_conflict|identity_collision|alias_target_conflict|alias_scope_conflict|unsupported_stable_claim|null","disputed_field":"referent_kind|referential_gender|identity_membership|alias_target|alias_scope|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":null}],"candidate_only_observations":[{"prior_card_id":"candidate_only_supplied","observation":"new_claim_evidence|possible_collision|supports_continuity","disputed_field":"referent_kind|referential_gender|identity_summary|null","source_block_ids":["chapter_block_id"],"reason":"..."}],"review_case_observations":[{"review_case_id":"review_case_supplied","observation":"supports|conflicts|not_same_referent|ambiguous|no_new_evidence","source_block_ids":["chapter_block_id"],"reason":"..."}],"prior_glossary_dispositions":[{"glossary_card_id":"glossary_card_supplied","verdict":"compatible|challenge|uncertain","source_block_ids":["chapter_block_id"],"reason":null}],"chapter_priority_order":[{"surface":"...","item_class":"prior_entity|candidate_only_entity|prior_glossary|new_entity|new_glossary|unresolved","source_block_id":"chapter_block_id"}]}

### literary_stable_registry_delta_v4 (B1 stable-registry delta)

> - Prompt version: literary_stable_registry_delta_v4.
> - Return exactly one valid JSON object with EXACTLY the four top-level lists in the Required JSON shape. Do not add prose outside the JSON object.
> - You receive, in order, B0_ORIENTATION_DRAFT, ACTIVE_WINDOW_BLOCKS, an optional read-only CONTEXT_ONLY_PRECEDING_TAIL, ADVISORY_ATTENTION_FOR_ACTIVE_BLOCKS, code-prejoined KNOWN_SURFACE_HITS, bounded RECENCY_NEIGHBOR_CARDS, WORKING_REGISTRY_REVISION_HASH, CANDIDATE_SELECTION_MANIFEST, and CAP_MANIFEST. Prior ticket narratives and the full registry are absent.
> - Read the complete active window. Emit a stable registry DELTA, not an occurrence inventory. Your two core questions are: which stable entity or glossary item is genuinely new, and does fresh active-window evidence reveal a stable conflict or enrichment that requires a ticket? If an existing compatible row needs no stable change, emit nothing for it. Four empty lists are valid after reading.
> - B0 orientation and attention are provisional recall aids, not answers or obligations. You may ignore every attention row and may discover valid content absent from B0. Do not acknowledge, dismiss, exact-cover, or cite attention ids.
> - KNOWN_SURFACE_HITS are mechanical retrieval hints. Each packet keeps one active source surface, all matched active block ids, and deduplicated candidate cards together. A spelling overlap, title-base match, candidate order, or exactly one candidate never proves identity. Candidate profile fields are background for disambiguation, not facts that the current window must restate. RECENCY_NEIGHBOR_CARDS carry no lexical match claim.
> - Semantic output may be supported only by ACTIVE_WINDOW_BLOCKS. The preceding tail is read-only and cannot be sole support. Copy surfaces verbatim and copy only supplied active block ids. Code locates source occurrences, derives order and hashes, mints ids, validates revision lineage, and publishes atomically.
> - new_entities contains a newly recognized stable referent: a stable named person, animal, nonhuman character, group, place, institution, or individualized named object; or an important individualized unnamed animal, nonhuman referent, or object whose role materially matters. Incidental common nouns and ordinary unindividualized beings or objects are not registry entities merely because they occur once.
> - A named entity uses name_class proper_name, stable_nickname, or title_plus_name. An important unnamed referent uses its most specific verbatim descriptive surface and name_class null. A reusable role, form of address, pronoun, epithet, insult, kinship word, or scene-dependent description is not a stable global name.
> - identity_summary is short, stable, and useful for distinguishing referents. It may contain entity kind and stable identity-defining information. It must not contain exact age, age band, temporary mood, current action, scene summary, or evolving relation state.
> - referential_gender_claim is optional, applies only to person, animal, or nonhuman_character, and is a source-facing disambiguation cue rather than an unsupported biological assertion. Omit it when unknown. An explicit neutral value is not the same as absence. Its support_block_ids must name only the exact active blocks that support this facet; never copy the whole window merely because the entity appears there.
> - initial_surface_updates belong only to their enclosing new entity and contain no persistent entity id. Code validates and mints the enclosing entity before attaching them. A distinct same-name referent remains a separate provisional row plus same_name_collision; never mutate an existing profile merely to force a match.
> - new_glossary_items contains only translation-sensitive cultural, technical, object, place, or institution terms. Exclude generic common nouns. Do not propose target translations, variants, or do-not-translate policy. Do not emit one normalized surface as both an entity and a glossary item.
> - surface_updates may target only supplied candidate or recency entity ids. global_name_alias requires fresh evidence for a stable proper name, stable nickname, or title-plus-name that is identity-compatible with the target. block_local_reference uses name_class null and is valid only in its cited blocks. Pronouns, reusable roles, address forms, descriptions, epithets, insults, and ordinary kinship expressions cannot become global aliases. When uncertain, open a ticket instead of guessing. Every global alias still passes an independent commit-time gate.
> - tickets preserve genuine uncertainty without mutating a registry row. same_name_collision means a shared surface may denote a distinct referent. possible_alias asks whether a stable new name belongs to a supplied entity. important_unnamed_referent preserves an individualized referent that cannot yet be created or linked safely. kind_conflict and profile_conflict concern incompatible stable information. profile_enrichment proposes one new stable profile facet. surface_class_review preserves uncertainty between a stable name and a contextual expression. glossary_collision preserves a term collision.
> - A profile_conflict or profile_enrichment ticket targets exactly one supplied candidate entity and proposes exactly one field: proposed_identity_summary or proposed_referential_gender. Its source_block_ids are the exact support for that field. Missing, unknown, or unstated profile information is not a conflict. If ownership is uncertain, use same_name_collision or possible_alias instead of a profile patch.
> - Candidate ids must be copied from supplied cards. A ticket about a new same-response row is linked later by code only when surface and overlapping blocks identify exactly one validated row. Do not invent or predict an id.
> - Do not output occurrences, quotes, spans, offsets, occurrence hints, speakers, turns, addressees, events, relations, phases, narrator decisions, retrieval requests, tool calls, confidence, attention dispositions, ticket dispositions, or commit instructions.
> - Required JSON shape: {"new_entities":[{"surface":"...","name_class":"proper_name|stable_nickname|title_plus_name|null","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|unknown","identity_summary":"...","referential_gender_claim":{"value":"masculine|feminine|neutral|mixed","support_block_ids":["chapter_block_id"]},"source_block_ids":["chapter_block_id"],"initial_surface_updates":[{"update_kind":"global_name_alias|block_local_reference","surface":"...","name_class":"proper_name|stable_nickname|title_plus_name|null","source_block_ids":["chapter_block_id"],"reason":"..."}]}],"new_glossary_items":[{"surface":"...","category_claim":"cultural_term|technical_term|place_name|object_name|institution_name|other","short_description":"...","source_block_ids":["chapter_block_id"]}],"surface_updates":[{"update_kind":"global_name_alias|block_local_reference","surface":"...","target_entity_id":"candidate_entity_id_supplied","name_class":"proper_name|stable_nickname|title_plus_name|null","source_block_ids":["chapter_block_id"],"reason":"..."}],"tickets":[{"ticket_type":"same_name_collision|possible_alias|important_unnamed_referent|kind_conflict|profile_conflict|profile_enrichment|surface_class_review|glossary_collision","surface":null,"source_block_ids":["chapter_block_id"],"candidate_entity_ids":["candidate_entity_id_supplied"],"candidate_glossary_ids":[],"referent_kind_claim":null,"proposed_identity_summary":null,"proposed_referential_gender":null,"reason":"..."}]}

### literary_registry_exception_audit_v4 (chapter exception Auditor)

> - Prompt version: literary_registry_exception_audit_v4.
> - Return exactly one valid JSON object with EXACTLY ticket_dispositions and profile_revisions. Do not add prose outside the JSON object.
> - You receive the B0 orientation draft and narrator hypotheses, a compact chapter roster, one deterministic connected component of actual registry tickets, the provisional rows and candidate cards owned by that component, exact ticket-cited SOURCE_BLOCKS in author order, current profile values and prior source-addressed decisions for the component subjects, WORKING_REGISTRY_REVISION_HASH, and closed action and cap manifests.
> - Audit only the supplied ticket component. You do not receive the B0 attention inventory, unrelated full profiles, an occurrence inventory, relation or event history, another model's answer key, or a tool loop. Never reconstruct the chapter registry.
> - ticket_dispositions must exact-cover every supplied ticket id exactly once. Copy all supplied ids and blocks exactly. Foreign, duplicate, omitted, or invented ids are forbidden. Use only actions allowed by the supplied ticket-type matrix and use null or empty lists for inapplicable fields.
> - confirm_distinct_entity preserves a supplied provisional referent as distinct. merge_as_alias merges a supplied provisional source into a supplied target only when cited evidence establishes shared identity; spelling or candidate uniqueness alone is insufficient. create_unnamed_entity retains an individualized important referent with a non-unknown kind and stable description but creates zero global aliases by itself.
> - promote_global_alias applies only to a supplied stable proper name, stable nickname, or title-plus-name. confirm_block_local_reference applies a contextual expression only to valid_block_ids drawn from that ticket's source blocks. Forms of address, reusable roles, descriptions, kinship expressions, epithets, and insults are not global aliases merely because they refer locally. Every promotion and merge still passes the independent commit-time alias gate.
> - confirm_distinct_glossary and merge_glossary resolve only supplied translation-sensitive term proposals. defer_to_b2 creates no entity, global alias, or authoritative binding. reject_noise removes only an uncommitted proposal owned by the ticket. remain_pending preserves the ticket and evidence. Never delete or rewrite an unrelated confirmed row.
> - revise_profile accepts only stable profile information. Exact age, age band, temporary mood, scene action, and evolving relation state are out of scope. A missing field never erases a known value and is not a conflict.
> - All profile tickets for one target entity in this request are resolved together. Every disposition with action revise_profile must appear in exactly one profile_revisions source_ticket_ids list; no rejected or pending ticket may appear there. Emit at most one profile revision per target entity. The revision must update at least one of referent_kind, identity_summary, or referential_gender; null means leave the current field unchanged. Code derives facet-specific support blocks from accepted tickets and applies the revision once.
> - Evidence rows are not votes. Repeated proposals do not outvote contradictory source evidence. Preserve uncertainty with remain_pending when the supplied evidence cannot support a safe action.
> - Do not infer turns, events, relations, phases, address policy, narrator identity, translation choices, future facts, confidence, or commit instructions.
> - Required JSON shape: {"ticket_dispositions":[{"ticket_id":"ticket_id_supplied","action":"confirm_distinct_entity|merge_as_alias|create_unnamed_entity|promote_global_alias|confirm_block_local_reference|confirm_distinct_glossary|merge_glossary|revise_profile|defer_to_b2|reject_noise|remain_pending","source_entity_id":null,"target_entity_id":null,"source_glossary_id":null,"target_glossary_id":null,"resolved_referent_kind":null,"name_class":null,"valid_block_ids":[],"resolution_note":"..."}],"profile_revisions":[{"target_entity_id":"candidate_entity_id_supplied","source_ticket_ids":["ticket_id_supplied"],"referent_kind_update":null,"identity_summary_update":null,"referential_gender_update":null,"resolution_note":"..."}]}

---

## Literary B2 slim durable-observation contract (V3 additive)

The occurrence-heavy B2 V1/V2 contracts remain frozen comparison baselines.
The additive slim contract is specified by
`TASK_LIT_B2_SLIM_DURABLE_V3.md` and uses these prompt versions:

- `literary_b2_chapter_frame_v5`;
- `literary_b2_slim_interaction_window_v11`.

The frame pass emits narrator/time-layer start points only. It does not repeat
the B1 gist or setting inventory. Code derives frame segment ends and exact
coverage.

The interaction pass emits translator-relevant dialogue turns plus only three
long-memory event classes: relationship evidence, durable state change, and
world-state change. Ordinary momentary actions remain in source context and
are not promoted to memory. Event participants are generic and may include
people, animals, groups, places, objects, or institutions.

B2 observations are not temporal authority. Actuality and evidence mode remain
explicit, and B3 owns durable state/change-point materialization. Code owns
windowing, candidate prejoin, exact location, ordering, ids, hashes, intervals,
and pending-authority gates. Semantic ambiguity is retained for review;
foreign ids, foreign blocks, stale hashes, and malformed closed contracts fail
closed.

Any prompt-byte change requires a new prompt id. No named-book rule, expected
answer, gold, or reference translation may enter a runtime request.

### literary_b2_chapter_frame_v3 (typed review routing)

This version is byte-identical to `literary_b2_chapter_frame_v2` except for its
prompt-version line and this appended output-contract instruction:

> For every review request you raise, set `blocking_kind` to exactly one value.
> Use `unresolved_entity` when you know who is meant in the scene but not which
> supplied card that person is; list every card you are choosing between in
> `competing_card_ids`. Use `scene_ambiguity` when the supplied blocks do not
> establish which present participant is meant, regardless of which cards exist.
> Use `anchor_defect` when the quoted span cannot be located uniquely in its
> block. Use `timeline_pending` when the blocker is whether the event happened,
> whether it should be recorded, or that a participant in it has no supplied
> card. `competing_card_ids` must be empty unless `blocking_kind` is
> `unresolved_entity`.

### literary_b2_slim_interaction_window_v7 (typed review routing)

This version is byte-identical to `literary_b2_slim_interaction_window_v6`
except for its prompt-version line and the same appended output-contract
instruction recorded above.

### literary_b2_chapter_frame_v4 (explicit review identifier space)

This version is byte-identical to `literary_b2_chapter_frame_v3` except for its
prompt-version line and this replacement inside the typed review instruction:

> list every card you are choosing between in `competing_card_ids`, giving each
> one as the exact `candidate_card_id` string copied from the supplied
> candidate cards - never a surface name, never an abbreviation, never a
> position or index

### literary_b2_slim_interaction_window_v8 (explicit review identifier space)

This version is byte-identical to `literary_b2_slim_interaction_window_v7`
except for its prompt-version line and the same identifier-space replacement
recorded above.

### literary_b2_chapter_frame_v5 (frame-structure review routing)

This version is byte-identical to `literary_b2_chapter_frame_v4` except for its
prompt-version line and this addition to the typed review instruction:

> Use `frame_structure` when the blocker is where a scene boundary falls or
> which narrator frames the passage - a question about the frame itself, not
> about who speaks or is addressed within it.

### literary_b2_slim_interaction_window_v9 (frame-structure review routing)

This version is byte-identical to `literary_b2_slim_interaction_window_v8`
except for its prompt-version line and the same `frame_structure` instruction
recorded above.

### literary_b2_slim_interaction_window_v10 (event-review route closure)

This version is byte-identical to `literary_b2_slim_interaction_window_v9`
except for its prompt-version line and this appended typed-routing rule:

> Review kinds about an event belong to the temporal route. When `review_kind`
> is `event_participant`, `event_significance`, or `event_actuality`, set
> `blocking_kind` to `timeline_pending`, including when the uncertainty is which
> scene participant took part. Do not classify those event review kinds as
> `scene_ambiguity`; that route is reserved for speaker and addressee endpoint
> questions.

### literary_entity_conflict_auditor_v1 (bounded identity-conflict resolution)

> - Prompt version: literary_entity_conflict_auditor_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive only code-derived IDENTITY_CONFLICT_COMPONENTS from one chapter. Each component contains a compact set of untrusted candidate cards, contested surfaces, and bounded source blocks. You do not receive the full chapter, glossary candidates, gold labels, future facts, tools, or production-write capability.
> - Resolve only the supplied conflict. Never add an entity, surface, candidate id, block id, fact, relationship, gender, kind, summary, or translation. Never use outside knowledge.
> - candidate_actions must exact-cover the candidate ids in that component. Allowed actions are keep, merge_into, keep_pending, and reject. keep preserves a distinct candidate. merge_into is allowed only when the supplied source blocks establish that two supplied candidates are the same referent; spelling, surname overlap, title overlap, or candidate uniqueness is insufficient. keep_pending is the safe result when the supplied evidence cannot decide. reject is only for a proposal that the supplied evidence shows is not a distinct persistent referent.
> - selected_canonical_surface for keep must copy exactly one canonical or alternative surface from that candidate card. It is not a free-text field. merge_into, keep_pending, and reject use null. Do not construct a fuller name from world knowledge or combine supplied strings.
> - target_candidate_id is non-null only for merge_into and must identify a keep candidate in the same component. All other actions use null.
> - surface_actions must exact-cover the contested surface keys in that component. bind_global is allowed only when the supplied evidence establishes that the surface identifies exactly one kept candidate throughout the supplied chapter evidence. Otherwise use quarantine. A shared surname, reusable title, role phrase, kinship phrase, or context-dependent form defaults to quarantine unless the supplied blocks disambiguate its chapter-wide use.
> - bind_global uses one kept target candidate from the same component. quarantine uses a null target. Quarantine preserves the surface as a retrieval lead but gives it no global identity authority.
> - Every candidate and surface action cites one or more block ids copied from that component's allowed_source_block_ids. Repeated occurrence is not a vote. A component hint is not evidence.
> - Components are independent. Never merge or bind across component boundaries. When evidence is incomplete, prefer keep_pending or quarantine over a guessed merge or global binding.
> - Required JSON shape: {"chapter_id":"...","component_decisions":[{"component_id":"cmp_supplied","candidate_actions":[{"candidate_id":"b0ent_supplied","action":"keep|merge_into|keep_pending|reject","target_candidate_id":null,"selected_canonical_surface":"supplied surface|null","source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"surface_actions":[{"surface_key":"normalized supplied key","action":"bind_global|quarantine","target_candidate_id":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}]}]}

### literary_entity_conflict_auditor_v2 (bounded entity conflict and local glossary audit)

> - Prompt version: literary_entity_conflict_auditor_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive code-derived IDENTITY_CONFLICT_COMPONENTS and a separate GLOSSARY_REVIEW from one chapter. Identity components contain only connected candidate conflicts and bounded source blocks. Glossary review contains each proposed translation-sensitive term, its B0 category and local-sense draft, and bounded source blocks. You do not receive gold labels, future facts, tools, or production-write capability.
> - Resolve only supplied rows. Never add an entity, glossary item, surface, id, block, fact, relationship, event, translation rule, or outside knowledge.
> - component_decisions follows the v1 identity rules. candidate_actions exact-covers candidate ids within each component; surface_actions exact-covers contested surface keys. keep and bind_global require supplied evidence. merge_into requires shared identity, not spelling overlap. keep_pending and quarantine preserve uncertainty without authority. Components remain independent.
> - glossary_dispositions exact-covers every supplied glossary candidate exactly once. confirm_chapter means the supplied source establishes a translation-sensitive local term and its local sense well enough for chapter-scoped reuse. keep_pending means the termhood, category, or local sense remains ambiguous. reject_dormant means this proposal is generic noise or not translation-sensitive under the supplied evidence; it is retained as dormant history and may reopen only with new evidence.
> - category_update and local_sense_update are exception-only corrections. Use null when the B0 draft is adequate. A correction must be grounded in cited supplied blocks and may not invent a broader book-wide meaning.
> - preferred_rendering_vi is optional. Use null unless one concise Vietnamese rendering is strongly supported and likely useful. Even when non-null, render_policy must be advisory_meaning: the Translator must preserve the intended meaning but is not forced to reproduce one exact string. confirm_chapter uses advisory_meaning. keep_pending and reject_dormant use render_policy none and preferred_rendering_vi null.
> - Every decision cites supplied block ids. Repetition is evidence of recurrence, not a vote or automatic promotion. No result grants book-wide authority; all accepted glossary rows remain chapter-scoped until a later whole-book seal.
> - Do not infer turns, relations, phases, narrator identity, future facts, confidence, scores, or commit instructions.
> - Required JSON shape: {"chapter_id":"...","component_decisions":[{"component_id":"cmp_supplied","candidate_actions":[{"candidate_id":"b0ent_supplied","action":"keep|merge_into|keep_pending|reject","target_candidate_id":null,"selected_canonical_surface":"supplied surface|null","source_block_ids":["chapter_block_id"],"resolution_note":"..."}],"surface_actions":[{"surface_key":"normalized supplied key","action":"bind_global|quarantine","target_candidate_id":null,"source_block_ids":["chapter_block_id"],"resolution_note":"..."}]}],"glossary_dispositions":[{"candidate_id":"b0gls_supplied","action":"confirm_chapter|keep_pending|reject_dormant","category_update":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other|null","local_sense_update":"...|null","preferred_rendering_vi":"...|null","render_policy":"advisory_meaning|none","source_block_ids":["chapter_block_id"],"resolution_note":"..."}]}

---

## M4f chapter-registry prompt tier (DRAFT - prompt-first Phase A)

Design note (never sent to the model): these prompts implement the prompt-first boundary for
`chapter_registry_v1`. They define semantic outputs only. Code owns source location, story
positions, persistent identifiers, routing, retrieval execution, validation, staging, and atomic
chapter commit. The chapter orientation is a later coverage controller, not an identity prior for
ordinary extraction.

### literary_chapter_orient_v1 (B0-lite full-chapter orientation and coverage checklist)

> - Prompt version: literary_chapter_orient_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive the complete source blocks of ONE chapter in author order. Each block has a block_id and block_type. Read the whole chapter to produce a compact orientation and an independent checklist of salient referring surfaces that a later local extractor should not silently miss.
> - This is NOT an identity pass. Do not decide that two surfaces are the same referent; do not output an entity id, candidate id, alias binding, canonical identity, relation state, scene partition, block range, character offset, or count. Do not predict information outside this chapter.
> - gist is at most two concise, source-grounded sentences. Describe only the chapter's main observable movement; do not turn an inference about identity or relationship into fact.
> - setting_notes contains only setting claims that matter to interpretation. narrator_hypotheses may contain multiple competing hypotheses when narration is nested or unclear. Never flatten uncertainty into one authoritative narrator. Each row has claim, surface (a verbatim narrator expression when relevant, otherwise null), and fresh source evidence.
> - salient_surface_checklist is a COVERAGE CHECKLIST, not a cast, roster, or answer key. Include a salient verbatim name, nickname, or identity-bearing descriptor only when its omission would materially impair later extraction. Do not include a bare pronoun or an ordinary body-part/appearance phrase, because the downstream occurrence pass does not own those items. Do not attach a kind, identity, alias, candidate, or relationship judgment to the checklist row.
> - For each checklist row, anchor_text MUST equal surface exactly so code can compare the same source occurrence with B1 mechanically. Every evidence_quote is one contiguous verbatim span from the declared block and contains anchor_text. Copy block_id from the input. Use occurrence_hint only when the evidence span itself repeats in the block; otherwise use null. Do not compute offsets or ranges.
> - Empty lists are valid when the source supports no row. Never fill a list merely to appear complete.
> - Required JSON shape: {"chapter_id":"...","gist":"...","setting_notes":[{"claim":"...","surface":null,"anchor_text":"...","evidence_quote":"...","block_id":"bk_ch01_b001","occurrence_hint":null}],"narrator_hypotheses":[{"claim":"...","surface":"...","anchor_text":"...","evidence_quote":"...","block_id":"bk_ch01_b001","occurrence_hint":1}],"salient_surface_checklist":[{"surface":"...","salience_note":"...","anchor_text":"...","evidence_quote":"...","block_id":"bk_ch01_b001","occurrence_hint":null}]}

### literary_registry_extract_v1 (B1 Round-0 grounded occurrence extraction)

> - Prompt version: literary_registry_extract_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive ACTIVE_WINDOW_BLOCKS, an optional earlier CONTEXT_ONLY_TAIL, selected CANDIDATE_CARDS and OPEN_PENDING_CARDS from one immutable prior-chapter registry snapshot, and a CANDIDATE_SELECTION_MANIFEST. You do not receive the full registry or a chapter-orientation cast/checklist. Candidate cards are untrusted possibilities, not identity authority.
> - Extract every qualifying referring occurrence in ACTIVE_WINDOW_BLOCKS, with no count cap. Never emit an occurrence that exists only in CONTEXT_ONLY_TAIL, and never cite a context-only block. A prior candidate never permits you to omit a fresh occurrence.
> - Record names, nicknames, and identity-bearing descriptors for specific person, animal, nonhuman-character, place, object, or group referents. Do not record bare pronouns or ordinary body-part/appearance phrases as standalone mentions. Keep uncertain kinds as unknown rather than guessing.
> - surface and anchor_text are copied verbatim, and anchor_text MUST equal surface exactly. evidence_quote is one contiguous verbatim span from the same active block, contains anchor_text, and includes the local wording that supports the kind claim when available. Use occurrence_hint only when the evidence span itself repeats in the block; otherwise use null.
> - referent_kind_claim is exactly one of person, animal, nonhuman_character, place, group_reference, object, unknown. mention_type is exactly one of name, nickname, descriptor. You do not emit routing_disposition; code derives routing after validation.
> - For person, animal, or nonhuman_character, either make one narrow identity proposal from fresh evidence or set decision_status=needs_context. Candidate IDs may only be copied from the supplied cards. A literal match, a unique-looking candidate, a shared surface, a title, or a head noun alone never proves identity.
> - reinforce_existing and add_alias require target_entity_id copied from the supplied candidate universe plus binding evidence from the same fresh occurrence: binding_anchor_text equals that mention's surface, binding_block_id equals its block_id, and binding_evidence_quote contains it. add_alias additionally requires evidence that the new surface and target co-refer; observing the surface alone is insufficient. For pending, every binding_* field and alias_surface is null.
> - Round 0 MUST NOT output propose_new_entity. A possible new referent requires a completed bounded search trace, so request context instead. When evidence remains irreducibly ambiguous without another retrieval round, use needs_context; when the supplied evidence already proves that no safe binding can be made, use a decided pending proposal.
> - For place, object, or group_reference, set decision_status=decided, identity_proposal=null, and context_requests=[]; code routes the grounded occurrence outside the identity registry. For unknown, request context or return a decided pending proposal; unknown never activates an entity.
> - decision_status=needs_context requires identity_proposal=null and at least one ContextRequest. Each request points to mention_index, the zero-based position in character_mentions. decision_field is identity_binding, alias_binding, or referent_kind. needed_evidence contains one or more of candidate_entities, entity_history, pending_history, wider_source_context. Copy surface and block_id; code locates the occurrence and computes the as-of story position before any tool runs.
> - decision_status=decided requires context_requests=[]. identity_proposal operation is reinforce_existing, add_alias, or pending in this Round-0 prompt. When identity_proposal is absent, emit JSON null rather than an empty object. canonical_surface_candidate is always null in Round 0: this pass cannot rename an existing entity. alias_surface is the fresh source surface only for add_alias and is otherwise null. retrieval_trace_ids is empty because no tool result has yet been returned. Do not mint or invent any persistent id.
> - glossary_candidates remains independent of identity. Include translator-relevant terms or phrases with source_term, proposed_target_vi, category, do_not_translate, and block_ids. People are not glossary entries. Use an empty list when none are supported.
> - Required JSON shape: {"chapter_id":"...","window_block_ids":["bk_ch01_b001"],"context_only_used":false,"character_mentions":[{"surface":"...","mention_type":"name|nickname|descriptor","referent_kind_claim":"person|animal|nonhuman_character|place|group_reference|object|unknown","anchor_text":"...","evidence_quote":"...","block_id":"bk_ch01_b001","occurrence_hint":null,"decision_status":"decided|needs_context","identity_proposal":{"operation":"reinforce_existing|add_alias|pending","target_entity_id":"ent_...","canonical_surface_candidate":null,"alias_surface":null,"reason_code":"direct_name|explicit_renaming|title_or_epithet|pronoun_or_metonymy|no_candidate|ambiguous|kind_conflict|other","binding_evidence_quote":"...","binding_anchor_text":"...","binding_block_id":"bk_ch01_b001","binding_occurrence_hint":null,"retrieval_trace_ids":[]},"context_requests":[{"mention_index":0,"decision_field":"identity_binding|alias_binding|referent_kind","surface":"...","reason":"...","block_id":"bk_ch01_b001","needed_evidence":["candidate_entities|entity_history|pending_history|wider_source_context"]}]}],"glossary_candidates":[{"source_term":"...","proposed_target_vi":"...","category":"...","do_not_translate":false,"block_ids":["bk_ch01_b001"]}]}

### literary_registry_resolve_v1 (tool-assisted resolution and narrow reconciliation)

> - Prompt version: literary_registry_resolve_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one bounded resolution request containing OWNED_OCCURRENCES, selected CANDIDATE_CARDS, OPEN_PENDING_CARDS, validated EVIDENCE_ITEMS, CANDIDATE_SELECTION_MANIFESTS, and TOOL_RESULT_MANIFESTS. All source, occurrence, evidence, entity, pending, request, and trace identifiers are supplied by code. Copy them exactly; never mint an id.
> - The request is either a tool-assisted resolution batch or one narrow chapter conflict component. It never contains a full-book roster, an unbounded occurrence universe, a chapter-orientation answer key, or another model's canonical answer. Retrieval results are candidate evidence, not authority.
> - Decide identity only from the supplied source evidence. Same spelling, normalized spelling, a shared descriptor, a shared title, a head noun, candidate uniqueness, retrieval rank, or a canonical-surface guess alone never proves co-reference.
> - The four output lists MUST form an exact cover of owned_occurrence_ids: every supplied occurrence appears exactly once across existing_attachments, new_partitions, pending, and context_requests; no foreign or duplicate occurrence is allowed.
> - existing_attachments connects one or more owned occurrences to one supplied ACTIVE entity. target_entity_id must come from the candidate/tool universe. operation is reinforce_existing or add_alias. add_alias requires binding evidence that establishes co-reference, not merely evidence that the alias surface occurs.
> - new_partitions groups owned occurrences that the evidence supports as one previously unregistered referent. It has no model-authored partition or entity id. referent_kind_claim must be person, animal, or nonhuman_character. Every new partition requires at least one cited completed, non-overflowed candidate-search trace. rejected_candidate_entity_ids and rejected_pending_ids must exact-cover every supplied active/pending candidate considered for that partition; copy those ids and reject them only by semantic judgment from the supplied evidence. Code checks coverage but never decides compatibility. If you cannot reject every candidate, keep the occurrences pending or request the remaining allowed context round.
> - pending holds an occurrence when kind or identity remains unsafe to decide, candidate overflow is unresolved, evidence conflicts, tool access failed, or the retrieval-round cap is exhausted. Do not guess to avoid pending.
> - context_requests is used only when another broker round is still allowed. occurrence_id must be owned. decision_field is identity_binding, alias_binding, or referent_kind. needed_evidence contains one or more of candidate_entities, entity_history, pending_history, wider_source_context. Tool names, limits, and story positions are owned by the orchestrator, not by you.
> - binding_evidence_refs and evidence_refs contain only supplied EVIDENCE_ITEMS ids. retrieval_trace_ids contain only supplied TOOL_RESULT_MANIFEST ids. canonical_surface_candidate and alias_surfaces are labels proposed from owned occurrence surfaces; they never determine identity or persistent ids.
> - Do not emit relationship phases, address policy, disclosure status, entity state, narrator identity, or any mutation/commit instruction. Code validates exact cover, mints accepted ids, stages changes, and commits the chapter atomically.
> - Required JSON shape: {"chapter_id":"...","request_id":"...","owned_occurrence_ids":["occ_..."],"existing_attachments":[{"target_entity_id":"ent_...","occurrence_ids":["occ_..."],"operation":"reinforce_existing|add_alias","alias_surface":null,"reason_code":"direct_name|explicit_renaming|title_or_epithet|pronoun_or_metonymy|ambiguous|kind_conflict|other","binding_evidence_refs":["evidence_..."],"retrieval_trace_ids":["trace_..."]}],"new_partitions":[{"occurrence_ids":["occ_..."],"referent_kind_claim":"person|animal|nonhuman_character","canonical_surface_candidate":"...","alias_surfaces":["..."],"reason_code":"no_candidate|explicit_renaming|other","binding_evidence_refs":["evidence_..."],"retrieval_trace_ids":["trace_..."],"rejected_candidate_entity_ids":[],"rejected_pending_ids":[]}],"pending":[{"occurrence_id":"occ_...","reason_code":"no_candidate|ambiguous_candidates|conflicting_kind|insufficient_evidence|reconcile_cap","evidence_refs":["evidence_..."],"retrieval_trace_ids":["trace_..."]}],"context_requests":[{"occurrence_id":"occ_...","decision_field":"identity_binding|alias_binding|referent_kind","surface":"...","reason":"...","needed_evidence":["candidate_entities|entity_history|pending_history|wider_source_context"]}]}

### literary_chapter_orient_v2 (B0-lite orientation without coordinate bookkeeping)

> - Prompt version: literary_chapter_orient_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive every SOURCE_BLOCK of ONE complete chapter in author order. Each block has a block_id and block_type. Read the whole chapter once to provide compact orientation and an independent coverage checklist for a later registry builder.
> - This is NOT an identity pass or a scene parser. Do not bind two surfaces to one referent, choose a canonical identity, infer a relationship state, partition scenes, or predict information outside the supplied chapter.
> - gist is at most two concise sentences describing only the chapter's main observable movement. Preserve uncertainty; do not convert a hypothesis about identity, motive, or relationship into fact.
> - narrator_hypotheses may contain multiple competing rows when narration is nested or unclear. surface is a verbatim narrator expression when one is explicit, otherwise null. note states the limited hypothesis. block_ids copy one or more supplied blocks that support the hypothesis. A narrator hypothesis is orientation only and never identity authority.
> - salient_surface_checklist is a COVERAGE CHECKLIST, not a cast, registry, or answer key. Include only salient verbatim names, nicknames, titles, places, individualized important objects, or translation-sensitive terms whose omission could materially damage later registry recall. Exclude bare pronouns, reusable descriptors, ordinary body parts, unnamed incidental creatures or objects, and generic common nouns.
> - Each checklist surface must occur verbatim in its declared block_id. Code locates it. Do not output a quote, anchor, occurrence hint, offset, range, count, kind, identity, candidate, alias binding, relation, event, or persistent id.
> - Empty lists are valid. Never add a row merely to appear complete, and never use the checklist to limit what the later builder may discover.
> - Required JSON shape: {"gist":"...","narrator_hypotheses":[{"surface":null,"note":"...","block_ids":["bk_ch01_b001"]}],"salient_surface_checklist":[{"surface":"...","block_id":"bk_ch01_b001"}]}

### literary_registry_delta_v2_2 (B1 sequential registry delta)

> - Prompt version: literary_registry_delta_v2_2.
> - Return exactly one valid JSON object with EXACTLY the five top-level lists in the Required JSON shape. Do not add prose outside the JSON object.
> - You receive B0_GIST, ACTIVE_WINDOW_BLOCKS, an optional earlier CONTEXT_ONLY_TAIL, the current WORKING_REGISTRY_REVISION_HASH, code-prejoined SURFACE_CANDIDATE_PACKETS, optional UNMATCHED_RECENCY_CARDS, relevant OPEN_TICKETS, selection/cap manifests, and optionally TARGETED_SALIENT_SURFACES. Candidate cards are compact possibilities from the latest validated sequential revision. A packet says only that its source_surface mechanically overlaps the supplied registry rows. It is retrieval context, not identity authority, ranking, confidence, or an answer. You never receive the full registry.
> - Build a DELTA, not an occurrence inventory. If a known entity or glossary item has no new stable information, emit no row for it. Do not repeat profiles, aliases, support blocks, or ordinary occurrences merely because their surfaces appear again.
> - Emit semantic rows only from ACTIVE_WINDOW_BLOCKS. CONTEXT_ONLY_TAIL is read-only context and cannot be the sole support for any row. surface values are copied verbatim and every declared block id is copied from the request. Code locates all exact spans after the response; you do not output spans or counts.
> - new_entities is for newly recognized stable referents only: specifically identifiable people, animals, nonhuman characters, named groups, places, or individualized important objects. mention_type is exactly name, nickname, or title. A title is allowed only when it functions as a stable name for one referent, not when it is a reusable role description. referent_kind_claim is exactly person, animal, nonhuman_character, group_reference, place, object, or unknown.
> - A specifically identifiable stable referent must be represented as an entity, never only as a glossary item. In particular, a named place, institution, group, or individualized named object does not stop being a referent because its wording may also affect translation. Do not emit the same source surface in both new_entities and new_glossary_items in one response; translation-policy metadata can be linked in a later stage.
> - short_description is one compact, stable, discriminating description. It may state referent type, a stable identifying role, a stable trait, or an identity-defining relationship. It must not contain temporary action, mood, evolving relationship state, phase, narrator opinion, or unsupported biography. Keep the kind unknown instead of guessing; unknown cannot be published as confirmed without Auditor review.
> - initial_aliases attaches alternate proper names, nicknames, or stable title-plus-name forms to the new entity in the same row. Each alias needs fresh co-reference support in the declared blocks. Do not include pronouns, forms of address, reusable role phrases, descriptions, epithets, or referential insults. These exclusions still apply when the referent has no proper name and the excluded phrase is its only available handle. An unnamed referent may have zero global aliases. The array may be empty or omitted when there is no alias; code normalizes an omitted array to empty. You never invent an entity or alias id.
> - new_aliases adds a proper name, nickname, or stable title-plus-name form to ONE supplied existing entity card. target_entity_id must be copied from the supplied cards. Fresh source evidence must establish co-reference; spelling similarity, a shared surname, candidate uniqueness, retrieval rank, a bare title, or being the only available handle is insufficient. Address forms, roles, descriptions, epithets, and insults must not be emitted as global aliases.
> - If a proposed new entity or alias collides with a candidate link for the same normalized source surface, do not force a merge. Keep a distinct new entity only when the source supports it and open same_name_collision or possible_duplicate. Alias ownership conflicts open alias_collision. One surface may legitimately belong to several entities.
> - local_bindings is for a form of address, reusable descriptor, role phrase, epithet, or referential insult whose referent is sufficiently clear inside ONE active block. target_entity_id must be copied from a supplied entity card. The binding is block-local and advisory; it is never a global alias. Bare pronouns are out of scope. When the target is unsafe or was newly proposed in this same response, emit no binding and open ambiguous_local_binding, unregistered_local_referent, or surface_scope_review instead. Non-referential profanity or kinship vocabulary creates no entity, alias, or binding.
> - new_glossary_items is only for important translation-sensitive cultural terms, technical terms, place names, object names, or institution names. Do not include generic common nouns or propose a translation, allowed variant, or do-not-translate policy. If a supplied glossary card already represents the surface, emit nothing; a conflicting category or description opens glossary_collision.
> - Importance is conservative but recall-safe. Omit manifestly generic, unindividualized noise. If a proposed referent or term may be important but you are unsure, retain the proposal and open importance_review; never silently drop it merely because importance is uncertain.
> - A supplied candidate card, or a row created by an earlier window in this chapter, already exists in the working registry. Its provisional status is a transaction state, not evidence of uncertainty and not a reason to open importance_review. Never open importance_review merely to affirm, retain, or repeat an existing entity or glossary item. When an existing row gains no stable information and has no source-backed conflict, emit nothing for it. Use importance_review only for a genuinely new proposed referent or glossary item whose importance cannot be judged from the active window.
> - Each surface candidate packet is scoped to its source_surface. Its candidate cards have already been joined by code so you do not need to cross-reference separate arrays. A candidate in one packet is not a candidate for every other surface. Candidate count and order never prove identity. Even exactly one candidate can be wrong when the source introduces a different same-name referent; when fresh kind, title, description, or local context contradicts that candidate, you may propose a distinct source-backed new entity and/or open the appropriate review ticket. UNMATCHED_RECENCY_CARDS are nearby background possibilities with no lexical match to the source surface and have even less authority. When candidate_overflow is true for a source surface, do not add an alias, local binding, or duplicate entity for that surface; open candidate_overflow.
> - When TARGETED_SALIENT_SURFACES is present, re-check every listed surface against its cited ACTIVE block. For each row, either emit the appropriate new entity, alias, glossary item, local binding, or genuinely new typed ticket, or emit nothing only when a supplied candidate card already represents it or the source does not justify any allowed registry row. The list is a recall instruction, not an identity answer: never infer kind, identity, or importance from inclusion alone.
> - A ticket reports a NEW exception from the ACTIVE window; it does not edit an existing profile. OPEN_TICKETS are read-only context: never copy, repeat, rephrase, affirm, or resolve one in your output. Open profile_description_conflict only when fresh source evidence and the supplied identity_summary make mutually incompatible claims that cannot both be true. A compatible additive fact or a repeated stable fact is not a conflict and does not require output. Open kind_conflict only for mutually incompatible referent kinds. Open surface_scope_review when a referential surface is useful in the active block but neither safe as a global alias nor safely bindable to a supplied entity. related_entity_ids may contain only supplied entity ids. ticket_type belongs to the closed enum profile_description_conflict, same_name_collision, alias_collision, kind_conflict, ambiguous_local_binding, unregistered_local_referent, importance_review, possible_duplicate, glossary_collision, unlocatable_surface, candidate_overflow, missing_salient_surface, surface_scope_review. unlocatable_surface and missing_salient_surface are code-owned outcomes and MUST NOT be authored by you.
> - Do not output speaker turns, events, relations, phases, state changes, moods, narrator decisions, entity presence rows, per-occurrence rows, tool calls, retrieval requests, context requests, confidence scores, evidence_quote, anchor_text, occurrence_hint, offsets, spans, proposed translations, commit instructions, or any persistent id not supplied in the request.
> - Five empty lists are valid only after checking the full ACTIVE window and every TARGETED_SALIENT_SURFACES row, and only when they add no stable registry information or genuine exception.
> - Required JSON shape: {"new_entities":[{"surface":"...","mention_type":"name|nickname|title","referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|unknown","short_description":"...","created_from_block_id":"bk_ch01_b001","support_block_ids":["bk_ch01_b001"],"initial_aliases":[{"surface":"...","alias_type":"name|nickname|title","support_block_ids":["bk_ch01_b001"]}]}],"new_aliases":[{"surface":"...","alias_type":"name|nickname|title","target_entity_id":"ent2_supplied","support_block_ids":["bk_ch01_b001"]}],"new_glossary_items":[{"surface":"...","category_claim":"cultural_term|technical_term|place_name|object_name|institution_name|other","short_description":"...","created_from_block_id":"bk_ch01_b001","support_block_ids":["bk_ch01_b001"]}],"local_bindings":[{"surface":"...","block_id":"bk_ch01_b001","target_entity_id":"ent2_supplied","support_block_ids":["bk_ch01_b001"]}],"tickets":[{"ticket_type":"profile_description_conflict|same_name_collision|alias_collision|kind_conflict|ambiguous_local_binding|unregistered_local_referent|importance_review|possible_duplicate|glossary_collision|unlocatable_surface|candidate_overflow|missing_salient_surface|surface_scope_review","surface":"...","block_id":"bk_ch01_b001","related_entity_ids":["ent2_supplied"],"note":"..."}]}

### literary_registry_audit_v1_1 (bounded chapter exception Auditor)

> - Prompt version: literary_registry_audit_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive B0_GIST, one bounded EXCEPTION_MANIFEST, detailed cards for its connected ticket/collision components, related SOURCE_BLOCKS and code-located span manifests, the WORKING_REGISTRY_REVISION_HASH, and chapter-wide counts and content hashes of clean B1 rows. You do not receive unrelated full profiles, a full occurrence inventory, relation/event history, another model's answer key, or a tool loop.
> - Audit ONLY the supplied exception rows and open tickets. Clean B1 rows are represented by counts and hashes only; do not invent dispositions for them. The typed disposition lists must exact-cover the corresponding exception entity, alias, glossary, and local-binding ids, and ticket_dispositions must exact-cover open ticket ids. Copy every supplied id exactly once. Foreign, duplicate, or omitted ids are forbidden.
> - entity_dispositions action is confirm, reject_noise, merge_provisional, or remain_pending. confirm requires a stable named referent, supported kind, and sufficient source support; unknown cannot be confirmed. reject_noise is for generic, unindividualized, or non-salient rows. remain_pending preserves uncertainty without inventing an identity.
> - merge_provisional consumes one current-chapter provisional entity and may target another supplied current-chapter provisional entity or one supplied confirmed entity. It appends accepted identity support to that target but never rewrites prior history. Shared identity does NOT prove that the merged-away canonical surface is a name or global alias; code routes that surface independently after the merge. Never merge or split two entities that were both confirmed before this chapter. merge_target_entity_id is required only for merge_provisional and otherwise null.
> - revised_identity_summary on an entity disposition is optional and applies only to that provisional entity. Keep it compact, stable, and discriminating; exclude temporary action, mood, evolving relationship state, phase, and narrator opinion.
> - alias_dispositions and local_binding_dispositions use confirm, reject, or remain_pending. A global alias must be a supported proper name, nickname, or stable title-plus-name form. A form of address, role phrase, description, epithet, or referential insult must be rejected as a global alias even when it is the only handle for an unnamed referent; preserve it through a supplied block-local advisory binding or scope ticket. Same-name entities may remain separate; a multimap collision is not automatically an error.
> - glossary_dispositions uses confirm, reject_noise, or remain_pending. Confirm only translation-sensitive items, not generic common nouns. Do not author a translation policy.
> - ticket_dispositions uses resolve or carry. resolve requires a resolution_note grounded in the supplied component; carry preserves an unresolved collision or ambiguity. Every owned ticket appears exactly once even when its related row is rejected or merged.
> - profile_revisions may revise a supplied existing entity only when an owned profile_description_conflict or kind_conflict ticket and cited source blocks support the revision. resolved_ticket_ids must be supplied owned ticket ids. Do not revise an unrelated profile.
> - An unnamed referent may remain a valid entity with zero global aliases. A descriptor-only unresolved referent never becomes a global entity. Do not mint ids, create unsupplied rows, call tools, request more context, alter clean rows, emit events/relations/phases, or issue commit instructions. Code verifies exact cover, independently routes every alias candidate by scope, and performs the atomic commit.
> - Empty typed disposition lists are valid when that row type is absent. This prompt is not called at all when the exception manifest is empty.
> - Required JSON shape: {"entity_dispositions":[{"entity_id":"ent2_supplied","action":"confirm|reject_noise|merge_provisional|remain_pending","merge_target_entity_id":null,"revised_identity_summary":null}],"alias_dispositions":[{"alias_id":"als2_supplied","action":"confirm|reject|remain_pending"}],"glossary_dispositions":[{"glossary_id":"gls2_supplied","action":"confirm|reject_noise|remain_pending"}],"local_binding_dispositions":[{"binding_id":"bind2_supplied","action":"confirm|reject|remain_pending"}],"ticket_dispositions":[{"ticket_id":"tick2_supplied","action":"resolve|carry","resolution_note":"..."}],"profile_revisions":[{"entity_id":"ent2_supplied","revised_identity_summary":"...","resolved_ticket_ids":["tick2_supplied"]}]}

### literary_chapter_entity_inventory_auditor_exp_v1_2 (full-chapter candidate Auditor experiment)

> - Prompt version: literary_chapter_entity_inventory_auditor_exp_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive the source blocks of ONE complete chapter exactly once, a compact roster of entity, glossary, and unresolved candidates produced by an earlier inventory model, and a code-derived AUDIT_CASE_MANIFEST. The candidates and case hints are untrusted proposals, not an answer key. You receive no gold labels, future chapter facts, production registry mutation capability, or tool loop.
> - Review every supplied candidate id exactly once in the matching decision list. Copy candidate ids and source block ids exactly. Never invent an id, block, entity, alias, term, relation, event, translation, or fact merely to make the inventory look complete. When source evidence is insufficient, preserve pending or dormant state.
> - entity_decisions must exact-cover the supplied entity roster. confirm_as_is accepts the supplied stable identity fields and retained aliases. confirm_with_patch changes only the explicitly supplied patch fields. keep_pending and keep_dormant preserve uncertainty. reject_candidate removes only this uncommitted proposal. merge_into_candidate is allowed only between two supplied candidates when the chapter establishes shared identity; spelling alone, one matching surname, one title, or candidate uniqueness is insufficient. open_split_ticket records a suspected conflation but does not create entities.
> - Follow the closed action/scope matrix. confirm_as_is and confirm_with_patch use global or block_local. keep_pending and open_split_ticket use pending. keep_dormant uses dormant. reject_candidate and merge_into_candidate use not_published. A candidate carrying any supplied audit_reasons may not use confirm_as_is; resolve it with confirm_with_patch or preserve/reject it. merge_into_candidate targets another supplied candidate that is confirmed in this response. All other actions use a null target_candidate_id.
> - confirm_as_is carries no field update, uses keep for both patch operations, and retains every supplied alternative name. confirm_with_patch must either change at least one field/retained alias or resolve a supplied audit reason through publication scope. keep_pending, keep_dormant, reject_candidate, merge_into_candidate, and open_split_ticket carry no field updates and retain every supplied alternative name unchanged.
> - A global entity needs a stable proper name, stable nickname, or title-plus-name. An important unnamed referent may be block_local or dormant with zero global aliases. Pronouns, ordinary forms of address, reusable roles, descriptions, epithets, insults, and kinship phrases are not global aliases. retained_alternative_name_surfaces may contain only supplied alternatives that the source establishes as stable names for that same referent.
> - A name visible only inside an inscription, sign, document, title, quotation, or other represented artifact does not by itself establish an active person in the chapter. Keep such a candidate pending or dormant unless the surrounding source establishes the referent strongly enough.
> - referent_kind and referential_gender are separate. Gender applies only to person, animal, nonhuman_character, group_reference, or unknown. A place, object, or institution has no referential gender; clear the field instead of assigning neutral. Contextual evidence may support a decision, but a case hint or block address alone never proves the claim.
> - identity_summary is a compact, stable, discriminating description. Remove temporary location, action, mood, possession-in-one-scene, evolving relationship state, narrator opinion, and unsupported biography unless the detail is identity-defining across the work. Do not add event, phase, address, or translation policy.
> - glossary_decisions must exact-cover the supplied glossary roster. Confirm only translation-sensitive, culturally marked, regional, technical, place, object, or institutional terms. Generic common nouns are not terms merely because they occur repeatedly. Do not author a target translation.
> - unresolved_decisions must exact-cover the supplied unresolved roster. keep_dormant is the normal safe outcome for a real but unnamed or locally scoped referent. open_promotion_ticket records that later evidence may justify a separate entity; it does not publish one. reject_noise requires source-grounded reason. Never force a dormant row into the global registry to reduce uncertainty.
> - AUDIT_CASE_MANIFEST narrows attention but is not exhaustive. Scan the compact roster for semantic defects that code cannot detect. additional_issue_tickets may report a high-salience omitted candidate, possible duplicate, wrong kind, wrong gender, wrong publication scope, non-durable summary, or glossary termhood problem. A missing-candidate ticket must copy one verbatim surface and source block; it never creates the candidate.
> - Every decision cites one or more supplied source blocks that support that decision. Candidate packets contain suggested_support_block_ids for convenience, but the complete supplied chapter is the evidence boundary: you may cite another supplied chapter block when it materially supports the decision. Never cite a foreign block. Evidence rows are not votes, and repeated mention does not outweigh contradictory evidence.
> - canonical_name_class_operation and referential_gender_operation are keep, clear, or replace. For keep, the matching update value is null. For clear, the matching update value is null. For replace, the matching update value is non-null. canonical_surface_update, referent_kind_update, and identity_summary_update are null when unchanged. target_candidate_id is non-null only for merge_into_candidate. source rows never mint persistent ids.
> - Required JSON shape: {"chapter_id":"...","entity_decisions":[{"candidate_id":"b0ent_supplied","action":"confirm_as_is|confirm_with_patch|keep_pending|keep_dormant|reject_candidate|merge_into_candidate|open_split_ticket","target_candidate_id":null,"canonical_surface_update":null,"canonical_name_class_operation":"keep|clear|replace","canonical_name_class_value":null,"referent_kind_update":null,"referential_gender_operation":"keep|clear|replace","referential_gender_value":null,"identity_summary_update":null,"retained_alternative_name_surfaces":[],"publication_scope":"global|block_local|dormant|pending|not_published","source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"glossary_decisions":[{"candidate_id":"b0gls_supplied","action":"confirm_as_is|confirm_with_patch|keep_pending|reject_noise|merge_into_candidate","target_candidate_id":null,"category_update":null,"short_description_update":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"unresolved_decisions":[{"candidate_id":"b0unr_supplied","action":"keep_dormant|keep_pending|reject_noise|open_promotion_ticket","source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"additional_issue_tickets":[{"target_type":"entity|glossary|unresolved|chapter","target_candidate_id":null,"issue_code":"missing_candidate|possible_duplicate|possible_wrong_kind|possible_wrong_gender|possible_wrong_scope|summary_not_durable|glossary_termhood|other","surface":null,"source_block_ids":["bk_ch01_b001"],"note":"..."}]}

### literary_cross_chapter_entity_auditor_v1 (bounded whole-book identity component)

> - Prompt version: literary_cross_chapter_entity_auditor_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive exactly ONE code-built cross-chapter conflict component. The component contains only supplied candidate claims, chapter-scoped surface decisions, optional short gists when an existing gist artifact was supplied, and source blocks cited by those claims. It is not the whole book, a complete cast list, or an answer key.
> - Decide only whether the supplied candidate proposals should remain distinct, merge, split by their already supplied source blocks, remain pending, or be rejected as unsupported. Spelling, one surname, one title, one shared description, one matching kind, or candidate uniqueness alone never proves shared identity.
> - candidate_actions must exact-cover every supplied candidate_ref once. keep preserves a distinct candidate. merge_into may target only a candidate marked keep in this response. pending preserves uncertainty. reject closes only an unsupported proposal. split is allowed only when one supplied candidate appears conflated: split_partitions must non-overlap and exact-cover that candidate's supplied source_block_ids.
> - A split partition may copy only surfaces already supplied for that candidate and supported in the partition blocks. A split never creates a confirmed entity: code materializes every partition as pending and mints all persistent ids.
> - surface_actions must exact-cover every supplied surface_case_id once. promote_book_global is allowed only when the same kept target is supported in every supplied chapter where that surface was observed. retain_chapter_scoped preserves the supplied chapter-local bindings without extending their authority. quarantine keeps the surface only as a non-authoritative retrieval lead.
> - A local action formerly named bind_global proves at most chapter-scoped use. Never treat a chapter-local binding as book-wide evidence by itself. Reusable titles, forms of address, roles, descriptions, epithets, insults, pronouns, and family names may refer to different entities across chapters; retain chapter scope or quarantine when evidence is not conclusive.
> - source_block_ids and partition source_block_ids must be copied from the supplied component. Every resolution note must state the evidence distinction or uncertainty without adding biography. Repetition is provenance, not a vote.
> - Never invent or rewrite a candidate_ref, surface, source block, chapter, name, kind, gender, summary, fact, relationship, event, translation, persistent entity id, first-supported position, or production mutation. Never ask for tools or more context. If supplied evidence is insufficient, use pending, retain_chapter_scoped, or quarantine.
> - target_candidate_ref is non-null only for merge_into and promote_book_global. split_partitions is non-empty only for split. retained_surfaces is empty unless the action is split. Code validates exact cover, derives first_supported_block_id, mints ids, and atomically publishes the registry after all components are resolved.
> - Required JSON shape: {"component_id":"bicomp_supplied","candidate_actions":[{"candidate_ref":"bkcand_supplied","action":"keep|merge_into|split|pending|reject","target_candidate_ref":null,"split_partitions":[{"source_block_ids":["bk_ch01_b001"],"retained_surfaces":["Supplied Surface"],"resolution_note":"..."}],"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"surface_actions":[{"surface_case_id":"bisurf_supplied","action":"promote_book_global|retain_chapter_scoped|quarantine","target_candidate_ref":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}]}

### literary_cross_chapter_prior_claim_auditor_v2 (bounded stable-claim component)

> - Prompt version: literary_cross_chapter_prior_claim_auditor_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive exactly ONE code-built prior-claim component. It contains only supplied stable prior cards, challenge tickets, optional short gists for involved chapters, and a bounded evidence closure from immutable source blocks. It is not the whole book, a full registry, an answer key, or a vote tally.
> - Decide only the disputed stable field named by each ticket. Do not decide unrelated identity, alias, event, relationship, translation, or narrative questions. ticket_actions must exact-cover every supplied ticket_id once.
> - retain_prior keeps the supplied prior_claim_value. revise_claim replaces only the disputed field under the closed field rules below. pending preserves uncertainty. refer_identity_conflict reports that the evidence indicates a different or conflated referent and delegates identity work to the separate identity Auditor.
> - For disputed_field referent_kind, revise_claim requires one supplied allowed referent-kind value. For referential_gender, revise_claim requires one supplied allowed gender value or null to clear the field. For identity_summary, revise_claim may only use null to invalidate an unsupported summary; never author a replacement summary in V1.
> - revised_value must be null for retain_prior, pending, and refer_identity_conflict. A revise_claim value must differ from the supplied prior_claim_value. Never revise a field outside the ticket or transfer a value between tickets.
> - pending_reason_code is required only for pending. Use conflicting_evidence when attributable evidence supports incompatible values; evidence_not_attributable when the text cannot safely connect the evidence to this referent; insufficient_context when the bounded closure lacks the material needed to decide; and model_uncertain only for residual uncertainty not described by the other reasons. Use null for retain_prior, revise_claim, and refer_identity_conflict.
> - source_block_ids must be copied from the supplied evidence closure and materially support the action. Evidence roles such as anchor, bridge, direct, and neighbour describe deterministic retrieval only; they are not conclusions. Repetition is provenance, not a vote.
> - A pronoun-only or descriptor-only block does not establish identity by itself. Use the supplied anchor and contiguous bridge together. If the bounded evidence does not establish that the claim and challenge concern the same referent, use pending or refer_identity_conflict instead of guessing.
> - Never invent or rewrite a ticket id, prior-card id, source block, chapter, field, allowed value, source text, fact, relationship, event, translation, persistent entity id, effective position, or production mutation. Never ask for tools or more context. Code validates exact cover, keeps prior history, derives effective support positions, and publishes any later generation atomically.
> - Required JSON shape: {"component_id":"bclaimcomp1_supplied","ticket_actions":[{"ticket_id":"bclaimtk1_supplied","action":"retain_prior|revise_claim|pending|refer_identity_conflict","revised_value":null,"source_block_ids":["bk_ch01_b001"],"pending_reason_code":"conflicting_evidence|evidence_not_attributable|insufficient_context|model_uncertain|null","resolution_note":"..."}]}

### literary_cross_chapter_prior_claim_auditor_batch_v2 (bounded transport batch)

> - Prompt version: literary_cross_chapter_prior_claim_auditor_batch_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one bounded TRANSPORT BATCH containing two or more independent prior-claim components. Batching only avoids repeated prompt and chapter context. It does not merge components, transfer authority, or make one component evidence for another.
> - component_results must exact-cover every supplied component_id once. Within each component result, ticket_actions must exact-cover every ticket_id owned by that component once. Never move a ticket, prior card, source block, claim, or conclusion between components.
> - Decide only the disputed stable field named by each ticket. Do not decide unrelated identity, alias, event, relationship, translation, or narrative questions.
> - retain_prior keeps the supplied prior_claim_value. revise_claim replaces only the disputed field under the closed field rules below. pending preserves uncertainty. refer_identity_conflict reports that the evidence indicates a different or conflated referent and delegates identity work to the separate identity Auditor.
> - For disputed_field referent_kind, revise_claim requires one supplied allowed referent-kind value. For referential_gender, revise_claim requires one supplied allowed gender value or null to clear the field. For identity_summary, revise_claim may only use null to invalidate an unsupported summary; never author a replacement summary in V1.
> - revised_value must be null for retain_prior, pending, and refer_identity_conflict. A revise_claim value must differ from the supplied prior_claim_value. Never revise a field outside its ticket.
> - pending_reason_code is required only for pending. Use conflicting_evidence when attributable evidence supports incompatible values; evidence_not_attributable when attribution to this referent is unsafe; insufficient_context when the bounded closure lacks material evidence; and model_uncertain only for residual uncertainty. Use null for every non-pending action.
> - source_block_ids must be copied from the supplied source-block catalog, must belong to the same component and ticket, and must materially support the action. Evidence roles describe deterministic retrieval only; they are not conclusions. Repetition is provenance, not a vote.
> - A pronoun-only or descriptor-only block does not establish identity by itself. Use the supplied anchor and contiguous bridge together. If the bounded evidence does not establish that the prior claim and challenge concern the same referent, use pending or refer_identity_conflict instead of guessing.
> - Judge every component independently even when components share a chapter. Do not force consistent actions across components merely because their source blocks are nearby.
> - Never invent or rewrite a batch id, component id, ticket id, prior-card id, source block, chapter, field, allowed value, source text, fact, relationship, event, translation, persistent entity id, effective position, or production mutation. Never ask for tools or more context.
> - Required JSON shape: {"batch_id":"bclaimbatch1_supplied","component_results":[{"component_id":"bclaimcomp1_supplied","ticket_actions":[{"ticket_id":"bclaimtk1_supplied","action":"retain_prior|revise_claim|pending|refer_identity_conflict","revised_value":null,"source_block_ids":["bk_ch01_b001"],"pending_reason_code":"conflicting_evidence|evidence_not_attributable|insufficient_context|model_uncertain|null","resolution_note":"..."}]}]}

### literary_incremental_identity_surface_auditor_v1 (bounded reversible identity hearing)

> - Prompt version: literary_incremental_identity_surface_auditor_v1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive exactly ONE code-built identity component whose evidence manifest changed since its last hearing. It contains candidate cards, queued review leads, any prior hearing summaries, and bounded source blocks from only the involved chapters. It is not the whole book, a complete cast list, or an answer key.
> - candidate_actions must exact-cover every supplied prior_card_id once. keep preserves that candidate as a distinct provisional identity. link_to records a reversible same-identity link to another candidate marked keep in this response. pending preserves uncertainty. A link is never a book-global merge or permission to erase either candidate.
> - Same spelling, one surname, one title, one matching kind or gender, lexical proximity, repetition, or the absence of another candidate never proves shared identity. Use narrative role, time, attributed action, relationship, or an explicit naming statement only when the supplied blocks support it.
> - keep is appropriate when supplied evidence distinguishes candidates, including a historical inscription versus a living participant. link_to is appropriate only when supplied evidence positively attributes both candidate proposals to the same referent. Use pending when attribution or distinction remains insufficient.
> - target_prior_card_id is non-null only for link_to and must name a candidate marked keep in the same response. keep and pending require null. Every action must cite one or more supplied source_block_ids that materially support the action.
> - resolution_note must explain the supplied distinction, link, or uncertainty without inventing biography. Repeated evidence is provenance, not a vote. A prior hearing is context, not authority; new evidence may overturn it.
> - Never invent or rewrite a component id, prior-card id, surface, source block, chapter, kind, gender, summary, fact, relationship, event, translation, persistent entity id, global alias, first-supported position, or production mutation. Never ask for tools or more context. If the bounded closure is insufficient, use pending.
> - Code validates exact cover, hashes evidence, suppresses replay of unchanged evidence, keeps candidate histories append-only, and decides whether a later chapter may reopen the component. This response grants no book-wide authority.
> - Required JSON shape: {"component_id":"incidcomp1_supplied","candidate_actions":[{"prior_card_id":"pcard1_supplied","action":"keep|link_to|pending","target_prior_card_id":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}]}

### literary_incremental_identity_surface_auditor_v2 (bounded identity and surface-scope hearing)

> - Prompt version: literary_incremental_identity_surface_auditor_v2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive exactly ONE code-built component whose evidence manifest changed since its last hearing. It contains one or more candidate cards, queued review leads, any prior hearing summaries, and bounded source blocks from only the involved chapters. It is not the whole book, a complete cast list, or an answer key.
> - candidate_actions must exact-cover every supplied prior_card_id once. keep preserves that candidate as a distinct provisional identity. link_to records a reversible same-identity link to another candidate marked keep in this response. pending preserves uncertainty. A link is never a book-global merge or permission to erase either candidate.
> - Same spelling, one surname, one title, one matching kind or gender, lexical proximity, repetition, or the absence of another candidate never proves shared identity. Use narrative role, time, attributed action, relationship, or an explicit naming statement only when the supplied blocks support it.
> - A component may contain one candidate when the hearing concerns only the scope of one proposed surface. In that case candidate_actions still exact-covers the candidate; keep does not approve the surface.
> - surface_scope_actions must exact-cover every supplied review lead whose disputed_field is alias_scope or alias_target. confirm_block_scope permits the surface only in valid_block_ids. confirm_chapter_scope permits it only in the chapter or chapters represented by the cited source blocks. nominate_book_candidate records a non-authoritative candidate for book-end review; it does not create book-global authority. keep_pending preserves the open question and states the missing evidence. dismiss_dormant removes active authority and waits for materially new evidence before reopening.
> - A reusable form of address, role, description, kinship expression, epithet, insult, or pronoun must not receive chapter or book scope merely because it refers to one person in one scene. Prefer block scope or pending when ownership changes with context. A stable proper name, stable nickname, or title-plus-name may be nominated for wider scope only when the supplied evidence supports that scope.
> - target_prior_card_id is required for confirm_block_scope, confirm_chapter_scope, and nominate_book_candidate and must name one candidate in this component. It is null for keep_pending and dismiss_dormant. valid_block_ids is non-empty only for confirm_block_scope and must be copied from the component source blocks.
> - evidence_needed is non-null only for keep_pending and must be one of explicit_naming, cross_chapter_recurrence, referent_attribution, scope_disambiguation, identity_resolution, or additional_source_context. All other actions use null.
> - Every candidate and surface action cites one or more supplied source_block_ids that materially support the decision. resolution_note explains the supplied distinction, link, scope, dormancy, or uncertainty without inventing biography. Repeated evidence is provenance, not a vote. A prior hearing is context, not authority; materially new evidence may overturn it.
> - Never invent or rewrite a component id, review item id, prior-card id, surface, source block, chapter, kind, gender, summary, fact, relationship, event, translation, persistent entity id, global alias, first-supported position, or production mutation. Never ask for tools or more context. If the bounded closure is insufficient, use pending or keep_pending.
> - Code validates exact cover, hashes evidence, suppresses replay of unchanged evidence, keeps histories append-only, applies hearing limits, and decides whether a later chapter may reopen the case. This response grants no book-wide authority.
> - Required JSON shape: {"component_id":"incidcomp1_supplied","candidate_actions":[{"prior_card_id":"pcard1_supplied","action":"keep|link_to|pending","target_prior_card_id":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}],"surface_scope_actions":[{"review_item_id":"cycrev1_supplied","action":"confirm_block_scope|confirm_chapter_scope|nominate_book_candidate|keep_pending|dismiss_dormant","target_prior_card_id":null,"valid_block_ids":[],"source_block_ids":["bk_ch01_b001"],"evidence_needed":"explicit_naming|cross_chapter_recurrence|referent_attribution|scope_disambiguation|identity_resolution|additional_source_context|null","resolution_note":"..."}]}

### literary_b1_scan_v1_2 (lightweight chapter entity scan)

> - Prompt version: literary_b1_scan_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read every supplied SOURCE_BLOCK in author order. This is a lightweight scan, not a dossier builder. Record source-grounded entity surfaces and translator-relevant terms; do not write gender, age, occupation, residence, relationships, biography, aliases, canonical identity, events, dialogue attribution, or translation policy.
> - entity_observations includes named people, individualized animals or nonhuman characters, named groups, places, institutions, named texts, and individualized important objects. It may also preserve a materially important unnamed referent when losing that referent would damage later understanding.
> - Copy surface exactly as it occurs in every declared source block. source_block_ids for an observation may contain ONLY blocks that contain that exact surface; do not add a later pronoun, synonym, paraphrase, or coreferential block to that observation. Later builders may add broader referential support. Use one row for one coherent surface observation; the same surface may appear in multiple rows when the chapter itself supports distinct possible referents or distinct presence classes. Never invent an id.
> - record_class is named_entity_candidate when the text presents a usable named referent; important_unnamed_referent when an individualized unnamed referent matters but must remain chapter-local; unresolved_named_reference when a name appears but the text does not establish what kind of referent it denotes. When a name appears only on a plaque, signature, inscription, list, document, memorial, or quoted record and the chapter supplies no independent evidence of its referent kind, you MUST use unresolved_named_reference with referent_kind_claim=unknown. A person-shaped name is not evidence that the referent is a person.
> - presence_basis is direct_presence when the referent participates in the current narrated action or speech; referenced_by_other when another participant mentions it; reported_only when the chapter only reports it; inscription_or_document when it occurs only in written or quoted material; unclear when the supplied chapter cannot establish the mode.
> - Pronouns, ordinary forms of address, reusable role descriptions, kinship words, insults, and generic nouns are evidence, not standalone entity candidates. Include one only when it is necessary to preserve a materially important individualized unnamed referent; then use important_unnamed_referent and keep the observation local. Do not collect every he, she, I, you, sir, madam, master, mother, father, servant, dog, house, or similar reusable expression.
> - For important unnamed referents, include an individualized person, animal, or nonhuman participant who directly causes, undergoes, prevents, ends, or materially changes a confrontation or other consequential current-chapter action. Omit unnamed background occupations, generic imagined examples, and brief remembered relatives or acquaintances when they do not participate in the chapter's current action and are not needed to distinguish a continuing referent. Importance is about narrative function, not unusual wording.
> - glossary_observations is separate from referential entities. Include only a term whose local sense is genuinely opaque, culturally marked, regional, technical, or non-compositional enough that a later translator may misread it without a glossary. Ordinary furniture, clothing, food, weapons, scenery, and common descriptive compounds are not glossary rows merely because they are concrete nouns. Do not propose a translation or duplicate a named referent merely because its spelling may matter to translation.
> - PRIOR_CANDIDATE_PACKETS are selected mechanically because one of their stable surfaces appears in this chapter. They are candidates, not answers. Each card is shown once beside all current hit blocks; do not infer identity from candidate count, order, spelling, surname, title, or retrieval alone.
> - prior_continuity_proposals must exact-cover every supplied prior_card_id once. Use propose_continue only when the current evidence is compatible with the same referent. Use propose_distinct only when stable identity evidence makes the same referent untenable. Use uncertain when evidence is insufficient, the prior row is only an inscription or unresolved reference, or two readings remain plausible.
> - Temporary changes in age, mood, activity, location, title, occupation, relationship, or social role are not by themselves hard identity contradictions. Do not merge or split identities, create an alias, mutate a registry, or call an Auditor. Code routes propose_distinct and uncertain to a later hearing after enrichment.
> - scan_note is one short source-grounded explanation of what this observation is and why its class/presence basis was chosen. It is not an identity summary and must not add unsupported facts.
> - Empty entity or glossary lists are valid when the chapter supports none. Do not omit a clear salient referent merely to keep output short.
> - Required JSON shape: {"schema_id":"LiteraryB1ScanOutputV1","chapter_id":"bk_ch01","entity_observations":[{"surface":"Avery Cole","source_block_ids":["bk_ch01_b001"],"referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|named_text|unknown","record_class":"named_entity_candidate|important_unnamed_referent|unresolved_named_reference","presence_basis":"direct_presence|referenced_by_other|reported_only|inscription_or_document|unclear","scan_note":"..."}],"glossary_observations":[{"surface":"...","source_block_ids":["bk_ch01_b002"],"category_hint":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other"}],"prior_continuity_proposals":[{"prior_card_id":"pcard_supplied","verdict":"propose_continue|propose_distinct|uncertain","reason_code":"consistent_current_reference|hard_identity_contradiction|same_surface_multiple_referents|prior_reference_not_established_entity|current_reference_not_established_entity|temporal_change_possible|insufficient_evidence|other","source_block_ids":["bk_ch01_b001"],"reason":"..."}]}

### literary_b1_enrich_v1_0 (second-pass identity enrichment)

> - Prompt version: literary_b1_enrich_v1_0.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. ENTITY_TASKS are provisional observations from B1-Scan, not settled entities. For every supplied scan_observation_id, emit exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character; do not assign gender to a place, object, institution, group, document, or glossary term.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly.
> - role_or_occupation is conditional: emit it only when the chapter supports it. Exact numeric age wording belongs verbatim in story_time_note; never calculate an age or age band from a date.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - address_forms_used is stored on the speaker's dossier: counterpart_ref, mode=to|about, exact English form, and anchors. An address form is evidence, not an alias. Use task_ref values supplied in ENTITY_TASKS when referring to another scanned entity.
> - kinship_links and links are source-grounded proposals. links may use resides_at|owned_by|held_by|part_of|member_of|head_of|serves_in. Treat every such link as true only as of this chapter; do not grant book-wide permanence. For other_kin, relation_note is mandatory. Do not infer or emit reverse links merely for completeness.
> - additional_entities is the recall backstop. Add a genuinely omitted named entity or an individualized unnamed person, animal, or nonhuman participant whose current-chapter participation materially changes a confrontation or consequential action. Copy its surface exactly and build the same dossier. Do not add generic background roles, imagined examples, dates, headings, or ordinary nouns.
> - Use spurious_challenges when a Scan observation is not a usable entity candidate, including a bare date/number, decorative text, generic phrase, or incidental remembered referent with no registry value. A challenge is not a deletion; a later Local Auditor decides.
> - If supplied prior information conflicts with chapter evidence, emit conflict_findings instead of resolving it. If Scan presence_basis is wrong, emit presence_correction_findings instead of overwriting it. Do not merge, split, publish, or mutate a registry.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"bk_ch01","entities":[{"scan_observation_id":"b1obs_supplied","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["bk_ch01_b001"],"story_time_note":null}],"kinship_links":[{"relation":"parent_of|child_of|mother_of|father_of|sibling_of|spouse_of|betrothed_to|widow_of|widower_of|grandparent_of|grandchild_of|other_kin","target_ref":"scan:b1obs_supplied","basis":"explicit_textual|self_identification|contextual_inference","anchor_block_ids":["bk_ch01_b001"],"relation_note":null}],"links":[{"relation":"resides_at|owned_by|held_by|part_of|member_of|head_of|serves_in","target_ref":"scan:b1obs_supplied","basis":"explicit_textual|self_identification|contextual_inference","anchor_block_ids":["bk_ch01_b001"]}],"address_forms_used":[{"counterpart_ref":"scan:b1obs_supplied","mode":"to|about","form":"exact form","anchor_block_ids":["bk_ch01_b001"]}],"aliases_observed":[{"surface":"exact stable name","anchor_block_ids":["bk_ch01_b001"]}],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[{"term_observation_id":"b1term_supplied","contextual_sense":"...","ambiguity_status":"clear|ambiguous|unclear","source_block_ids":["bk_ch01_b002"]}]}

### literary_b1_enrich_v1_1 (prejoined second-pass identity enrichment)

> - Prompt version: literary_b1_enrich_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. Every ENTITY_TASK is a provisional B1-Scan observation with one inline continuity packet. Exact-cover every scan_observation_id with exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - continuity.state=new_candidate means build from this chapter. continuity.state=continue_prior includes the one prior card that B1-Scan allowed for this task; use it only as continuity context and emit only new or changed source-grounded claims. continuity.state=linkage_pending intentionally withholds prior dossiers: build independently from current chapter evidence, do not infer what was withheld, and do not decide same-versus-distinct identity.
> - A prior card is a candidate, not an answer. If a continue_prior card conflicts with current evidence, emit conflict_findings instead of silently choosing either value. The runner retains continuity_case_ids for the later cross-chapter hearing.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly. role_or_occupation is conditional; exact numeric age wording belongs verbatim in story_time_note and must not be calculated into a band.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - address_forms_used is stored on the speaker's dossier and is evidence, not an alias. kinship_links and links are source-grounded, chapter-as-of proposals. Use only supplied task_ref values for counterparts and targets; do not infer reverse links merely for completeness.
> - additional_entities is a recall backstop for a genuinely omitted named entity or individualized participant. spurious_challenges disputes a Scan task but never deletes it; the Local Auditor decides. If supplied continued information conflicts with chapter evidence, emit conflict_findings. If Scan presence_basis is wrong, emit presence_correction_findings.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"bk_ch01","entities":[{"scan_observation_id":"b1obs_supplied","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["bk_ch01_b001"],"story_time_note":null}],"kinship_links":[],"links":[],"address_forms_used":[],"aliases_observed":[],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[]}


### literary_b1_enrich_v1_2 (prejoined second-pass identity enrichment)

> - Prompt version: literary_b1_enrich_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. Every ENTITY_TASK is a provisional B1-Scan observation with one inline continuity packet. Exact-cover every scan_observation_id with exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - continuity.state=new_candidate means build from this chapter. continuity.state=continue_prior includes the one prior card that B1-Scan allowed for this task; use it only as continuity context and emit only new or changed source-grounded claims. continuity.state=linkage_pending intentionally withholds prior dossiers: build independently from current chapter evidence, do not infer what was withheld, and do not decide same-versus-distinct identity.
> - A prior card is a candidate, not an answer. If a continue_prior card conflicts with current evidence, emit conflict_findings instead of silently choosing either value. The runner retains continuity_case_ids for the later cross-chapter hearing.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly. role_or_occupation is conditional; exact numeric age wording belongs verbatim in story_time_note and must not be calculated into a band.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - address_forms_used is stored on the speaker's dossier and is evidence, not an alias. kinship_links and links are source-grounded, chapter-as-of proposals. Use only supplied task_ref values for counterparts and targets; do not infer reverse links merely for completeness.
> - additional_entities is a recall backstop for a genuinely omitted named entity or individualized participant. spurious_challenges disputes a Scan task but never deletes it; the Local Auditor decides. If supplied continued information conflicts with chapter evidence, emit conflict_findings. If Scan presence_basis is wrong, emit presence_correction_findings.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"<the chapter_id supplied in this request>","entities":[{"scan_observation_id":"b1obs_supplied","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["<a block_id supplied in this request>"],"story_time_note":null}],"kinship_links":[],"links":[],"address_forms_used":[],"aliases_observed":[],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[]}

### literary_b1_enrich_v1_3 (prejoined enrichment with same-referent proposals)

> - Prompt version: literary_b1_enrich_v1_3.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. Every ENTITY_TASK is a provisional B1-Scan observation with one inline continuity packet. Exact-cover every scan_observation_id with exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - continuity.state=new_candidate means build from this chapter. continuity.state=continue_prior includes the one prior card that B1-Scan allowed for this task; use it only as continuity context and emit only new or changed source-grounded claims. continuity.state=linkage_pending intentionally withholds prior dossiers: build independently from current chapter evidence, do not infer what was withheld, and do not decide cross-chapter identity.
> - A prior card is a candidate, not an answer. If a continue_prior card conflicts with current evidence, emit conflict_findings instead of silently choosing either value. The runner retains continuity_case_ids for the later cross-chapter hearing.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly. role_or_occupation is conditional; exact numeric age wording belongs verbatim in story_time_note and must not be calculated into a band.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - same_referent_proposals is a proposal channel, never a decision. Use it only when an important_unnamed_referent such as a chapter-local description is linked by the supplied chapter evidence to one named_entity_candidate already present in ENTITY_TASKS. Put the unnamed observation in subject_scan_observation_id and the named observation in target_scan_observation_id, use proposal_basis=chapter_context_description, and cite evidence for both sides. Do not use this field for two names that merely overlap in spelling; code separately proposes mechanical name variants. When uncertain, emit no proposal and leave the dossiers separate.
> - A description accepted as coreferential remains evidence only. It must not become an alias, canonical surface, stable surface, or future retrieval key. The later Local Auditor alone may accept, reject, defer, or refer each supplied proposal.
> - address_forms_used is stored on the speaker's dossier and is evidence, not an alias. kinship_links and links are source-grounded, chapter-as-of proposals. Use only supplied task_ref values for counterparts and targets; do not infer reverse links merely for completeness.
> - additional_entities is a recall backstop for a genuinely omitted named entity or individualized participant. spurious_challenges disputes a Scan task but never deletes it; the Local Auditor decides. If supplied continued information conflicts with chapter evidence, emit conflict_findings. If Scan presence_basis is wrong, emit presence_correction_findings.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"<the chapter_id supplied in this request>","entities":[{"scan_observation_id":"<a supplied local scan-observation label>","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["<a block_id supplied in this request>"],"story_time_note":null}],"kinship_links":[],"links":[],"address_forms_used":[],"aliases_observed":[],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"same_referent_proposals":[{"subject_scan_observation_id":"<the supplied local label for the unnamed observation>","target_scan_observation_id":"<the supplied local label for the named observation>","proposal_basis":"chapter_context_description","source_block_ids":["<supplied block ids for both observations>"],"reason":"short source-grounded reason"}],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[]}

### literary_b1_enrich_v1_4 (transport-resolvable same-referent refs)

> - Prompt version: literary_b1_enrich_v1_4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. Every ENTITY_TASK is a provisional B1-Scan observation with one inline continuity packet. Exact-cover every scan_observation_id with exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - continuity.state=new_candidate means build from this chapter. continuity.state=continue_prior includes the one prior card that B1-Scan allowed for this task; use it only as continuity context and emit only new or changed source-grounded claims. continuity.state=linkage_pending intentionally withholds prior dossiers: build independently from current chapter evidence, do not infer what was withheld, and do not decide cross-chapter identity.
> - A prior card is a candidate, not an answer. If a continue_prior card conflicts with current evidence, emit conflict_findings instead of silently choosing either value. The runner retains continuity_case_ids for the later cross-chapter hearing.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly. role_or_occupation is conditional; exact numeric age wording belongs verbatim in story_time_note and must not be calculated into a band.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - same_referent_proposals is a proposal channel, never a decision. Use it only when an important_unnamed_referent such as a chapter-local description is linked by the supplied chapter evidence to one named_entity_candidate already present in ENTITY_TASKS. Put the unnamed task_ref in subject_ref and the named task_ref in target_ref, use proposal_basis=chapter_context_description, and cite evidence for both sides. Do not use this field for two names that merely overlap in spelling; code separately proposes mechanical name variants. When uncertain, emit no proposal and leave the dossiers separate.
> - A description accepted as coreferential remains evidence only. It must not become an alias, canonical surface, stable surface, or future retrieval key. The later Local Auditor alone may accept, reject, defer, or refer each supplied proposal.
> - address_forms_used is stored on the speaker's dossier and is evidence, not an alias. kinship_links and links are source-grounded, chapter-as-of proposals. Use only supplied task_ref values for counterparts and targets; do not infer reverse links merely for completeness.
> - additional_entities is a recall backstop for a genuinely omitted named entity or individualized participant. spurious_challenges disputes a Scan task but never deletes it; the Local Auditor decides. If supplied continued information conflicts with chapter evidence, emit conflict_findings. If Scan presence_basis is wrong, emit presence_correction_findings.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"<the chapter_id supplied in this request>","entities":[{"scan_observation_id":"<a supplied local scan-observation label>","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["<a block_id supplied in this request>"],"story_time_note":null}],"kinship_links":[],"links":[],"address_forms_used":[],"aliases_observed":[],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"same_referent_proposals":[{"subject_ref":"<the supplied task_ref for the unnamed observation>","target_ref":"<the supplied task_ref for the named observation>","proposal_basis":"chapter_context_description","source_block_ids":["<supplied block ids for both observations>"],"reason":"short source-grounded reason"}],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[]}

### literary_b1_enrich_local_auditor_v1_0 (exception-only B1-Enrich review, historical)

> - Prompt version: literary_b1_enrich_local_auditor_v1_0.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims are intentionally absent. Resolve only supplied components; never add a component, entity, fact, relation, alias, block, or id.
> - Components and cards are proposals, not answers. A matching surface, supplied card, or unique candidate does not prove identity. Use only the supplied source blocks, and copy every component_id, target ref, relation, and block id exactly when retained.
> - Exact-cover every supplied component exactly once. accept_proposal accepts the specific proposal shown in that component. reject_proposal rejects only that proposal while preserving its history. keep_pending preserves it without authority. refer_cross_chapter is for a decision that genuinely needs evidence from another chapter. It is not permitted for a purely chapter-local presence correction.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's closed relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For every other action, both revision fields are null. Never reverse a relation automatically merely to make it plausible; reject an incorrectly directed proposal unless the supplied chapter evidence supports one explicit replacement with the same subject.
> - For spurious_challenge, accept_proposal means the Scan row is dormant in this chapter; reject_proposal means retain the Scan candidate. A bare date, decorative inscription, generic expression, or unsupported referent may be challenged, but uncertainty is not proof of spuriousness.
> - For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis. For stable_claim_conflict, accept_proposal selects the newly observed value and reject_proposal retains the prior value only when supplied evidence permits that conclusion; otherwise keep pending or refer across chapters.
> - For alias_proposal, acceptance grants chapter-only evidence status. It never grants global lookup authority. Pronouns, ordinary address forms, reusable roles, kinship words, insults, and descriptions are not stable aliases merely because they refer to someone locally.
> - For additional_entity, acceptance permits only a chapter-local entity proposal. It does not merge, split, mint a book entity id, or establish book-wide identity. For glossary_ambiguity, acceptance is chapter-scoped and advisory; never prescribe one mandatory Vietnamese rendering.
> - Every decision cites at least one direct evidence block for its component. Do not cite a neighboring context block alone. Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"bk_ch01","manifest_hash":"64_hex_chars","decisions":[{"component_id":"b1lac_supplied","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_target_ref":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}]}

### literary_b1_enrich_local_auditor_v1_1 (chapter-local B1-Enrich review)

> - Prompt version: literary_b1_enrich_local_auditor_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent. Resolve only supplied components; never add a component, entity, fact, relation, alias, block, or id.
> - Components and cards are proposals, not answers. A matching surface, supplied card, or unique candidate does not prove identity. Use only the supplied source blocks, and copy every component_id, target ref, relation, and block id exactly when retained.
> - Exact-cover every supplied component exactly once. accept_proposal accepts the specific chapter-local proposal. reject_proposal rejects only that proposal while preserving its history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied local proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's closed relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For every other action, both revision fields are null.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis.
> - Alias acceptance grants chapter-only evidence status and never global lookup authority. Pronouns, ordinary address forms, reusable roles, kinship words, insults, and descriptions are not stable aliases merely because they refer to someone locally.
> - Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory. Every decision cites at least one direct evidence block for its component. Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"bk_ch01","manifest_hash":"64_hex_chars","decisions":[{"component_id":"b1lac_supplied","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_target_ref":null,"source_block_ids":["bk_ch01_b001"],"resolution_note":"..."}]}


### literary_b1_enrich_local_auditor_v1_2 (chapter-local B1-Enrich review)

> - Prompt version: literary_b1_enrich_local_auditor_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent. Resolve only supplied components; never add a component, entity, fact, relation, alias, block, or id.
> - Components and cards are proposals, not answers. A matching surface, supplied card, or unique candidate does not prove identity. Use only the supplied source blocks, and copy every component_id, target ref, relation, and block id exactly when retained.
> - Exact-cover every supplied component exactly once. accept_proposal accepts the specific chapter-local proposal. reject_proposal rejects only that proposal while preserving its history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied local proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's closed relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For every other action, both revision fields are null.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis.
> - Alias acceptance grants chapter-only evidence status and never global lookup authority. Pronouns, ordinary address forms, reusable roles, kinship words, insults, and descriptions are not stable aliases merely because they refer to someone locally.
> - Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory. Every decision cites at least one direct evidence block for its component. Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"<the chapter_id supplied in this request>","manifest_hash":"64_hex_chars","decisions":[{"component_id":"b1lac_supplied","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_target_ref":null,"source_block_ids":["<a block_id supplied in this request>"],"resolution_note":"..."}]}

---

### literary_b1_enrich_local_auditor_v1_3 (chapter-local review with identity components)

> - Prompt version: literary_b1_enrich_local_auditor_v1_3.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent.
> - Resolve only supplied components. Exact-cover every supplied component exactly once. Never create or decide an unasked component, entity, fact, relation, alias, block, or id. accept_proposal accepts only the specific chapter-local proposal shown. reject_proposal rejects only that proposal while preserving history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - Components and cards are proposals, not answers. A matching surface, overlapping name tokens, supplied card, or unique candidate does not prove identity. Use only supplied source blocks. Every decision cites at least one direct evidence block for its component.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's closed relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For every other action, both revision fields are null.
> - For same_referent_proposal, accept_proposal means the two supplied observations denote one referent in this chapter. It grants chapter-only grouping and no book-wide identity authority. reject_proposal keeps them distinct. keep_pending or refer_cross_chapter keeps both cards separate. A proposal_basis=name_variant is only a mechanical string clue; a proposal_basis=chapter_context_description is an Enrich reading. Neither is proof by itself.
> - An accepted descriptive observation remains evidence only: it is not a canonical name, alias, stable surface, or future retrieval key. An accepted named variant may become a chapter-confirmed stable name surface, but still grants no book-wide alias authority.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis. Alias acceptance grants chapter-only evidence status and never global lookup authority. Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory.
> - You may record a possible same-referent pair that was NOT supplied as a component in unasked_same_referent_observations. This is a non-authoritative note for a later pass, not a current decision. Use only two visible ENTITY_CARD refs and supplied source blocks. Do not repeat a pair already represented by a supplied same_referent_proposal. An empty list is normal.
> - Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations. In resolution_note and observation reason, use chapter surface names rather than transport labels when prose can identify the referent.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"<the chapter_id supplied in this request>","manifest_hash":"64_hex_chars","decisions":[{"component_id":"<a supplied local component label>","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_target_ref":null,"source_block_ids":["<a block_id supplied in this request>"],"resolution_note":"..."}],"unasked_same_referent_observations":[{"left_ref":"<one visible ENTITY_CARD local ref>","right_ref":"<another visible ENTITY_CARD local ref>","source_block_ids":["<a supplied block id>"],"reason":"short source-grounded observation for a later pass"}]}

---

### literary_b1_enrich_local_auditor_v1_4 (chapter-local review with transport-bound observations)

> - Prompt version: literary_b1_enrich_local_auditor_v1_4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent.
> - Resolve only supplied components. Exact-cover every supplied component exactly once. Never create or decide an unasked component, entity, fact, relation, alias, block, or id. accept_proposal accepts only the specific chapter-local proposal shown. reject_proposal rejects only that proposal while preserving history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - Components and cards are proposals, not answers. A matching surface, overlapping name tokens, supplied card, or unique candidate does not prove identity. Use only supplied source blocks. Every decision cites at least one direct evidence block for its component.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's closed relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For every other action, both revision fields are null.
> - For same_referent_proposal, accept_proposal means the two supplied observations denote one referent in this chapter. It grants chapter-only grouping and no book-wide identity authority. reject_proposal keeps them distinct. keep_pending or refer_cross_chapter keeps both cards separate. A proposal_basis=name_variant is only a mechanical string clue; a proposal_basis=chapter_context_description is an Enrich reading. Neither is proof by itself.
> - An accepted descriptive observation remains evidence only: it is not a canonical name, alias, stable surface, or future retrieval key. An accepted named variant may become a chapter-confirmed stable name surface, but still grants no book-wide alias authority.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis. Alias acceptance grants chapter-only evidence status and never global lookup authority. Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory.
> - You may record a possible same-referent pair that was NOT supplied as a component in unasked_same_referent_observations. This is a non-authoritative note for a later pass, not a current decision. Use only two visible ENTITY_CARD refs and supplied source blocks. Do not repeat a pair already represented by a supplied same_referent_proposal. An empty list is normal.
> - Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations. In resolution_note and observation reason, use chapter surface names rather than transport labels when prose can identify the referent.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"<the chapter_id supplied in this request>","manifest_hash":"64_hex_chars","decisions":[{"component_id":"<a supplied local component label>","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_target_ref":null,"source_block_ids":["<a block_id supplied in this request>"],"resolution_note":"..."}],"unasked_same_referent_observations":[{"subject_ref":"<one visible ENTITY_CARD local ref>","target_ref":"<another visible ENTITY_CARD local ref>","source_block_ids":["<a supplied block id>"],"reason":"short source-grounded observation for a later pass"}]}

---

### literary_cross_chapter_identity_hearing_v1_0 (bounded cross-chapter identity hearing)

> - Prompt version: literary_cross_chapter_identity_hearing_v1_0.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE bounded question about whether records from different chapters name the same referent. Decide ONLY from the supplied packet: the PRIOR_CARD_SNAPSHOT, the CURRENT_CARD_SNAPSHOTS and CURRENT_DOSSIER_SNAPSHOTS, the TRIGGER trail, and the SOURCE_BLOCKS. Do not use outside knowledge about any book, and do not treat any summary text as evidence.
> - Answer with exactly one verdict from ALLOWED_VERDICTS in your packet. Definitions: confirmed_distinct - the prior record and the current referent are different referents; merge_referents - one referent, and you MUST set merge_target_prior_card_id to the supplied prior_card_id and adjudicate every conflicting identity field in field_adjudications (resolution keep_prior, keep_current, or mark_disputed); alias_confirmed - the proposed surface names the same supplied prior referent; alias_rejected_distinct - it does not; dismiss_observation - the referred observation is not a usable entity record; keep_observation - it remains a usable record; insufficient_evidence - the supplied evidence cannot settle the question. Do not guess and do not average.
> - Weigh presence classes and time markers seriously: a name that appears only carved, written, quoted, or reported is not, by itself, a living participant; a referent cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - A matching surface never proves identity, and a differing surface never disproves it. Prior fields marked provisional carry no authority; treat them as unconfirmed hints.
> - Every verdict except insufficient_evidence must cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. Keep reason short and source-grounded.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, you never rewrite either dossier, and you never invent an id, alias, field, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","merge_target_prior_card_id":null,"field_adjudications":[{"field":"gender","resolution":"keep_prior|keep_current|mark_disputed","value":null,"basis":null}],"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text"}],"reason":"short source-grounded reason"}

---

### literary_cross_chapter_stable_claim_hearing_v1_0 (bounded cross-chapter stable-claim hearing)

> - Prompt version: literary_cross_chapter_stable_claim_hearing_v1_0.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE bounded conflict between an earlier chapter's stable field value (EXISTING_VALUE on FIELD of the PRIOR_CARD_SNAPSHOT) and a later chapter's observation (OBSERVED_VALUE, with the CURRENT_CARD_SNAPSHOT and CURRENT_DOSSIER_SNAPSHOT). Decide ONLY from the supplied packet and SOURCE_BLOCKS. Do not use outside knowledge about any book, and do not treat any summary text as evidence.
> - Answer with exactly one verdict from ALLOWED_VERDICTS. Definitions: uphold_existing - the new observation misreads the text and the existing value stands; correction - the earlier value was wrong when recorded, and corrected_value states the value the evidence supports; in_story_change - the value truly changed inside the story, and you MUST set effective_from_block_id to the supplied block where the new value takes effect; reveal_only - the value was always true and the story reveals it here, and you MUST set revealed_at_block_id to the supplied revealing block, with earlier chapters not knowing it; split_referent - the conflict exists because these are two different referents, so no single field value is at issue; insufficient_evidence - the supplied evidence cannot settle the conflict. Do not guess and do not average.
> - Prior fields marked provisional carry no authority. A person cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - Every verdict except insufficient_evidence must cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. Keep reason short and source-grounded.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, and you never invent an id, field, value, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","effective_from_block_id":null,"revealed_at_block_id":null,"corrected_value":null,"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text"}],"reason":"short source-grounded reason"}

---

### literary_cross_chapter_identity_hearing_v1_1 (bounded cross-chapter identity hearing)

> - Prompt version: literary_cross_chapter_identity_hearing_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE bounded question about whether records from different chapters name the same referent. Decide ONLY from the supplied packet: the PRIOR_CARD_SNAPSHOT, the CURRENT_CARD_SNAPSHOTS and CURRENT_DOSSIER_SNAPSHOTS, the TRIGGER trail, and the SOURCE_BLOCKS. Do not use outside knowledge about any book, and do not treat any summary text as evidence.
> - Answer with exactly one verdict from ALLOWED_VERDICTS in your packet. Definitions: confirmed_distinct - the prior record and the current referent are different referents; merge_referents - one referent, and you MUST set merge_target_prior_card_id to the supplied prior_card_id and adjudicate every conflicting identity field in field_adjudications (resolution keep_prior, keep_current, or mark_disputed); alias_confirmed - the proposed surface names the same supplied prior referent; alias_rejected_distinct - it does not; dismiss_observation - the referred observation is not a usable entity record; keep_observation - it remains a usable record; insufficient_evidence - the supplied evidence cannot settle the question. Do not guess and do not average.
> - Weigh presence classes and time markers seriously: a name that appears only carved, written, quoted, or reported is not, by itself, a living participant; a referent cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - A matching surface never proves identity, and a differing surface never disproves it. Prior fields marked provisional carry no authority; treat them as unconfirmed hints.
> - Every verdict, including insufficient_evidence, MUST cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. Keep reason short and source-grounded.
> - If verdict is insufficient_evidence, resolution_condition MUST be a concise, specific description of the future source evidence that would settle this exact question. Do not claim that the missing evidence is already present, and do not invent a future block id. For every other verdict, resolution_condition MUST be null.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, you never rewrite either dossier, and you never invent an id, alias, field, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","merge_target_prior_card_id":null,"field_adjudications":[{"field":"gender","resolution":"keep_prior|keep_current|mark_disputed","value":null,"basis":null}],"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text"}],"reason":"short source-grounded reason","resolution_condition":"what future evidence would settle the case, or null when decided"}

---

### literary_cross_chapter_stable_claim_hearing_v1_1 (bounded cross-chapter stable-claim hearing)

> - Prompt version: literary_cross_chapter_stable_claim_hearing_v1_1.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE bounded conflict between an earlier chapter's stable field value (EXISTING_VALUE on FIELD of the PRIOR_CARD_SNAPSHOT) and a later chapter's observation (OBSERVED_VALUE, with the CURRENT_CARD_SNAPSHOT and CURRENT_DOSSIER_SNAPSHOT). Decide ONLY from the supplied packet and SOURCE_BLOCKS. Do not use outside knowledge about any book, and do not treat any summary text as evidence.
> - Answer with exactly one verdict from ALLOWED_VERDICTS. Definitions: uphold_existing - the new observation misreads the text and the existing value stands; correction - the earlier value was wrong when recorded, and corrected_value states the value the evidence supports; in_story_change - the value truly changed inside the story, and you MUST set effective_from_block_id to the supplied block where the new value takes effect; reveal_only - the value was always true and the story reveals it here, and you MUST set revealed_at_block_id to the supplied revealing block, with earlier chapters not knowing it; split_referent - the conflict exists because these are two different referents, so no single field value is at issue; insufficient_evidence - the supplied evidence cannot settle the conflict. Do not guess and do not average.
> - Prior fields marked provisional carry no authority. A person cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - Every verdict, including insufficient_evidence, MUST cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. Keep reason short and source-grounded.
> - If verdict is insufficient_evidence, resolution_condition MUST be a concise, specific description of the future source evidence that would settle this exact conflict. Do not claim that the missing evidence is already present, and do not invent a future block id. For every other verdict, resolution_condition MUST be null.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, and you never invent an id, field, value, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","effective_from_block_id":null,"revealed_at_block_id":null,"corrected_value":null,"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text"}],"reason":"short source-grounded reason","resolution_condition":"what future evidence would settle the conflict, or null when decided"}

---

### literary_cross_chapter_identity_hearing_v1_2 (bounded multi-candidate identity hearing)

> - Prompt version: literary_cross_chapter_identity_hearing_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE contested current referent against ALL supplied prior candidate records together. Decide ONLY from the supplied packet: PRIOR_CANDIDATE_SNAPSHOTS, CURRENT_CARD_SNAPSHOTS and CURRENT_DOSSIER_SNAPSHOTS, CANDIDATE_CONTEXTS, and SOURCE_BLOCKS. Do not use outside knowledge about any book.
> - Answer with exactly one verdict from ALLOWED_VERDICTS in your packet. Definitions: confirmed_distinct - the current referent is distinct from every supplied prior candidate; merge_referents - one referent, and you MUST set merge_target_prior_card_id to one of the supplied candidate prior card ids and adjudicate every conflicting identity field in field_adjudications (resolution keep_prior, keep_current, or mark_disputed); alias_confirmed - the proposed surface names exactly one supplied prior candidate, named in merge_target_prior_card_id; alias_rejected_distinct - it names none of the supplied prior candidates; dismiss_observation - the referred observation is not a usable entity record; keep_observation - it remains a usable record; insufficient_evidence - the supplied evidence cannot select one candidate or exclude all candidates. Do not guess and do not average.
> - A verdict may be established by a coherent chain of mutually reinforcing facts drawn from the supplied SOURCE_BLOCKS. The chain may use chronology, direct presence or agency, kinship, role, ownership or residence, forms of address, and continuity of action to connect or separate the records. No single block need explicitly state that two names are equal or distinct. Explain why the chain bears on this exact contested referent.
> - Surface overlap, surface difference, and absence of contradiction are not sufficient by themselves. Summary text and provisional dossier fields are not evidence at all: do not rest any part of a chain on them.
> - Weigh presence classes and time markers seriously: a name that appears only carved, written, quoted, or reported is not, by itself, a living participant; a referent cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - A matching surface never proves identity, and a differing surface never disproves it. Prior fields marked provisional carry no authority; treat them as unconfirmed hints.
> - For merge_referents or alias_confirmed, select exactly one merge_target_prior_card_id from the supplied candidate prior card ids. For confirmed_distinct or alias_rejected_distinct, the current referent must be distinct from every supplied candidate. If the evidence cannot select one candidate or exclude all candidates, return insufficient_evidence with resolution_condition. You may also return excluded_prior_card_ids for candidates the supplied evidence does rule out; every excluded id MUST be named by supports_excluded_prior_card_ids on at least one evidence row. Do not exclude every candidate under insufficient_evidence; use a distinct verdict instead.
> - Every verdict, including insufficient_evidence, MUST cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. SOURCE_BLOCK role labels distinguish original card evidence from bounded context; either may be cited when its text supports the reasoning. Keep reason short and source-grounded.
> - If verdict is insufficient_evidence, resolution_condition MUST be a concise, specific description of the future source evidence that would settle the remaining candidates after any exclusions. Do not claim that the missing evidence is already present, and do not invent a future block id. For every other verdict, resolution_condition MUST be null and excluded_prior_card_ids MUST be empty.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, you never rewrite a dossier, and you never invent an id, alias, field, value, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","merge_target_prior_card_id":null,"excluded_prior_card_ids":[],"field_adjudications":[{"field":"gender","resolution":"keep_prior|keep_current|mark_disputed","value":null,"basis":null}],"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text","supports_excluded_prior_card_ids":[]}],"reason":"short source-grounded reason","resolution_condition":"what future evidence would settle the remaining case, or null when decided"}

---

### literary_cross_chapter_stable_claim_hearing_v1_2 (bounded cross-chapter stable-claim hearing)

> - Prompt version: literary_cross_chapter_stable_claim_hearing_v1_2.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You adjudicate ONE bounded conflict between an earlier chapter's stable field value (EXISTING_VALUE on FIELD of the PRIOR_CARD_SNAPSHOT) and a later chapter's observation (OBSERVED_VALUE, with the CURRENT_CARD_SNAPSHOT and CURRENT_DOSSIER_SNAPSHOT). Decide ONLY from the supplied packet and SOURCE_BLOCKS. Do not use outside knowledge about any book.
> - Answer with exactly one verdict from ALLOWED_VERDICTS. Definitions: uphold_existing - the new observation misreads the text and the existing value stands; correction - the earlier value was wrong when recorded, and corrected_value states the value the evidence supports; in_story_change - the value truly changed inside the story, and you MUST set effective_from_block_id to the supplied block where the new value takes effect; reveal_only - the value was always true and the story reveals it here, and you MUST set revealed_at_block_id to the supplied revealing block, with earlier chapters not knowing it; split_referent - the conflict exists because these are two different referents, so no single field value is at issue; insufficient_evidence - the supplied evidence cannot settle the conflict. Do not guess and do not average.
> - A verdict may be established by a coherent chain of mutually reinforcing facts drawn from the supplied SOURCE_BLOCKS. The chain may use chronology, explicit descriptions, direct actions, relationships, roles, and state transitions. No single block need explicitly label the conflict as a correction, a reveal, or an in-story change. Explain why the chain resolves this exact field conflict and distinguishes the selected verdict from the alternatives.
> - Absence of contradiction is not sufficient by itself. Summary text and provisional dossier fields are not evidence at all: do not rest any part of a chain on them.
> - Prior fields marked provisional carry no authority. A person cannot act before existing or after leaving the story unless the supplied text itself says otherwise.
> - Every verdict, including insufficient_evidence, MUST cite at least one evidence row quoting a supplied source block verbatim, with its block_id. Cite only supplied block ids. SOURCE_BLOCK role labels distinguish original card evidence from bounded context; either may be cited when its text supports the reasoning. Keep reason short and source-grounded.
> - If verdict is insufficient_evidence, resolution_condition MUST be a concise, specific description of the future source evidence that would settle this exact conflict. Do not claim that the missing evidence is already present, and do not invent a future block id. For every other verdict, resolution_condition MUST be null.
> - Your answer is a proposal for deterministic code application. You grant no identity or claim authority, and you never invent an id, field, value, or block.
> - Required JSON shape: {"component_id":"b1xhear_supplied","verdict":"one_allowed_verdict","effective_from_block_id":null,"revealed_at_block_id":null,"corrected_value":null,"evidence":[{"block_id":"bk_ch01_b001","quote":"verbatim supplied text"}],"reason":"short source-grounded reason","resolution_condition":"what future evidence would settle the conflict, or null when decided"}

---

### literary_b1_scan_v1_3 (chapter entity scan with registry roster recognition)

> - Prompt version: literary_b1_scan_v1_3.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read every supplied SOURCE_BLOCK in author order. This is a lightweight scan, not a dossier builder. Record source-grounded entity surfaces and translator-relevant terms; do not write gender, age, occupation, residence, relationships, biography, aliases, canonical identity, events, dialogue attribution, or translation policy.
> - entity_observations includes named people, individualized animals or nonhuman characters, named groups, places, institutions, named texts, and individualized important objects. It may also preserve a materially important unnamed referent when losing that referent would damage later understanding.
> - Copy surface exactly as it occurs in every declared source block. source_block_ids for an observation may contain ONLY blocks that contain that exact surface; do not add a later pronoun, synonym, paraphrase, or coreferential block to that observation. Later builders may add broader referential support. Use one row for one coherent surface observation; the same surface may appear in multiple rows when the chapter itself supports distinct possible referents or distinct presence classes. Never invent an id.
> - record_class is named_entity_candidate when the text presents a usable named referent; important_unnamed_referent when an individualized unnamed referent matters but must remain chapter-local; unresolved_named_reference when a name appears but the text does not establish what kind of referent it denotes. When a name appears only on a plaque, signature, inscription, list, document, memorial, or quoted record and the chapter supplies no independent evidence of its referent kind, you MUST use unresolved_named_reference with referent_kind_claim=unknown. A person-shaped name is not evidence that the referent is a person.
> - presence_basis is direct_presence when the referent participates in the current narrated action or speech; referenced_by_other when another participant mentions it; reported_only when the chapter only reports it; inscription_or_document when it occurs only in written or quoted material; unclear when the supplied chapter cannot establish the mode.
> - Pronouns, ordinary forms of address, reusable role descriptions, kinship words, insults, and generic nouns are evidence, not standalone entity candidates. Include one only when it is necessary to preserve a materially important individualized unnamed referent; then use important_unnamed_referent and keep the observation local. Do not collect every he, she, I, you, sir, madam, master, mother, father, servant, dog, house, or similar reusable expression.
> - For important unnamed referents, include an individualized person, animal, or nonhuman participant who directly causes, undergoes, prevents, ends, or materially changes a confrontation or other consequential current-chapter action. Omit unnamed background occupations, generic imagined examples, and brief remembered relatives or acquaintances when they do not participate in the chapter's current action and are not needed to distinguish a continuing referent. Importance is about narrative function, not unusual wording.
> - glossary_observations is separate from referential entities. Include only a term whose local sense is genuinely opaque, culturally marked, regional, technical, or non-compositional enough that a later translator may misread it without a glossary. Ordinary furniture, clothing, food, weapons, scenery, and common descriptive compounds are not glossary rows merely because they are concrete nouns. Do not propose a translation or duplicate a named referent merely because its spelling may matter to translation.
> - PRIOR_CANDIDATE_PACKETS are selected mechanically because one of their stable surfaces appears in this chapter. They are candidates, not answers. Each card is shown once beside all current hit blocks; do not infer identity from candidate count, order, spelling, surname, title, or retrieval alone.
> - REGISTRY_ROSTER lists every known prior entity compactly: prior_card_id, canonical surface, known stable surfaces, kind, and record class. It exists so a referent appearing in this chapter under a DIFFERENT name, title, married or changed name, nickname, or spelling can still be recognized. The roster is retrieval context with zero authority; similarity of spelling, surname, title, or kind alone never proves recognition.
> - roster_recognition_proposals: when a current-chapter surface plainly refers to a roster entity under a different name and the supplied text itself supports that link, propose it: copy the current surface exactly as it occurs in every cited block, name the roster prior_card_id, cite one to three supporting blocks, and give one short source-grounded reason. Never propose a surface already listed among that entity's stable surfaces; same-surface continuity is handled by PRIOR_CANDIDATE_PACKETS. Never propose from spelling similarity alone, and never decide the identity: every proposal is routed to a later hearing. An empty list is the normal case.
> - prior_continuity_proposals must exact-cover every supplied prior_card_id once. Use propose_continue only when the current evidence is compatible with the same referent. Use propose_distinct only when stable identity evidence makes the same referent untenable. Use uncertain when evidence is insufficient, the prior row is only an inscription or unresolved reference, or two readings remain plausible.
> - Temporary changes in age, mood, activity, location, title, occupation, relationship, or social role are not by themselves hard identity contradictions. Do not merge or split identities, create an alias, mutate a registry, or call an Auditor. Code routes propose_distinct and uncertain to a later hearing after enrichment.
> - scan_note is one short source-grounded explanation of what this observation is and why its class/presence basis was chosen. It is not an identity summary and must not add unsupported facts.
> - Empty entity or glossary lists are valid when the chapter supports none. Do not omit a clear salient referent merely to keep output short.
> - Required JSON shape: {"schema_id":"LiteraryB1ScanOutputV1","chapter_id":"bk_ch01","entity_observations":[{"surface":"Avery Cole","source_block_ids":["bk_ch01_b001"],"referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|named_text|unknown","record_class":"named_entity_candidate|important_unnamed_referent|unresolved_named_reference","presence_basis":"direct_presence|referenced_by_other|reported_only|inscription_or_document|unclear","scan_note":"..."}],"glossary_observations":[{"surface":"...","source_block_ids":["bk_ch01_b002"],"category_hint":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other"}],"prior_continuity_proposals":[{"prior_card_id":"pcard_supplied","verdict":"propose_continue|propose_distinct|uncertain","reason_code":"consistent_current_reference|hard_identity_contradiction|same_surface_multiple_referents|prior_reference_not_established_entity|current_reference_not_established_entity|temporal_change_possible|insufficient_evidence|other","source_block_ids":["bk_ch01_b001"],"reason":"..."}],"roster_recognition_proposals":[{"surface":"...","prior_card_id":"pcard_supplied","source_block_ids":["bk_ch01_b003"],"reason":"..."}]}

---

### literary_b2_slim_interaction_window_v11 (social register and delivery tone)

> - Prompt version: literary_b2_slim_interaction_window_v11.
> - This version keeps every instruction and routing rule from literary_b2_slim_interaction_window_v10.
> - register_cue describes only social stance. Use neutral, formal, intimate, deferential, paternal, hostile, mocking, or unclear. If the social stance is known but outside that vocabulary, use register_cue=other and preserve the short description in register_cue_raw. Otherwise register_cue_raw is null.
> - delivery_tone is independent of social stance. Put a short vocal or emotional description such as mournful, trembling, pleading, or weary there when the passage supports one; otherwise use null. Do not force delivery tone into register_cue.

### literary_b1_enrich_v1_5 (open entity-link relation)

> - Prompt version: literary_b1_enrich_v1_5.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read the complete supplied chapter. Every ENTITY_TASK is a provisional B1-Scan observation with one inline continuity packet. Exact-cover every scan_observation_id with exactly one entity dossier OR one spurious_challenge. Never silently omit a task and never invent an entity id.
> - continuity.state=new_candidate means build from this chapter. continuity.state=continue_prior includes the one prior card that B1-Scan allowed for this task; use it only as continuity context and emit only new or changed source-grounded claims. continuity.state=linkage_pending intentionally withholds prior dossiers: build independently from current chapter evidence, do not infer what was withheld, and do not decide cross-chapter identity.
> - A prior card is a candidate, not an answer. If a continue_prior card conflicts with current evidence, emit conflict_findings instead of silently choosing either value. The runner retains continuity_case_ids for the later cross-chapter hearing.
> - This is an identity-enrichment pass, not an event or dialogue pass. Do not summarize scene actions, moods, temporary gestures, event sequences, speaker turns, translation choices, or plot consequences. identity_summary is at most two compact sentences and contains only durable identifying information or an explicit statement that the referent remains unresolved.
> - claims use field, status, value, basis, anchor_block_ids, and story_time_note. status=supported requires a non-null value, basis in explicit_textual|self_identification|contextual_inference, and source anchors. status=unclear requires value=null and basis=null but still cites the blocks that leave the field unresolved. status=not_applicable requires value=null, basis=not_applicable, and anchor_block_ids=[]. Omit inapplicable optional fields rather than manufacturing values.
> - For every person task, attempt gender and life_stage. Either support gender with masculine|feminine|neutral|mixed or return it as unclear; either support life_stage with child|youth|adult|elder or return it as unclear. A person's name shape alone may be only contextual_inference and never explicit evidence.
> - For animal tasks attempt species. For place, group_reference, object, institution, and named_text tasks attempt respectively place_type, group_type, object_type, institution_type, and document_type. Use unclear when the chapter cannot say. Gender applies only to person, animal, or nonhuman_character.
> - Emit referent_kind only when you challenge the Scan kind. Do not upgrade an inscription, date, label, signature, or quoted name into a living person without independent chapter evidence. Preserve unresolved identity honestly. role_or_occupation is conditional; exact numeric age wording belongs verbatim in story_time_note and must not be calculated into a band.
> - aliases_observed contains only stable name-like alternatives copied verbatim from the chapter. Pronouns, ordinary address terms, reusable roles, kinship words, insults, and descriptions are not aliases. Every alias remains proposed.
> - same_referent_proposals is a proposal channel, never a decision. Use it only when an important_unnamed_referent such as a chapter-local description is linked by the supplied chapter evidence to one named_entity_candidate already present in ENTITY_TASKS. Put the unnamed task_ref in subject_ref and the named task_ref in target_ref, use proposal_basis=chapter_context_description, and cite evidence for both sides. Do not use this field for two names that merely overlap in spelling; code separately proposes mechanical name variants. When uncertain, emit no proposal and leave the dossiers separate.
> - A description accepted as coreferential remains evidence only. It must not become an alias, canonical surface, stable surface, or future retrieval key. The later Local Auditor alone may accept, reject, defer, or refer each supplied proposal.
> - address_forms_used is stored on the speaker's dossier and is evidence, not an alias. kinship_links and links are source-grounded, chapter-as-of proposals. Use only supplied task_ref values for counterparts and targets; do not infer reverse links merely for completeness.
> - For links, use resides_at, owned_by, held_by, part_of, member_of, head_of, or serves_in when exact. When the source-grounded relation is known but outside that vocabulary, use relation=other_link and put a short verbatim-or-close description in relation_note. For a listed relation use relation_note=null. Preserve the direction written by the chapter. Kinship keeps its separate relation family and existing other_kin rule.
> - additional_entities is a recall backstop for a genuinely omitted named entity or individualized participant. spurious_challenges disputes a Scan task but never deletes it; the Local Auditor decides. If supplied continued information conflicts with chapter evidence, emit conflict_findings. If Scan presence_basis is wrong, emit presence_correction_findings.
> - Exact-cover every GLOSSARY_TASK with one glossary_item. contextual_sense explains only the local English sense; do not propose or enforce a translation. ambiguity_status is clear|ambiguous|unclear.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichOutputV1","chapter_id":"<the chapter_id supplied in this request>","entities":[{"scan_observation_id":"<a supplied local scan-observation label>","claims":[{"field":"gender","status":"supported|unclear|not_applicable","value":"...|null","basis":"explicit_textual|self_identification|contextual_inference|not_applicable|null","anchor_block_ids":["<a block_id supplied in this request>"],"story_time_note":null}],"kinship_links":[],"links":[{"relation":"resides_at|owned_by|held_by|part_of|member_of|head_of|serves_in|other_link","relation_note":null,"target_ref":"<a supplied task_ref>","basis":"explicit_textual|self_identification|contextual_inference","anchor_block_ids":["<a supplied block_id>"]}],"address_forms_used":[],"aliases_observed":[],"identity_summary":"...","distinguishing_note":null}],"additional_entities":[],"spurious_challenges":[],"same_referent_proposals":[{"subject_ref":"<the supplied task_ref for the unnamed observation>","target_ref":"<the supplied task_ref for the named observation>","proposal_basis":"chapter_context_description","source_block_ids":["<supplied block ids for both observations>"],"reason":"short source-grounded reason"}],"conflict_findings":[],"presence_correction_findings":[],"glossary_items":[]}

### literary_b1_enrich_local_auditor_v1_6 (open relation revision)

> - Prompt version: literary_b1_enrich_local_auditor_v1_6.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent.
> - Resolve only supplied components. Exact-cover every supplied component exactly once. Never create or decide an unasked component, entity, fact, relation, alias, block, or id. accept_proposal accepts only the specific chapter-local proposal shown. reject_proposal rejects only that proposal while preserving history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - Components and cards are proposals, not answers. A matching surface, overlapping name tokens, supplied card, or unique candidate does not prove identity. Use only supplied source blocks. Every decision cites at least one direct evidence block for its component.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For an entity_link revised to other_link, revised_relation_note is a required short description of the source-grounded relation. For a kinship_link revised to other_kin, revised_relation_note is likewise required and must name the source-grounded kinship or in-law relation. Other revisions use revised_relation_note=null. For every non-revision action, all three revision fields are null.
> - For same_referent_proposal, accept_proposal means the two supplied observations denote one referent in this chapter. It grants chapter-only grouping and no book-wide identity authority. reject_proposal keeps them distinct. keep_pending or refer_cross_chapter keeps both cards separate. A proposal_basis=name_variant is only a mechanical string clue; a proposal_basis=chapter_context_description is an Enrich reading. Neither is proof by itself.
> - An accepted descriptive observation remains evidence only: it is not a canonical name, alias, stable surface, or future retrieval key. An accepted named variant may become a chapter-confirmed stable name surface, but still grants no book-wide alias authority.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis. Alias acceptance grants chapter-only evidence status and never global lookup authority. Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory.
> - You may record a possible same-referent pair that was NOT supplied as a component in unasked_same_referent_observations. This is a non-authoritative note for a later pass, not a current decision. Use only two visible ENTITY_CARD refs and supplied source blocks. Do not repeat a pair already represented by a supplied same_referent_proposal. An empty list is normal.
> - Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations. In resolution_note and observation reason, use chapter surface names rather than transport labels when prose can identify the referent.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"<the chapter_id supplied in this request>","manifest_hash":"64_hex_chars","decisions":[{"component_id":"<a supplied local component label>","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_relation_note":null,"revised_target_ref":null,"source_block_ids":["<a block_id supplied in this request>"],"resolution_note":"..."}],"unasked_same_referent_observations":[{"subject_ref":"<one visible ENTITY_CARD local ref>","target_ref":"<another visible ENTITY_CARD local ref>","source_block_ids":["<a supplied block id>"],"reason":"short source-grounded observation for a later pass"}]}

### literary_b1_enrich_local_auditor_v1_5 (open entity-link revision)

> - Prompt version: literary_b1_enrich_local_auditor_v1_5.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - You receive one chapter-local EXCEPTION_COMPONENT batch produced mechanically from a validated B1-Enrich artifact, compact ENTITY_CARDS, and bounded SOURCE_BLOCKS. Clean scalar dossier claims and cross-chapter stable-claim conflicts are intentionally absent.
> - Resolve only supplied components. Exact-cover every supplied component exactly once. Never create or decide an unasked component, entity, fact, relation, alias, block, or id. accept_proposal accepts only the specific chapter-local proposal shown. reject_proposal rejects only that proposal while preserving history. keep_pending preserves it without authority. refer_cross_chapter is for a supplied proposal that genuinely needs evidence from another chapter; it is not permitted for a purely chapter-local presence correction.
> - Components and cards are proposals, not answers. A matching surface, overlapping name tokens, supplied card, or unique candidate does not prove identity. Use only supplied source blocks. Every decision cites at least one direct evidence block for its component.
> - revise_proposal is allowed only for entity_link or kinship_link. revised_relation must remain in that component's relation family, and revised_target_ref must copy one ALLOWED_TARGET_REF. For an entity_link revised to other_link, revised_relation_note is a required short description of the source-grounded relation. Other entity-link revisions use revised_relation_note=null. Kinship revision behavior is unchanged and uses revised_relation_note=null. For every non-revision action, all three revision fields are null.
> - For same_referent_proposal, accept_proposal means the two supplied observations denote one referent in this chapter. It grants chapter-only grouping and no book-wide identity authority. reject_proposal keeps them distinct. keep_pending or refer_cross_chapter keeps both cards separate. A proposal_basis=name_variant is only a mechanical string clue; a proposal_basis=chapter_context_description is an Enrich reading. Neither is proof by itself.
> - An accepted descriptive observation remains evidence only: it is not a canonical name, alias, stable surface, or future retrieval key. An accepted named variant may become a chapter-confirmed stable name surface, but still grants no book-wide alias authority.
> - For spurious_challenge, accept_proposal makes the Scan row dormant only in this chapter; reject_proposal retains it. For presence_correction, accept_proposal selects the proposed basis; reject_proposal retains the Scan basis. Alias acceptance grants chapter-only evidence status and never global lookup authority. Additional-entity acceptance permits only a chapter-local proposal. Glossary ambiguity remains chapter-scoped and advisory.
> - You may record a possible same-referent pair that was NOT supplied as a component in unasked_same_referent_observations. This is a non-authoritative note for a later pass, not a current decision. Use only two visible ENTITY_CARD refs and supplied source blocks. Do not repeat a pair already represented by a supplied same_referent_proposal. An empty list is normal.
> - Do not rewrite identity summaries, clean scalar claims, dialogue, events, or translations. In resolution_note and observation reason, use chapter surface names rather than transport labels when prose can identify the referent.
> - Required JSON shape: {"schema_id":"LiteraryB1EnrichLocalAuditOutputV1","chapter_id":"<the chapter_id supplied in this request>","manifest_hash":"64_hex_chars","decisions":[{"component_id":"<a supplied local component label>","action":"accept_proposal|revise_proposal|reject_proposal|keep_pending|refer_cross_chapter","revised_relation":null,"revised_relation_note":null,"revised_target_ref":null,"source_block_ids":["<a block_id supplied in this request>"],"resolution_note":"..."}],"unasked_same_referent_observations":[{"subject_ref":"<one visible ENTITY_CARD local ref>","target_ref":"<another visible ENTITY_CARD local ref>","source_block_ids":["<a supplied block id>"],"reason":"short source-grounded observation for a later pass"}]}

### literary_b1_scan_v1_5 (glossary category raw escape)

> - Prompt version: literary_b1_scan_v1_5.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read every supplied SOURCE_BLOCK in author order. This is a lightweight scan, not a dossier builder. Record source-grounded entity surfaces and translator-relevant terms; do not write gender, age, occupation, residence, relationships, biography, aliases, canonical identity, events, dialogue attribution, or translation policy.
> - entity_observations includes named people, individualized animals or nonhuman characters, named groups, places, institutions, named texts, and individualized important objects. It may also preserve a materially important unnamed referent when losing that referent would damage later understanding.
> - Copy surface exactly as it occurs in the chapter, without surrounding quotation marks or a trailing stop. source_block_ids are the blocks where you judge this referent to be present: normally blocks carrying that surface, and you may include a block that carries the same referent through a pronoun, an epithet, or unattributed dialogue when the passage itself makes that plain. Cite up to eight blocks, earliest first, and stop there even when more exist; the count bounds evidence and is never a claim about total occurrences. Use one row for one coherent surface observation; the same surface may appear in multiple rows when the chapter itself supports distinct possible referents or distinct presence classes. Never invent an id.
> - record_class is named_entity_candidate when the text presents a usable named referent; important_unnamed_referent when an individualized unnamed referent matters but must remain chapter-local; unresolved_named_reference when a name appears but the text does not establish what kind of referent it denotes. When a name appears only on a plaque, signature, inscription, list, document, memorial, or quoted record and the chapter supplies no independent evidence of its referent kind, you MUST use unresolved_named_reference with referent_kind_claim=unknown. A person-shaped name is not evidence that the referent is a person.
> - presence_basis is direct_presence when the referent participates in the current narrated action or speech; referenced_by_other when another participant mentions it; reported_only when the chapter only reports it; inscription_or_document when it occurs only in written or quoted material; unclear when the supplied chapter cannot establish the mode.
> - Pronouns, ordinary forms of address, reusable role descriptions, kinship words, insults, and generic nouns are evidence, not standalone entity candidates. Include one only when it is necessary to preserve a materially important individualized unnamed referent; then use important_unnamed_referent and keep the observation local. Do not collect every he, she, I, you, sir, madam, master, mother, father, servant, dog, house, or similar reusable expression.
> - For important unnamed referents, include an individualized person, animal, or nonhuman participant who directly causes, undergoes, prevents, ends, or materially changes a confrontation or other consequential current-chapter action. Omit unnamed background occupations, generic imagined examples, and brief remembered relatives or acquaintances when they do not participate in the chapter's current action and are not needed to distinguish a continuing referent. Importance is about narrative function, not unusual wording.
> - glossary_observations is separate from referential entities. Include only a term whose local sense is genuinely opaque, culturally marked, regional, technical, or non-compositional enough that a later translator may misread it without a glossary. Ordinary furniture, clothing, food, weapons, scenery, and common descriptive compounds are not glossary rows merely because they are concrete nouns. Do not propose a translation or duplicate a named referent merely because its spelling may matter to translation. category_hint uses cultural_term, regional_term, technical_term, place_term, object_term, institution_term, or other. When category_hint=other, term_category_raw is a required short description of the known category; otherwise term_category_raw is null. other means known but unlisted, not unclear.
> - PRIOR_CANDIDATE_PACKETS are selected mechanically because one of their stable surfaces appears in this chapter. They are candidates, not answers. Each card is shown once beside all current hit blocks; do not infer identity from candidate count, order, spelling, surname, title, or retrieval alone.
> - REGISTRY_ROSTER lists every known prior entity compactly: prior_card_id, canonical surface, known stable surfaces, kind, and record class. It exists so a referent appearing in this chapter under a DIFFERENT name, title, married or changed name, nickname, or spelling can still be recognized. The roster is retrieval context with zero authority; similarity of spelling, surname, title, or kind alone never proves recognition.
> - roster_recognition_proposals: when a current-chapter surface plainly refers to a roster entity under a different name and the supplied text itself supports that link, propose it: copy the current surface exactly as it occurs in every cited block, name the roster prior_card_id, cite up to eight supporting blocks, and give one short source-grounded reason. Never propose a surface already listed among that entity's stable surfaces; same-surface continuity is handled by PRIOR_CANDIDATE_PACKETS. Never propose from spelling similarity alone, and never decide the identity: every proposal is routed to a later hearing. An empty list is the normal case.
> - prior_continuity_proposals must exact-cover every supplied prior_card_id once. Use propose_continue only when the current evidence is compatible with the same referent. Use propose_distinct only when stable identity evidence makes the same referent untenable. Use uncertain when evidence is insufficient, the prior row is only an inscription or unresolved reference, or two readings remain plausible.
> - Temporary changes in age, mood, activity, location, title, occupation, relationship, or social role are not by themselves hard identity contradictions. Do not merge or split identities, create an alias, mutate a registry, or call an Auditor. Code routes propose_distinct and uncertain to a later hearing after enrichment.
> - scan_note is one short source-grounded explanation of what this observation is and why its class/presence basis was chosen. It is not an identity summary and must not add unsupported facts.
> - Empty entity or glossary lists are valid when the chapter supports none. Do not omit a clear salient referent merely to keep output short.
> - Required JSON shape: {"schema_id":"LiteraryB1ScanOutputV1","chapter_id":"bk_ch01","entity_observations":[{"surface":"Avery Cole","source_block_ids":["bk_ch01_b001"],"referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|named_text|unknown","record_class":"named_entity_candidate|important_unnamed_referent|unresolved_named_reference","presence_basis":"direct_presence|referenced_by_other|reported_only|inscription_or_document|unclear","scan_note":"..."}],"glossary_observations":[{"surface":"...","source_block_ids":["bk_ch01_b002"],"category_hint":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other","term_category_raw":null}],"prior_continuity_proposals":[{"prior_card_id":"pcard_supplied","verdict":"propose_continue|propose_distinct|uncertain","reason_code":"consistent_current_reference|hard_identity_contradiction|same_surface_multiple_referents|prior_reference_not_established_entity|current_reference_not_established_entity|temporal_change_possible|insufficient_evidence|other","source_block_ids":["bk_ch01_b001"],"reason":"..."}],"roster_recognition_proposals":[{"surface":"...","prior_card_id":"pcard_supplied","source_block_ids":["bk_ch01_b003"],"reason":"..."}]}

---

---

### literary_b1_scan_v1_4 (chapter entity scan with coreferential evidence)

> - Prompt version: literary_b1_scan_v1_4.
> - Return exactly one valid JSON object matching the Required JSON shape. Do not add prose outside the JSON object.
> - Read every supplied SOURCE_BLOCK in author order. This is a lightweight scan, not a dossier builder. Record source-grounded entity surfaces and translator-relevant terms; do not write gender, age, occupation, residence, relationships, biography, aliases, canonical identity, events, dialogue attribution, or translation policy.
> - entity_observations includes named people, individualized animals or nonhuman characters, named groups, places, institutions, named texts, and individualized important objects. It may also preserve a materially important unnamed referent when losing that referent would damage later understanding.
> - Copy surface exactly as it occurs in the chapter, without surrounding quotation marks or a trailing stop. source_block_ids are the blocks where you judge this referent to be present: normally blocks carrying that surface, and you may include a block that carries the same referent through a pronoun, an epithet, or unattributed dialogue when the passage itself makes that plain. Cite up to eight blocks, earliest first, and stop there even when more exist; the count bounds evidence and is never a claim about total occurrences. Use one row for one coherent surface observation; the same surface may appear in multiple rows when the chapter itself supports distinct possible referents or distinct presence classes. Never invent an id.
> - record_class is named_entity_candidate when the text presents a usable named referent; important_unnamed_referent when an individualized unnamed referent matters but must remain chapter-local; unresolved_named_reference when a name appears but the text does not establish what kind of referent it denotes. When a name appears only on a plaque, signature, inscription, list, document, memorial, or quoted record and the chapter supplies no independent evidence of its referent kind, you MUST use unresolved_named_reference with referent_kind_claim=unknown. A person-shaped name is not evidence that the referent is a person.
> - presence_basis is direct_presence when the referent participates in the current narrated action or speech; referenced_by_other when another participant mentions it; reported_only when the chapter only reports it; inscription_or_document when it occurs only in written or quoted material; unclear when the supplied chapter cannot establish the mode.
> - Pronouns, ordinary forms of address, reusable role descriptions, kinship words, insults, and generic nouns are evidence, not standalone entity candidates. Include one only when it is necessary to preserve a materially important individualized unnamed referent; then use important_unnamed_referent and keep the observation local. Do not collect every he, she, I, you, sir, madam, master, mother, father, servant, dog, house, or similar reusable expression.
> - For important unnamed referents, include an individualized person, animal, or nonhuman participant who directly causes, undergoes, prevents, ends, or materially changes a confrontation or other consequential current-chapter action. Omit unnamed background occupations, generic imagined examples, and brief remembered relatives or acquaintances when they do not participate in the chapter's current action and are not needed to distinguish a continuing referent. Importance is about narrative function, not unusual wording.
> - glossary_observations is separate from referential entities. Include only a term whose local sense is genuinely opaque, culturally marked, regional, technical, or non-compositional enough that a later translator may misread it without a glossary. Ordinary furniture, clothing, food, weapons, scenery, and common descriptive compounds are not glossary rows merely because they are concrete nouns. Do not propose a translation or duplicate a named referent merely because its spelling may matter to translation.
> - PRIOR_CANDIDATE_PACKETS are selected mechanically because one of their stable surfaces appears in this chapter. They are candidates, not answers. Each card is shown once beside all current hit blocks; do not infer identity from candidate count, order, spelling, surname, title, or retrieval alone.
> - REGISTRY_ROSTER lists every known prior entity compactly: prior_card_id, canonical surface, known stable surfaces, kind, and record class. It exists so a referent appearing in this chapter under a DIFFERENT name, title, married or changed name, nickname, or spelling can still be recognized. The roster is retrieval context with zero authority; similarity of spelling, surname, title, or kind alone never proves recognition.
> - roster_recognition_proposals: when a current-chapter surface plainly refers to a roster entity under a different name and the supplied text itself supports that link, propose it: copy the current surface exactly as it occurs in every cited block, name the roster prior_card_id, cite up to eight supporting blocks, and give one short source-grounded reason. Never propose a surface already listed among that entity's stable surfaces; same-surface continuity is handled by PRIOR_CANDIDATE_PACKETS. Never propose from spelling similarity alone, and never decide the identity: every proposal is routed to a later hearing. An empty list is the normal case.
> - prior_continuity_proposals must exact-cover every supplied prior_card_id once. Use propose_continue only when the current evidence is compatible with the same referent. Use propose_distinct only when stable identity evidence makes the same referent untenable. Use uncertain when evidence is insufficient, the prior row is only an inscription or unresolved reference, or two readings remain plausible.
> - Temporary changes in age, mood, activity, location, title, occupation, relationship, or social role are not by themselves hard identity contradictions. Do not merge or split identities, create an alias, mutate a registry, or call an Auditor. Code routes propose_distinct and uncertain to a later hearing after enrichment.
> - scan_note is one short source-grounded explanation of what this observation is and why its class/presence basis was chosen. It is not an identity summary and must not add unsupported facts.
> - Empty entity or glossary lists are valid when the chapter supports none. Do not omit a clear salient referent merely to keep output short.
> - Required JSON shape: {"schema_id":"LiteraryB1ScanOutputV1","chapter_id":"bk_ch01","entity_observations":[{"surface":"Avery Cole","source_block_ids":["bk_ch01_b001"],"referent_kind_claim":"person|animal|nonhuman_character|group_reference|place|object|institution|named_text|unknown","record_class":"named_entity_candidate|important_unnamed_referent|unresolved_named_reference","presence_basis":"direct_presence|referenced_by_other|reported_only|inscription_or_document|unclear","scan_note":"..."}],"glossary_observations":[{"surface":"...","source_block_ids":["bk_ch01_b002"],"category_hint":"cultural_term|regional_term|technical_term|place_term|object_term|institution_term|other"}],"prior_continuity_proposals":[{"prior_card_id":"pcard_supplied","verdict":"propose_continue|propose_distinct|uncertain","reason_code":"consistent_current_reference|hard_identity_contradiction|same_surface_multiple_referents|prior_reference_not_established_entity|current_reference_not_established_entity|temporal_change_possible|insufficient_evidence|other","source_block_ids":["bk_ch01_b001"],"reason":"..."}],"roster_recognition_proposals":[{"surface":"...","prior_card_id":"pcard_supplied","source_block_ids":["bk_ch01_b003"],"reason":"..."}]}
